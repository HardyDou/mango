# Mockito 测试治理清单

## 1. 定位

- 本清单服务 GitHub issue #183。
- 长期测试规则以 `mango-pmo/rules/backend/08-test.md` 为准。
- 本清单只记录存量 Mockito 测试的分类、迁移优先级和治理进度。
- 当前清单是 #183 第一阶段治理范围，不代表全仓库 Mockito 已完成盘点。
- 第一阶段覆盖 `auth`、`identity`、`authorization`、`workflow`、`payment`、`notice`；infra、tools、org、cms、captcha 等模块后续按同一口径补充。

## 2. 分类

| 分类 | 含义 | 处理 |
|---|---|---|
| KEEP | 测试目标清楚，mock 只替换目标之外的协作者 | 保留 |
| MIGRATE | mock 掉被测核心链路 | 改为真实集成测试或组件测试 |
| REWRITE | 测试有价值但断言、物料或结构错误 | 重写 |
| DELETE | 只测常量、字段赋值、调用次数，或复述实现 | 删除 |

## 3. 判断问题

每个存量 Mockito 测试至少回答：

1. 测试目标是什么？
2. 被测目标是否真实执行？
3. mock 是否替换了关键验收链路？
4. 如果需要真实数据库，采用哪种隔离物料和清理策略？

数据库测试隔离和物料选择以 `mango-pmo/rules/backend/08-test.md` 为准。不能为了避免脏数据而 mock Mapper 或数据库结果；应使用 H2、Testcontainers、专用测试库、事务回滚或显式清理。

## 4. 初始扫描命令

```bash
rg -n "Mockito|mock\(|@Mock|MockBean|mockito" \
  mango/mango-platform/mango-auth \
  mango/mango-platform/mango-identity \
  mango/mango-platform/mango-authorization \
  mango/mango-platform/mango-workflow \
  mango/mango-platform/mango-payment \
  mango/mango-platform/mango-notice \
  -g "*.java" -g "pom.xml"
```

## 5. 治理台账

| 模块 | 命中文件 | 当前判断 | 原因 | 处理优先级 | 跟进 |
|---|---:|---|---|---|---|
| mango-authorization-core | 7 | MIGRATE | `service/impl` 测试 mock Mapper，不能证明权限/菜单持久化链路 | P1 | issue #183 |
| mango-authorization-starter | 7 | MIGRATE | resource handler 测试仍有 4 个 mock service/mapper；`AuthRoleResourceHandlerIntegrationTest` 已作为样板改造 | P1 | issue #183 |
| mango-identity-core | 4 | MIGRATE | 用户/成员 service 测试 mock Mapper，不能证明组织、成员、角色清理链路 | P1 | issue #183 |
| mango-identity-starter | 2 | REWRITE | provider/resource handler 测试需要区分外部 API 替身和真实落库验收 | P1 | issue #183 |
| mango-workflow-core | 7 | MIGRATE | 流程定义、运行时和任务测试 mock Mapper/Flowable 关键协作，需拆分真实数据库集成和外部引擎替身 | P1 | issue #183 |
| mango-payment-core | 33 | REWRITE | 支付通道外部系统 mock 多数合理，但 serviceImpl/Mapper 路径需补真实持久化验收 | P2 | issue #183 |
| mango-notice-core | 3 | REWRITE | outbox/announcement/notice service 需区分外部发送替身和真实持久化链路 | P2 | issue #183 |
| mango-notice-starter | 2 | KEEP | controller/autoconfiguration 测试以装配或 Web 边界为主，保留但需避免声明真实业务链路 | P2 | issue #183 |
| mango-auth-starter | 4 | KEEP/REWRITE | 安全配置和拦截器可使用替身；E2E 中 mock 外部 notice API 需要明确为外部系统替身 | P2 | issue #183 |
| mango-authorization-support | 2 | KEEP | token blacklist 测试 mock KV 存储属于旁路能力替身，保留并避免声称覆盖 KV 实现 | P2 | issue #183 |

## 6. 样板改造

| 测试 | 分类 | 测试目标 | mock 边界 | 真实链路 | 处理状态 |
|---|---|---|---|---|---|
| `AuthRoleResourceHandlerTest` | MIGRATE | 验证 `AUTH_ROLE` resource handler 将资源声明同步到 `authorization_role` | 原测试 mock `RoleMapper`，替换了关键落库链路 | 已改为 `AuthRoleResourceHandlerIntegrationTest`，使用 H2、MyBatis-Plus 和真实 `RoleMapper` 覆盖 create、business-key update、disable | DONE |
| `RoleDataScopeServiceImplTest` | MIGRATE | 验证角色数据范围保存与解析 | 原测试 mock `RoleDataScopeMapper`、`RoleMapper`、`MenuMapper`、`RoleMenuMapper`、`SubjectRoleBindingMapper`，替换了权限数据范围核心链路 | 已改为 `RoleDataScopeServiceImplIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖无角色 fallback、角色授权 list 资源后落库、主体角色与数据范围合并解析 | DONE |
| `ApiResourceServiceImplTest` | MIGRATE | 验证 API resource 扫描注册、失效旧路径和运行时访问决策 | 原测试 spy 被测 `ApiResourceServiceImpl` 并 mock `list`、`saveBatch`、`updateBatchById`，替换了关键持久化链路 | 已改为 `ApiResourceServiceImplIntegrationTest`，使用 H2、MyBatis-Plus 和真实 `ApiResourceMapper` 覆盖旧路径禁用和访问决策解析 | DONE |
| `SubjectAuthorityServiceImplTest` | MIGRATE | 验证主体角色到菜单权限解析 | 原测试 mock `SubjectRoleBindingMapper`、`RoleMenuMapper`、`MenuMapper`，替换了授权查询核心链路 | 已改为 `SubjectAuthorityServiceImplIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖权限字段优先、legacy menuCode fallback、非法 tenant 过滤 | DONE |
| `TenantMenuPackageBindingHandlerTest` | MIGRATE | 验证租户绑定套餐后管理员角色菜单授权重建 | 原测试 mock `RoleMapper`、`RoleMenuMapper`、`MenuMapper`，替换了角色菜单落库链路 | 已改为 `TenantMenuPackageBindingHandlerIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖租户上下文切换、旧绑定清理、菜单父级展开和新绑定落库；套餐菜单列表保留测试替身作为外部输入 | DONE |
| `MenuServiceImplTest` | MIGRATE | 验证菜单查询、树构建、用户菜单过滤、runtime config 和删除保护 | 原测试 mock `MenuMapper`、`FrontendMenuRuntimeConfigMapper` 等 Mapper，替换了菜单持久化链路 | 已改为 `MenuServiceImplIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖 runtime config 读写、隐藏启用菜单路由保留、树结构和叶子删除清理；主体权限服务保留测试替身作为外部授权输入 | DONE |
| `RoleServiceImplTest` | MIGRATE | 验证角色 CRUD、主体角色分配、菜单授权和关系清理 | 原测试 mock `RoleMapper`、`SubjectRoleBindingMapper`、`RoleMenuMapper`、`MenuMapper`，替换了角色授权核心持久化链路 | 已改为 `RoleServiceImplIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖角色创建/更新、删除级联清理、主体角色替换、菜单授权防越权与落库；菜单树和主体权限服务保留测试替身作为外部输入 | DONE |
| `AppModuleServiceImplTest` | MIGRATE | 验证应用模块资源清单注册、菜单/按钮/runtime config、套餐/角色绑定和禁用清理 | 原测试 mock `AuthorizationAppModuleMapper`、`MenuMapper`、`FrontendMenuRuntimeConfigMapper`、`MenuPackageMapper`、`MenuPackageItemMapper`、`RoleMapper`、`RoleMenuMapper`，替换了资源注入落库链路 | 已改为 `AppModuleServiceImplIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖菜单树/按钮落库、runtime config、套餐/角色绑定、禁用派生清理；租户套餐查询/绑定回调保留测试替身作为外部边界 | DONE |
| `AuthRoleDataScopeResourceHandlerTest` | MIGRATE | 验证 `AUTH_ROLE_DATA_SCOPE` resource handler 解析角色、组织和数据范围声明并落库 | 原测试 mock `RoleMapper`、`RoleDataScopeMapper`、`SysOrgMapper`，替换了资源注入落库链路 | 已改为 `AuthRoleDataScopeResourceHandlerIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖 scopeValues JSON、业务键更新、缺失角色/组织异常、组织编码解析、禁用声明和 disable 状态更新 | DONE |
| `IdentityUserSecurityServiceTest` | MIGRATE | 验证登录失败锁定、成功登录清理、强制改密清理和安全状态开关 | 原测试 mock `IdentityUserMapper`，只能证明内存对象变化，不能证明安全状态真实写回 | 已改为 `IdentityUserSecurityServiceIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖失败锁定、登录成功清理、强制改密清理、过期失败窗口、管理员解锁和强制重置标记；迁移中修复 MyBatis-Plus `updateById` 默认不写 null 导致锁定时间未清理的问题 | DONE |
| `IdentityUserServiceImplTest` | MIGRATE | 验证身份资料查询、目标用户解析、成员删除关系清理和外部身份绑定修复成员关系 | 原测试 mock `IdentityUserMapper`、`TenantMemberMapper`、`TenantMemberOrgMapper`、`ExternalIdentityBindingMapper`，替换了用户/成员/外部身份持久化核心链路 | 已改为 `IdentityUserServiceImplIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖用户资料查询、部门/角色目标解析、批量删除成员与组织关系清理、跳过当前用户删除、外部身份绑定自动修复成员并落库；`RoleBindingApi` 保留测试替身作为外部授权服务边界 | DONE |
| `IdentityUserResourceHandlerTest` | MIGRATE | 验证 `IDENTITY_USER` resource handler 创建/更新身份用户、租户成员和禁用状态 | 原测试 mock `IdentityUserMapper`、`TenantMemberMapper`，替换了资源注入落库链路 | 已改为 `IdentityUserResourceHandlerIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖初始密码加密保存、租户成员创建、业务键更新、禁用声明和 disable 状态更新 | DONE |
| `NoticeAnnouncementServiceTest` | MIGRATE | 验证公告发布 fan-out、公告可见性、阅读/确认状态和我的公告分页查询 | 原测试 mock `NoticeAnnouncementMapper`、`NoticeAnnouncementTargetMapper`、`NoticeAnnouncementRecipientMapper`，替换了公告持久化和分页 join 查询链路 | 已改为 `NoticeAnnouncementServiceIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖目标/接收人落库、去重、状态更新、可见性检查、阅读/确认写回和自定义分页 SQL；`NoticeRecipientResolver` 和 `INoticeService` 保留为外部边界替身 | DONE |
| `NoticeServiceTest` | MIGRATE | 验证通知发送入库、任务执行、站内信状态、业务配置发布和渠道配置敏感值保留 | 原测试 mock `NoticeTaskMapper`、`NoticeRecipientMapper`、`NoticeSendRecordMapper`、`NoticeSiteMessageMapper`、`NoticeBusinessTypeMapper`、`NoticeBusinessConfigVersionMapper`、`NoticeBusinessChannelTemplateMapper`、`NoticeChannelConfigMapper` 等 Mapper，替换了通知核心持久化链路，且包含大量只验证 mock 流转的低价值用例 | 已改为 `NoticeServiceIntegrationTest`，使用 H2、MyBatis-Plus 和真实 Mapper 覆盖发送任务、接收人、发送记录、outbox、任务执行、站内信落库/已读/删除、业务配置版本发布和渠道配置保存；外部身份、组织、企业微信和 outbox 存储保留测试替身作为外部边界；删除原 57 个 mock-heavy 用例，保留 6 个高价值链路用例 | DONE |
| `PaymentApplicationServiceImplTest` | MIGRATE | 验证支付应用创建时密钥加密落库、删除前关联检查和逻辑删除 | 原测试 mock `PaymentApplicationMapper`，无法证明 `insert`、`deleteById` 和 `PaymentApplicationMapper.xml#countDeleteRelations` 真实 SQL 可用 | 已改为 `PaymentApplicationServiceImplIntegrationTest`，使用 H2、MyBatis-Plus、真实 `PaymentApplicationMapper` 和真实 Mapper XML 覆盖密钥加密后的持久化、关联存在时拒绝删除、无有效关联时逻辑删除；审计服务和敏感值加密服务保留测试替身作为非数据库边界 | DONE |
| `PaymentEnterpriseSubjectServiceImplTest` | MIGRATE | 验证支付企业主体创建/详情脱敏/更新、默认银行账户同步、删除前关联检查和逻辑删除 | 原测试 mock `PaymentEnterpriseSubjectMapper`、`PaymentSubjectBankAccountMapper`，无法证明企业主体、默认银行账户真实落库，也无法证明 `PaymentEnterpriseSubjectMapper.xml#countDeleteRelations` 真实 SQL 可用 | 已改为 `PaymentEnterpriseSubjectServiceImplIntegrationTest`，使用 H2、MyBatis-Plus、真实 `PaymentEnterpriseSubjectMapper`/`PaymentSubjectBankAccountMapper` 和真实 Mapper XML 覆盖敏感字段加密持久化、默认银行账户插入/更新、详情脱敏、关联存在时拒绝删除、无有效关联时逻辑删除；审计服务和敏感值服务保留测试替身作为非数据库边界 | DONE |
| `PaymentCashierConfigServiceImplTest` | MIGRATE | 验证收银台配置创建、关联对象校验、默认收银台唯一性、展示顺序校验、删除前关联检查和逻辑删除 | 原测试 mock `PaymentCashierConfigMapper`、`PaymentApplicationMapper`、`PaymentEnterpriseSubjectMapper`、`PaymentMethodMapper`，无法证明关联校验、默认收银台唯一性和 `PaymentCashierConfigMapper.xml#countDeleteRelations` 真实 SQL 可用 | 已改为 `PaymentCashierConfigServiceImplIntegrationTest`，使用 H2、MyBatis-Plus、真实 Mapper 和真实 Mapper XML 覆盖配置落库、应用/企业主体/支付方式校验、重复默认收银台拒绝、展示顺序拒绝、支付订单关联拦截和逻辑删除；审计服务保留测试替身作为非数据库边界 | DONE |
| `PaymentMethodServiceImplTest` | MIGRATE | 验证支付方式创建、三级分类路径校验、编码唯一性、分类树、删除前关联检查和逻辑删除 | 原测试 mock `PaymentMethodMapper`、`PaymentMethodCategoryMapper`，无法证明分类路径查询、编码唯一性和 `PaymentMethodMapper.xml#countDeleteRelations` 真实 SQL 可用 | 已改为 `PaymentMethodServiceImplIntegrationTest`，使用 H2、MyBatis-Plus、真实 Mapper 和真实 Mapper XML 覆盖分类路径、编码重复拒绝、银行卡快捷分类、分类树、收银台引用拦截和逻辑删除；审计服务保留测试替身作为非数据库边界 | DONE |
| `PaymentMethodRouteServiceImplTest` | MIGRATE | 验证支付方式路由规则创建、签约能力环境解析、候选路由 SQL、删除前订单关联检查和试算过滤 | 原测试 mock `PaymentMethodRouteRuleMapper`、`PaymentMethodRouteRuleItemMapper`、`PaymentApplicationMapper`、`PaymentEnterpriseSubjectMapper`、`PaymentMethodMapper`、`PaymentChannelContractCapabilityMapper`，无法证明规则/明细落库、签约能力 join、候选路由 join 和删除关联 SQL 可用 | 已改为 `PaymentMethodRouteServiceImplIntegrationTest`，使用 H2、MyBatis-Plus、真实 Mapper 和真实 Mapper XML 覆盖规则/明细落库、支付订单关联删除拦截、试算命中、签约能力金额过滤、环境无法解析拒绝；删除只验证 mock 调用的低价值内容，审计服务保留测试替身作为非数据库边界 | DONE |
| `PaymentChannelServiceImplTest` | MIGRATE | 验证支付通道创建、通道能力落库、敏感字段模板校验、能力删除引用保护、通道删除前关联检查和逻辑删除 | 原测试 mock `PaymentChannelMapper`、`PaymentChannelCapabilityMapper`，无法证明通道/能力真实落库、`PaymentChannelMapper.xml#countDeleteRelations` 和 `PaymentChannelCapabilityMapper.xml#countDeleteRelations` 真实 SQL 可用，且包含只验证 mock 调用/字段搬运的低价值用例 | 已改为 `PaymentChannelServiceImplIntegrationTest`，使用 H2、MyBatis-Plus、真实 Mapper 和真实 Mapper XML 覆盖通道和能力落库、敏感字段模板拒绝、能力引用拒绝删除、通道关系拒绝删除、无关联时能力物理删除与通道逻辑删除；清理纯 mock 调用低价值用例；审计服务保留测试替身作为非数据库边界 | DONE |
| `PaymentCashierServiceImplTest` | MIGRATE | 验证收银台详情组合查询、路由匹配、支付下单、通道结果回写、业务单推进和处理中订单复用 | 原测试 mock `PaymentCashierConfigMapper`、`PaymentApplicationMapper`、`PaymentEnterpriseSubjectMapper`、`PaymentMethodMapper`、`PaymentBusinessOrderMapper`、`PaymentOrderMapper`、`PaymentChannelContractMapper`、`PaymentChannelContractCapabilityMapper`，无法证明收银台订单查询、路由 join、订单插入、结果回写和处理中订单查询真实 SQL 可用，且包含重复/实现顺序类低价值用例 | 已改为 `PaymentCashierServiceImplIntegrationTest`，使用 H2、MyBatis-Plus、真实 Mapper 和真实 Mapper XML 覆盖详情页路由 join 与脱敏、支付订单真实插入/回写、业务单状态推进、线下转账签约配置传递、处理中订单复用和不可用方式拒绝；通道适配器、敏感值、状态流水、主动查单和号段服务保留测试替身作为外部边界；删除重复推进和事务顺序类低价值用例 | DONE |
| `PaymentChannelContractServiceImplTest` | MIGRATE | 验证支付通道签约创建、敏感/文件/线下默认配置落库、签约能力同步、删除前关联检查、证书到期查询和证书轮换 | 原测试 mock `PaymentChannelContractMapper`、`PaymentChannelContractCapabilityMapper`、`PaymentChannelContractValueMapper`、`PaymentChannelCertificateRotationRecordMapper`、`PaymentChannelCapabilityMapper` 等，无法证明签约配置/能力/配置值真实落库、`PaymentChannelContractMapper.xml#countDeleteRelations`、`PaymentChannelContractCapabilityMapper.xml#countRouteRelations` 和 `selectExpiringCertificates` 真实 SQL 可用，且包含只验证 mock 调用/实现顺序的低价值用例 | 已改为 `PaymentChannelContractServiceImplIntegrationTest`，使用 H2、MyBatis-Plus、真实 Mapper 和真实 Mapper XML 覆盖敏感字段加密落库、文件 ID 配置落库、线下收款默认账户填充、能力复用与引用保护、关联存在拒绝删除、无关联时子表物理删除与合同逻辑删除、证书到期查询和证书轮换记录落库；审计服务和加密服务保留测试替身作为非数据库边界；清理重复 mock 调用低价值用例 | DONE |
| `PaymentOpenApiServiceTest` | MIGRATE | 验证开放接口签名认证、nonce 防重放、业务单创建幂等、收银台入口、支付单/小票查询、退款申请和退款查询 | 原测试 mock `PaymentApplicationMapper`、`PaymentBusinessOrderMapper`、`PaymentCashierConfigMapper`、`PaymentOpenApiNonceMapper`、`PaymentOrderMapper`、`PaymentRefundOrderMapper`、`PaymentTransactionFlowMapper` 等核心 Mapper，无法证明开放接口认证查询、nonce 唯一约束、业务单落库、订单/退款 join SQL、退款金额占用统计和状态流水真实可用，且包含大量只验证 `never`/调用顺序/字段搬运的低价值用例 | 已改为 `PaymentOpenApiServiceIntegrationTest`，使用 H2、MyBatis-Plus、真实 Mapper 和真实 Mapper XML 覆盖签名认证、nonce 唯一约束、业务订单真实插入、幂等冲突、收银台选择、支付单详情、小票、退款申请落库、金额边界和退款详情 latest flow SQL；收银台服务、通道适配器、编号服务、加密服务保留测试替身作为外部边界；清理 mock-only 低价值用例 | DONE |
| `WorkflowBusinessApplyServiceImplTest` | MIGRATE | 验证我的申请统计按当前申请人和申请状态分组计数 | 原测试 mock `WorkflowBusinessApplyMapper.selectCount` 直接返回数字，无法证明 `applicant_id` 和 `apply_status in (...)` 真实查询条件有效，且 `verify(never)` 只验证实现细节 | 已改为 `WorkflowBusinessApplyServiceImplIntegrationTest`，使用 H2、MyBatis-Plus 和真实 `WorkflowBusinessApplyMapper` 覆盖当前用户申请状态统计；缺少用户上下文时只保留返回 0 的行为验证；`TaskService` 作为未触达的 Flowable 外部边界替身 | DONE |

## 7. 后续文件级清单要求

后续迁移每个测试文件时必须补充文件级记录：

| 测试文件 | 分类 | 测试目标 | mock 替换对象 | 是否替换关键链路 | 目标验证方式 | 数据隔离/清理 | 状态 |
|---|---|---|---|---|---|---|---|
| 记录具体测试文件路径 | KEEP/MIGRATE/REWRITE/DELETE | 写明目标 | 写明替身对象 | 是/否 | 单元/组件/集成/E2E | H2/Testcontainers/rollback/显式清理/无数据库 | OPEN/DONE |
| `mango/mango-platform/mango-authorization/mango-authorization-core/src/test/java/io/mango/authorization/core/service/impl/RoleDataScopeServiceImplTest.java` | MIGRATE | 角色数据范围保存与解析 | `RoleDataScopeMapper`、`RoleMapper`、`MenuMapper`、`RoleMenuMapper`、`SubjectRoleBindingMapper` | 是 | 集成测试：`RoleDataScopeServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-authorization/mango-authorization-core/src/test/java/io/mango/authorization/core/service/impl/ApiResourceServiceImplTest.java` | MIGRATE | API resource 扫描注册、旧路径禁用和访问决策 | spy 被测 service，替换 `list`、`saveBatch`、`updateBatchById` | 是 | 集成测试：`ApiResourceServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-authorization/mango-authorization-core/src/test/java/io/mango/authorization/core/service/impl/SubjectAuthorityServiceImplTest.java` | MIGRATE | 主体角色到菜单权限解析 | `SubjectRoleBindingMapper`、`RoleMenuMapper`、`MenuMapper` | 是 | 集成测试：`SubjectAuthorityServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-authorization/mango-authorization-core/src/test/java/io/mango/authorization/core/service/impl/TenantMenuPackageBindingHandlerTest.java` | MIGRATE | 套餐绑定后租户管理员角色菜单授权重建 | `RoleMapper`、`RoleMenuMapper`、`MenuMapper` | 是 | 集成测试：`TenantMenuPackageBindingHandlerIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper，`IMenuPackageService` 仅作为套餐输入替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-authorization/mango-authorization-core/src/test/java/io/mango/authorization/core/service/impl/MenuServiceImplTest.java` | MIGRATE | 菜单持久化、runtime config、树结构和用户菜单过滤 | `MenuMapper`、`FrontendMenuRuntimeConfigMapper`、`AuthorizationAppModuleMapper` 等 Mapper | 是 | 集成测试：`MenuServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper，`ISubjectAuthorityService` 仅作为授权输入替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-authorization/mango-authorization-core/src/test/java/io/mango/authorization/core/service/impl/RoleServiceImplTest.java` | MIGRATE | 角色 CRUD、主体角色分配、菜单授权和关系清理 | `RoleMapper`、`SubjectRoleBindingMapper`、`RoleMenuMapper`、`MenuMapper` | 是 | 集成测试：`RoleServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper，`IMenuService` 和 `ISubjectAuthorityService` 仅作为外部输入替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-authorization/mango-authorization-core/src/test/java/io/mango/authorization/core/service/impl/AppModuleServiceImplTest.java` | MIGRATE | 应用模块资源清单注册、菜单/按钮/runtime config、套餐/角色绑定和禁用清理 | `AuthorizationAppModuleMapper`、`MenuMapper`、`FrontendMenuRuntimeConfigMapper`、`MenuPackageMapper`、`MenuPackageItemMapper`、`RoleMapper`、`RoleMenuMapper` | 是 | 集成测试：`AppModuleServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper，租户套餐查询/绑定回调仅作为外部边界替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-authorization/mango-authorization-starter/src/test/java/io/mango/authorization/starter/resource/AuthRoleDataScopeResourceHandlerTest.java` | MIGRATE | `AUTH_ROLE_DATA_SCOPE` 资源声明解析、角色/组织引用校验、数据范围落库和禁用 | `RoleMapper`、`RoleDataScopeMapper`、`SysOrgMapper` | 是 | 集成测试：`AuthRoleDataScopeResourceHandlerIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-identity/mango-identity-core/src/test/java/io/mango/identity/core/service/impl/IdentityUserSecurityServiceTest.java` | MIGRATE | 身份用户安全状态：登录失败锁定、登录成功清理、强制改密清理、解锁和强制重置 | `IdentityUserMapper` | 是 | 集成测试：`IdentityUserSecurityServiceIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-identity/mango-identity-core/src/test/java/io/mango/identity/core/service/impl/IdentityUserServiceImplTest.java` | MIGRATE | 身份资料查询、目标用户解析、成员删除关系清理和外部身份绑定修复成员关系 | `IdentityUserMapper`、`TenantMemberMapper`、`TenantMemberOrgMapper`、`ExternalIdentityBindingMapper` | 是 | 集成测试：`IdentityUserServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper，`RoleBindingApi` 仅作为外部授权服务替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-identity/mango-identity-starter/src/test/java/io/mango/identity/starter/resource/IdentityUserResourceHandlerTest.java` | MIGRATE | `IDENTITY_USER` 资源声明创建/更新身份用户、租户成员和禁用状态 | `IdentityUserMapper`、`TenantMemberMapper` | 是 | 集成测试：`IdentityUserResourceHandlerIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-notice/mango-notice-core/src/test/java/io/mango/notice/core/service/impl/NoticeAnnouncementServiceTest.java` | MIGRATE | 公告发布 fan-out、公告可见性、阅读/确认状态和我的公告分页查询 | `NoticeAnnouncementMapper`、`NoticeAnnouncementTargetMapper`、`NoticeAnnouncementRecipientMapper` | 是 | 集成测试：`NoticeAnnouncementServiceIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper，`NoticeRecipientResolver` 和 `INoticeService` 仅作为外部边界替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-notice/mango-notice-core/src/test/java/io/mango/notice/core/service/impl/NoticeServiceTest.java` | MIGRATE | 通知发送入库、任务执行、站内信状态、业务配置发布和渠道配置敏感值保留 | `NoticeTaskMapper`、`NoticeRecipientMapper`、`NoticeSendRecordMapper`、`NoticeSiteMessageMapper`、`NoticeBusinessTypeMapper`、`NoticeBusinessConfigVersionMapper`、`NoticeBusinessChannelTemplateMapper`、`NoticeChannelConfigMapper` 等 Mapper | 是 | 集成测试：`NoticeServiceIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper；外部身份、组织、企业微信和 outbox 存储仅作为外部边界替身；删除原 mock-heavy 低价值用例 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-payment/mango-payment-core/src/test/java/io/mango/payment/core/service/impl/PaymentApplicationServiceImplTest.java` | MIGRATE | 支付应用创建密钥加密落库、删除前关联检查和逻辑删除 | `PaymentApplicationMapper` | 是 | 集成测试：`PaymentApplicationServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper 和 Mapper XML；审计服务、敏感值加密服务仅作为外部边界替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-payment/mango-payment-core/src/test/java/io/mango/payment/core/service/impl/PaymentEnterpriseSubjectServiceImplTest.java` | MIGRATE | 支付企业主体敏感字段加密落库、默认银行账户同步、详情脱敏、删除关联检查和逻辑删除 | `PaymentEnterpriseSubjectMapper`、`PaymentSubjectBankAccountMapper` | 是 | 集成测试：`PaymentEnterpriseSubjectServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper 和 Mapper XML；审计服务、敏感值服务仅作为外部边界替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-payment/mango-payment-core/src/test/java/io/mango/payment/core/service/impl/PaymentCashierConfigServiceImplTest.java` | MIGRATE | 收银台配置创建、关联对象校验、默认收银台唯一性、展示顺序校验、删除关联检查和逻辑删除 | `PaymentCashierConfigMapper`、`PaymentApplicationMapper`、`PaymentEnterpriseSubjectMapper`、`PaymentMethodMapper` | 是 | 集成测试：`PaymentCashierConfigServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper 和 Mapper XML；审计服务仅作为外部边界替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-payment/mango-payment-core/src/test/java/io/mango/payment/core/service/impl/PaymentMethodServiceImplTest.java` | MIGRATE | 支付方式创建、三级分类路径校验、编码唯一性、分类树、删除关联检查和逻辑删除 | `PaymentMethodMapper`、`PaymentMethodCategoryMapper` | 是 | 集成测试：`PaymentMethodServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper 和 Mapper XML；审计服务仅作为外部边界替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-payment/mango-payment-core/src/test/java/io/mango/payment/core/service/impl/PaymentMethodRouteServiceImplTest.java` | MIGRATE | 路由规则创建、签约能力环境解析、候选路由 SQL、删除订单关联检查和试算过滤 | `PaymentMethodRouteRuleMapper`、`PaymentMethodRouteRuleItemMapper`、`PaymentApplicationMapper`、`PaymentEnterpriseSubjectMapper`、`PaymentMethodMapper`、`PaymentChannelContractCapabilityMapper` | 是 | 集成测试：`PaymentMethodRouteServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper 和 Mapper XML；删除只验证 mock 调用的低价值内容；审计服务仅作为外部边界替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-payment/mango-payment-core/src/test/java/io/mango/payment/core/service/impl/PaymentChannelServiceImplTest.java` | MIGRATE | 支付通道创建、通道能力落库、敏感字段模板校验、能力删除引用保护、通道删除关联检查和逻辑删除 | `PaymentChannelMapper`、`PaymentChannelCapabilityMapper` | 是 | 集成测试：`PaymentChannelServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper 和 Mapper XML；删除只验证 mock 调用/字段搬运的低价值内容；审计服务仅作为外部边界替身 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-payment/mango-payment-core/src/test/java/io/mango/payment/core/service/impl/PaymentCashierServiceImplTest.java` | MIGRATE | 收银台详情组合查询、路由匹配、支付下单、通道结果回写、业务单推进和处理中订单复用 | `PaymentCashierConfigMapper`、`PaymentApplicationMapper`、`PaymentEnterpriseSubjectMapper`、`PaymentMethodMapper`、`PaymentBusinessOrderMapper`、`PaymentOrderMapper`、`PaymentChannelContractMapper`、`PaymentChannelContractCapabilityMapper` | 是 | 集成测试：`PaymentCashierServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper 和 Mapper XML；H2 注册 `FIND_IN_SET` alias 兼容生产 MySQL XML；通道适配器、敏感值、流水、主动查单、号段服务仅作为外部边界替身；删除重复/事务顺序类低价值用例 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-payment/mango-payment-core/src/test/java/io/mango/payment/core/service/impl/PaymentChannelContractServiceImplTest.java` | MIGRATE | 支付通道签约创建、敏感/文件/线下默认配置落库、签约能力同步、删除前关联检查、证书到期查询和证书轮换 | `PaymentChannelContractMapper`、`PaymentChannelContractCapabilityMapper`、`PaymentChannelContractValueMapper`、`PaymentChannelCertificateRotationRecordMapper`、`PaymentChannelCapabilityMapper`、`PaymentChannelMapper`、`PaymentEnterpriseSubjectMapper` | 是 | 集成测试：`PaymentChannelContractServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper 和 Mapper XML；审计服务、加密服务仅作为外部边界替身；删除重复 mock 调用/实现顺序低价值内容 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-payment/mango-payment-core/src/test/java/io/mango/payment/core/service/impl/PaymentOpenApiServiceTest.java` | MIGRATE | 开放接口签名认证、nonce 防重放、业务单创建幂等、收银台入口、支付单/小票查询、退款申请和退款查询 | `PaymentApplicationMapper`、`PaymentBusinessOrderMapper`、`PaymentCashierConfigMapper`、`PaymentOpenApiNonceMapper`、`PaymentOrderMapper`、`PaymentRefundOrderMapper`、`PaymentTransactionFlowMapper` | 是 | 集成测试：`PaymentOpenApiServiceIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper 和 Mapper XML；收银台服务、通道适配器、编号服务、加密服务仅作为外部边界替身；删除 `never`/顺序验证/字段搬运类低价值内容 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-workflow/mango-workflow-core/src/test/java/io/mango/workflow/core/service/impl/WorkflowBusinessApplyServiceImplTest.java` | MIGRATE | 我的申请统计按当前申请人和申请状态分组计数 | `WorkflowBusinessApplyMapper` | 是 | 集成测试：`WorkflowBusinessApplyServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper；`TaskService` 仅作为未触达 Flowable 外部边界替身；删除 mock 数字和实现细节 verify | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-workflow/mango-workflow-core/src/test/java/io/mango/workflow/core/service/impl/WorkflowDefinitionServiceImplTest.java` | MIGRATE | 流程定义分页数据权限、详情权限、已发布快照、内部发布和按需创建并发布 | `WorkflowDefinitionMapper`、`WorkflowDefinitionVersionMapper`、`WorkflowCategoryMapper`、`WorkflowNodeDefinitionMapper` | 是 | 集成测试：`WorkflowDefinitionServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper 验证数据权限过滤、publishedOnly 快照查询、内部发布落库和 ensurePublished 创建分类/定义/版本；Flowable `RepositoryService` 与 `WorkflowDesignerBpmnConverter` 仅作为外部边界替身；删除 wrapper 字符串、`never`/调用次数和字段搬运类低价值内容 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
| `mango/mango-platform/mango-workflow/mango-workflow-core/src/test/java/io/mango/workflow/core/service/impl/WorkflowProcessServiceImplTest.java` | MIGRATE | 发起流程时从流程定义表读取表单配置、推导业务申请渲染模式、写入表单快照和启动记录 | `WorkflowDefinitionMapper`、`WorkflowFormInstanceMapper`、`WorkflowTaskRecordMapper` | 是 | 集成测试：`WorkflowProcessServiceImplIntegrationTest` 使用真实 H2/MyBatis-Plus Mapper 验证流程定义读取、CUSTOM 表单推导、form instance 和 task record 落库；Flowable runtime/task/history 与业务申请/任务运行时服务作为外部边界替身；删除只捕获 mock 参数的窄断言 | H2 内存库，`BeforeEach` 重建最小表结构 | DONE |
