# <模块名>

## 1. 能力定位

说明本模块提供的核心能力和主要使用者。

## 2. 适用场景

列出适合使用本模块的业务或平台场景。

## 3. 不适用场景

列出本模块不解决的问题，避免误用。

## 4. 模块边界

说明本模块负责什么，不负责什么，以及依赖的 Mango 能力。

## 5. 接入方式

说明后端依赖、前端包、CLI module、starter、模板或配置入口。

## 6. 配置项

列出关键配置入口和配置来源；默认值以配置类、模板或 PMO 规则源为准。

## 7. 对外接口 / 扩展点

列出公开 API、事件、SPI、注解、组件或扩展点。

## 8. 数据库 / 初始化数据

说明 migration、默认数据、资源清单、菜单、字典或初始化方式。
只写本模块的具体表、数据、资源和入口；长期数据库、事务和初始化规则只链接 `mango-pmo/rules/**`。

## 9. 菜单 / 权限 / 租户

说明菜单入口、权限资源、角色归口、租户边界和数据权限要求。
只写本模块的资源归属、默认授权和租户边界事实；长期权限、租户和数据权限规则只链接 `mango-pmo/rules/**`。

## 10. 验证方式

列出最小验证命令、代表性页面、接口断言或业务链路。
只写本模块可执行的验证命令或验收入口；通用测试和交付质量规则只链接 `mango-pmo/rules/**`。

## 11. 业务接入最小闭环

列出业务开发者接入本模块时的最小依赖、最小配置、最小调用链路、权限/租户/数据归属断言和常见误用防线。
只写本模块事实和接入示例；长期代码、测试、权限、租户和交付规则只链接 `mango-pmo/rules/**`。

## 12. 常见问题

记录常见错误、排查入口和已知限制。

## 13. 关联 PMO 规则

链接相关 `mango-pmo/rules/**` 规则源，不复制规则正文。

## 14. 历史设计 / 交付记录

链接相关设计文档、Sprint 计划、交付记录或验收证据。
