# <模块名> 使用说明

## 1. 概览

说明模块定位、面向的使用者、解决的问题，以及业务开发者应该保存或调用什么。

## 2. 功能清单

用表格列出主要能力、用途和常用入口。入口可以是后端依赖、HTTP API、Java API、前端组件、CLI 命令或管理页面。

## 3. 接入方式

说明开发时依赖、部署时依赖、前端包、CLI module、starter、模板或配置入口。开发依赖和部署依赖必须分清。

## 4. 配置说明

列出关键配置入口和配置来源；默认值以配置类、模板或 PMO 规则源为准。

配置项必须写到字段级：

| 配置入口 | 字段 / Key | 默认值 | 含义 | 影响行为 | 源码入口 |
|----------|------------|--------|------|----------|----------|
| `application.yml` | `mango.<module>.enabled` | `<默认值>` | `<含义>` | `<开启或关闭什么>` | `<配置类>` |
| `<运行时配置 API / 管理页>` | `<字段>` | `<默认值>` | `<含义>` | `<影响链路>` | `<Controller / Service>` |

## 5. API 与扩展

列出对业务开发者有用的公开 API、事件、SPI、注解、组件或扩展点。完整 HTTP 入参和返回字段优先由 OpenAPI 承担，README 只写常用入口和关键字段。

## 6. 数据与初始化

说明 migration、默认数据、资源清单、菜单、字典或初始化方式。
只写本模块的具体表、数据、资源和入口；长期数据库、事务和初始化规则只链接 `mango-pmo/rules/**`。

有持久化或默认数据时，必须登记：

| 类型 | 位置 | 初始化内容 | 幂等键 / 唯一键 | 生效时机 |
|------|------|------------|-----------------|----------|
| Flyway migration | `<db/migration/...>` | `<表 / 默认数据>` | `<唯一键>` | `<应用启动 / 模块启用>` |
| 资源清单 | `<META-INF/mango/resource-manifest.json>` | `<菜单 / 权限>` | `<appCode + moduleCode + menuCode>` | `<资源同步 starter>` |
| 启动初始化器 | `<Runner / Importer>` | `<初始化对象>` | `<幂等条件>` | `<启动阶段>` |

## 7. 管理入口

说明菜单入口、权限资源、角色归口、租户边界和数据权限要求。
只写本模块的资源归属、默认授权和租户边界事实；长期权限、租户和数据权限规则只链接 `mango-pmo/rules/**`。

有后台页面、按钮权限或接口权限时，必须登记：

| 菜单 / 页面 | component key | 权限码 | 入库来源 | 默认套餐 / 角色 | 后端校验入口 |
|-------------|---------------|--------|----------|-----------------|--------------|
| `<菜单名称>` | `<module/page/index>` | `<module:resource:action>` | `<resource-manifest 或 migration>` | `<packageCodes / roleCodes>` | `<Controller / Service>` |

## 8. 快速开始

列出业务开发者接入本模块时的最小依赖、最小配置、最小调用链路和常见误用防线。
只写本模块事实和接入示例；长期代码、测试、权限、租户和交付规则只链接 `mango-pmo/rules/**`。

## 9. 返回字段

有对外返回对象时，列出业务最常用字段和是否建议业务入库。完整字段以 OpenAPI、类型定义或源码为准。

## 10. 问题排查

记录常见错误、排查入口和已知限制。

## 11. 相关文档

链接相关 `mango-pmo/rules/**` 规则源，不复制规则正文。
如有架构设计、历史方案、前端包或场景示例，在这里链接，不在使用文档里展开内部设计。
