# <前端入口名>

本模板用于公共组件、公开 API、页面注册入口、运行时扩展点和管理页面入口。

纯管理页面入口 README 可以只说明具体管理能力、页面 key、依赖接口、菜单和权限关系；不强制展开 props、事件和 slot。

公共组件、公开 API、页面注册入口和运行时扩展点必须写详细用法，包含参数、默认值、事件、示例、后端依赖、权限边界和常见排障入口。

## 1. 概览

说明这是复用组件、页面注册入口、业务接入组件、API 封装还是运行时扩展点。

## 2. 功能清单

列出从 package `src/index.ts`、`src/admin-pages.ts` 或 package exports 暴露的组件、函数、类型和页面 key。

管理页面入口需要列出每个页面 key 对应的管理能力。公共组件需要列出组件、类型、composable、API 封装和样式入口。

## 3. 接入方式

说明 import 路径、注册函数、最小调用示例或页面 component key。

## 4. 参数与事件

列出关键 props、函数参数、返回值、事件、slot 和默认行为来源。

公共组件必须写到字段级：

| 名称 | 类型 | 默认值 | 作用 | 影响行为 |
|------|------|--------|------|----------|
| `<prop / 参数>` | `<类型>` | `<默认值>` | `<含义>` | `<影响>` |

管理页面入口如果没有对外 props 或事件，需要明确说明数据来自哪个 API 封装和页面支持哪些管理操作。

## 5. 后端依赖

列出依赖的后端模块、API 前缀、数据结构和初始化数据入口。

## 6. 权限与数据边界

说明组件或页面依赖的权限码、菜单 component、租户上下文和数据归属校验入口。

后台管理页面必须列出页面 key、菜单 component、按钮权限码和后端校验入口。公共组件必须说明权限由组件参数、后端接口还是宿主页面控制。

## 7. 快速开始

列出最小 import、注册、调用或页面接入步骤。

## 8. 问题排查

记录常见接入错误、排查入口和已知限制。

## 9. 相关文档

链接 package README、后端模块 README、能力地图和相关 PMO 规则源。
