server:
  port: ${MANGO_FILE_APP_PORT:18617}

spring:
  application:
    name: mango-file-capability-app

mango:
  module:
    module-service:
      modules:
        mango-resource:
          service-name: mango-resource-capability-app
