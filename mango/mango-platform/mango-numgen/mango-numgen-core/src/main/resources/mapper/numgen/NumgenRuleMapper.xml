<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.mango.numgen.core.mapper.NumgenRuleMapper">

    <select id="selectActiveByGenKey" resultType="io.mango.numgen.core.entity.NumgenRuleEntity">
        select *
        from numgen_rule
        where gen_key = #{genKey}
          and tenant_id = #{tenantId}
          and status = 1
          and version_state = 'ACTIVE'
          and del_flag = 0
        order by version desc
        limit 1
    </select>

    <select id="selectLatestDraftByGenKey" resultType="io.mango.numgen.core.entity.NumgenRuleEntity">
        select *
        from numgen_rule
        where gen_key = #{genKey}
          and tenant_id = #{tenantId}
          and status = 1
          and version_state = 'DRAFT'
          and del_flag = 0
        order by version desc
        limit 1
    </select>

    <select id="selectVersionsByGenKey" resultType="io.mango.numgen.core.entity.NumgenRuleEntity">
        select *
        from numgen_rule
        where gen_key = #{genKey}
          and tenant_id = #{tenantId}
          and del_flag = 0
        order by version desc
    </select>

    <select id="selectVersionIncludingDeleted" resultType="io.mango.numgen.core.entity.NumgenRuleEntity">
        select *
        from numgen_rule
        where tenant_id = #{tenantId}
          and gen_key = #{genKey}
          and version = #{version}
        order by del_flag asc, id asc
        limit 1
    </select>

    <delete id="physicalDeleteById">
        delete from numgen_rule
        where id = #{id}
    </delete>
</mapper>
