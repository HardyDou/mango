<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.mango</groupId>
        <artifactId>mango-org</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>mango-org-core</artifactId>
    <packaging>jar</packaging>

    <name>Mango Org Core</name>
    <description>Mango Org Module - Business implementation</description>

    <dependencies>
        <!-- Mango Org API -->
        <dependency>
            <groupId>io.mango</groupId>
            <artifactId>mango-org-api</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- 关系型持久化能力 -->
        <dependency>
            <groupId>io.mango</groupId>
            <artifactId>mango-infra-persistence-starter</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- API 访问权限声明注解 -->
        <dependency>
            <groupId>io.mango</groupId>
            <artifactId>mango-authorization-api</artifactId>
        </dependency>

        <dependency>
            <groupId>io.mango</groupId>
            <artifactId>mango-system-api</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- OpenAPI 注解仅用于 controller 文档展示 -->
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-api</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
