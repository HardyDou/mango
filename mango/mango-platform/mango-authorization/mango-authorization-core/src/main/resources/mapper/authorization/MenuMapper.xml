<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.mango.authorization.core.mapper.MenuMapper">

    <select id="selectDiagnosticMenus"
            resultType="io.mango.authorization.core.entity.MenuEntity"
            timeout="2">
        select app_code, module_code, menu_code, component, api_codes, status, del_flag
        from authorization_menu
        where tenant_id = #{tenantId}
          and app_code = #{appCode}
          and del_flag = 0
          and menu_code in
        <foreach collection="menuCodes" item="menuCode" open="(" separator="," close=")">
            #{menuCode}
        </foreach>
    </select>
</mapper>
