package com.mango.${module}.controller;

import com.mango.common.R;
import com.mango.${module}.service.${entityName}Service;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;

/**
 * ${entityName} Controller
 * Generated by Mango Scaffold
 */
@RestController
@RequestMapping("/${resourcePath}")
@RequiredArgsConstructor
public class ${entityName}Controller {

    private final ${entityName}Service ${entityNameCamel}Service;

    @GetMapping
    public R list() {
        return R.ok(${entityNameCamel}Service.list());
    }

    @GetMapping("/{id}")
    public R get(@PathVariable Long id) {
        return R.ok(${entityNameCamel}Service.findById(id));
    }

    @PostMapping
    public R save(@RequestBody ${entityName}DTO dto) {
        ${entityNameCamel}Service.save(dto);
        return R.ok();
    }

    @PutMapping("/{id}")
    public R update(@PathVariable Long id, @RequestBody ${entityName}DTO dto) {
        dto.setId(id);
        ${entityNameCamel}Service.update(dto);
        return R.ok();
    }

    @DeleteMapping("/{id}")
    public R delete(@PathVariable Long id) {
        ${entityNameCamel}Service.delete(id);
        return R.ok();
    }
}
