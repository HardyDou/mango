package com.mango.${module}.mapper;

import com.mango.${module}.dto.${entityName}DTO;
import org.apache.ibatis.annotations.*;

/**
 * ${entityName} Mapper
 * Generated by Mango Scaffold
 */
@Mapper
public interface ${entityName}Mapper {

    @Select("SELECT * FROM ${tableName}")
    List<${entityName}DTO> selectList();

    @Select("SELECT * FROM ${tableName} WHERE id = #{id}")
    ${entityName}DTO selectById(@Param("id") Long id);

    @Insert("INSERT INTO ${tableName} (name) VALUES (#{name})")
    void insert(${entityName}DTO dto);

    @Update("UPDATE ${tableName} SET name = #{name} WHERE id = #{id}")
    void update(${entityName}DTO dto);

    @Delete("DELETE FROM ${tableName} WHERE id = #{id}")
    void delete(@Param("id") Long id);
}
