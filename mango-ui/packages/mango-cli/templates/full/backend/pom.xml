<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>{{groupId}}</groupId>
    <artifactId>{{projectKebab}}-backend</artifactId>
    <version>{{projectVersion}}</version>
    <packaging>pom</packaging>

    <name>{{projectPascal}} Backend</name>
    <description>{{presetLabel}} Mango Admin backend consumer</description>

    <modules>
        <module>app</module>
        <!-- mango-cli:business-modules:start -->
{{backendBusinessModules}}
        <!-- mango-cli:business-modules:end -->
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <spring-boot.version>{{springBootVersion}}</spring-boot.version>
        <spring-cloud.version>{{springCloudVersion}}</spring-cloud.version>
        <mango.version>{{mangoBackendVersion}}</mango.version>
        <springdoc-openapi.version>{{springdocOpenapiVersion}}</springdoc-openapi.version>
        <swagger-annotations.version>{{swaggerAnnotationsVersion}}</swagger-annotations.version>
    </properties>

    <repositories>
        <repository>
            <id>mango-public</id>
            <url>{{mavenRepository}}</url>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- mango-cli:managed-dependencies:start -->
{{backendManagedDependencies}}
            <!-- mango-cli:managed-dependencies:end -->
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>{{mavenCompilerPluginVersion}}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
