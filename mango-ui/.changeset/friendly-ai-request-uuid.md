---
'@mango/ai': patch
---

Use the Mango UUID compatibility API for AI service chat request correlation so the workbench remains compatible with browsers that do not expose native `crypto.randomUUID`.
