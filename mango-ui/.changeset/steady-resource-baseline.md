---
'@mango/workflow': patch
'@mango/cli': patch
---

Publish the Resource cold-baseline and incremental-release support together with the pending Workflow designer and assignment API changes. The CLI patch carries the Maven platform release and generated consumer compatibility tuple.
