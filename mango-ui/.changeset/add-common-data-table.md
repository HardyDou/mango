---
'@mango/common': minor
'@mango/admin': patch
---

新增 `MangoDataTable`、`MangoTableCell` 和 `MangoStatusText` 公共组件，提供主题化状态文本、可切换卡片表格、可配置单元格与稳定分页事件；`Pagination` 新增对齐能力并合并同轮页码和页大小更新。Admin 同步消费包含表头语义变量的 Common 主题版本。
