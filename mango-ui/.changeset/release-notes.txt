## Pull Requests

- [PR #818](https://github.com/HardyDou/mango/pull/818) Fixed business-consumer README audit root and scope resolution. Packages: `@mango/pmo@1.3.16`, `@mango/cli@1.0.109`. Business Adaptation: upgrade the exact PMO/CLI tuple, rerun the locked PMO upgrade/check and the two M08 README audits; repair only genuine business-owned README findings.

## Fixed

- Resolve business README audit roots from the actual project root and `mango.config.json.paths`, audit only capability-map-owned business documents, and fail closed on invalid roots, empty scope, path escape or symlink escape.
- Preserve the fixed Mango source-repository README/source-facts gates and stop treating `mango.config.json.paths` as a runtime configuration fact.

## Versions

- `@mango/pmo`: `1.3.15` to `1.3.16`.
- `@mango/cli`: `1.0.108` to `1.0.109`, with an exact dependency on PMO `1.3.16`.
- Mango Maven remains `1.0.37`; every other npm coordinate remains unchanged.

## Published Packages

1. `@mango/pmo@1.3.16`.
2. `@mango/cli@1.0.109`.
3. No Maven or other npm package is published in this batch.

## Business Impact

- Business repositories using packaged PMO checks no longer receive false failures for Mango source-only README paths; checks apply to their configured backend, frontend and business documentation roots.
- No API, database, menu, permission, tenant, message, file, frontend runtime or application configuration contract changes.

## Upgrade Estimate

- Audience: business repositories using Mango PMO README/source-facts audits.
- Engineering Effort: 15 to 30 minutes for a clean repository; up to 1 hour when genuine business README links need repair.
- Execution Window: 20 to 60 minutes for dry-run, upgrade, locked check and M08 audits.
- Service Downtime: none; this is development-governance tooling only.
- Rollback Effort: 10 to 20 minutes using the PMO backup and the prior CLI/PMO tuple.
- Assumptions: company npm-group access, a clean business worktree and valid `mango.config.json.paths`.

## Upgrade Notes

1. Install `@mango/cli@1.0.109` from the company consume registry; it locks `@mango/pmo@1.3.16`.
2. Run `mango pmo upgrade --project-dir . --to 1.3.16 --dry-run`, review the managed projection, apply the upgrade, then run `mango pmo check --project-dir . --locked`.
3. Run `node business-pmo/mango-baseline/tools/audit-module-readmes.mjs --root .` and `node business-pmo/mango-baseline/tools/audit-readme-source-facts.mjs --root .`; repair genuine business-owned README findings without adding Mango source directories.

## Verification

- Reproduce the machine plan `@mango/pmo@1.3.16 -> @mango/cli@1.0.109` with no Maven or unrelated npm targets.
- Build and seal both tarballs once, then pass the mixed candidate/consume-registry PMO upgrade and README audit consumer.
- After publication, verify hosted/group integrity for both exact coordinates and repeat the upgrade/audits using only the consume registry.

## Rollback

- Revert the business PMO upgrade commit, run the validated PMO rollback to `1.3.15`, restore CLI `1.0.108`, and rerun the locked check.
- Do not delete, overwrite or republish either immutable coordinate; any release defect receives a new patch version.

## Audit History

- PR #815 and tag `v2026.08.16-issue-805-release-consumer-fix` are the successful release baseline, not new capability in this batch.
- PR #816 only closed the prior batch's audit record and is not a release-bearing change in this batch.
