## Pull Requests

- [PR #839](https://github.com/HardyDou/mango/pull/839) Changed the PMO frontend-page baseline opt-out contract. Packages: `@mango/pmo@1.4.0`, `@mango/cli@1.1.0`. Business Adaptation: upgrade the PMO/CLI tuple and use only the explicit boolean opt-out.
- [PR #840](https://github.com/HardyDou/mango/pull/840) Fixed Admin Shell confirmation cancellation and close handling. Packages: `@mango/admin-shell@1.0.63`, `@mango/admin@1.0.69`, `@mango/cli@1.1.0`. Business Adaptation: verify cancel/close behavior in shell-created applications.
- [PR #841](https://github.com/HardyDou/mango/pull/841) Changed the frontend-quality workflow pause contract. Packages: release tooling source only. Business Adaptation: none; maintainers follow the stable required-check policy.
- [PR #843](https://github.com/HardyDou/mango/pull/843) Added OFD conversion and document signing. Packages: Maven non-app reactor and `io.mango:mango-docs-bundle:1.0.39`, `@mango/template@1.0.37`. Business Adaptation: configure and test signing material in a non-production environment.
- [PR #844](https://github.com/HardyDou/mango/pull/844) Changed Catalog-bound release planning and sealed publication recovery. Packages: `@mango/pmo@1.4.0`, `@mango/admin@1.0.69`, `@mango/template@1.0.37`, `@mango/cli@1.1.0`. Business Adaptation: none for runtime consumers; release operators use the latest state machine.

## Added

- Add OFD conversion and document-signing APIs, providers, starter wiring and capability documentation.

## Fixed

- Treat confirmation cancellation and dialog close as expected Admin Shell interactions.

## Changed

- Make the PMO frontend-page baseline opt-out explicit, boolean-only and fail-closed.
- Make the release Catalog the source for publishable content and bind plan, source tree, sealed artifacts, registry checks and recovery journal to one candidate.

## Versions

- Maven non-app reactor and `io.mango:mango-docs-bundle`: `1.0.38` to `1.0.39`.
- `@mango/admin-shell`: `1.0.62` to `1.0.63`; `@mango/pmo`: `1.3.16` to `1.4.0`; `@mango/template`: `1.0.36` to `1.0.37`; `@mango/admin`: `1.0.68` to `1.0.69`; `@mango/cli`: `1.0.111` to `1.1.0`.

## Published Packages

1. Maven `--all-non-app` reactor and `io.mango:mango-docs-bundle:1.0.39`.
2. `@mango/admin-shell@1.0.63`.
3. `@mango/pmo@1.4.0`.
4. `@mango/template@1.0.37`.
5. `@mango/admin@1.0.69`.
6. `@mango/cli@1.1.0`.

## Business Impact

- Consumers using document conversion or signing gain OFD output and signing/verification APIs; they must supply authorized certificate/trust-store material and keep keys out of frontend/runtime configuration.
- Admin Shell consumers receive corrected cancellation semantics with no API or database migration.
- Business repositories upgrading PMO/CLI receive an explicit frontend baseline opt-out; baseline checks remain enabled unless the project sets boolean `false`.

## Upgrade Estimate

- Audience: Mango consumers of Admin Shell, PMO/CLI governance, template/fileproc conversion or document signing.
- Engineering Effort: 30 to 90 minutes for generated consumers; 2 to 4 hours for signing integrations.
- Execution Window: 1 to 3 hours for dependency update, generated-project checks, focused conversion/signing tests and registry consumer verification.
- Service Downtime: none required by the framework artifacts.
- Rollback Effort: 15 to 60 minutes to restore the prior Maven/npm tuple and application build.
- Assumptions: publish/consume registry access, clean locks, compatible JDK/PDF/OFD libraries and non-production signing material.

## Upgrade Notes

1. Upgrade backend dependencies together to Maven `1.0.39` and install `@mango/cli@1.1.0` or the exact generated npm matrix.
2. Keep the PMO baseline enabled unless an explicit boolean `false` is reviewed and intended.
3. Validate OFD conversion, signing, verification and Admin Shell cancel/close behavior before business deployment.

## Verification

- `mango release plan` generated the Catalog-bound plan from the source snapshot and target Maven `1.0.39`/CLI `1.1.0`; prepare must seal the exact source and artifacts and pass mixed consumer verification.
- Publication must read back every listed Maven/npm coordinate from publish and consume registries, then pass a pure consume-registry consumer before Tag or GitHub Release creation.

## Rollback

- Restore Maven `1.0.38`, `@mango/cli@1.0.111`, `@mango/pmo@1.3.16`, `@mango/admin-shell@1.0.62`, `@mango/template@1.0.36` and `@mango/admin@1.0.68`; never overwrite immutable coordinates.

## Audit History

- PRs #836 and #837 are the completed `1.0.38` closeout and are outside this candidate.
