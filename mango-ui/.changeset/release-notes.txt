## Pull Requests

- [PR #932](https://github.com/HardyDou/mango/pull/932) Fixed legacy runtime-prefix compatibility and bounded lifecycle/log reads in Mango CLI development commands. Packages: `@mango/cli@1.2.9`. Business Adaptation: local Mango workspaces may retain one legacy leading `runtime` token; existing commands and generated project contracts remain unchanged.
- [PR #935](https://github.com/HardyDou/mango/pull/935) Fixed API Resource version propagation from access annotations through MVC discovery and Resource Declaration conversion. Packages: Mango Maven `1.0.49`, `io.mango:mango-docs-bundle:1.0.49`, and generated consumer lock `@mango/cli@1.2.9`. Business Adaptation: upgrade the Maven/CLI tuple and allow normal Resource reconciliation; do not downgrade or hand-edit persisted resource versions.

## Fixed

- Preserve explicit API Resource declaration versions so the system public branding resource remains at version 2 during runtime reconciliation.
- Accept the governed legacy CLI runtime prefix and bound development log reads to the final 1 MiB.

## Changed

- Advance generated consumers to Mango Maven `1.0.49` through `@mango/cli@1.2.9`; PMO and all other npm package versions remain unchanged.

## Versions

- Mango Maven non-application reactor and `io.mango:mango-docs-bundle`: `1.0.48` to `1.0.49` (machine-generated full coordinate closure).
- `@mango/cli`: `1.2.8` to `1.2.9`; `@mango/pmo` remains `1.4.4`; all other npm package versions remain unchanged.

## Published Packages

1. Publish the machine-generated non-application Maven/docs batch at `1.0.49`.
2. Publish only `@mango/cli@1.2.9` from npm after Maven/docs verification.
3. Create the immutable Tag and GitHub Release after dual-registry and pure-consumer verification.

## Business Impact

- Applications using runtime API Resource scanning no longer repeatedly submit version 1 for the version 2 public branding resource, so eventual reconciliation can converge without weakening downgrade protection.
- Mango source workspaces avoid loading multi-gigabyte historical logs during startup diagnostics.
- No database migration, API path, access mode, permission code, PMO version, business deployment, or production data mutation is included.

## Upgrade Estimate

- Audience: generated Mango applications, direct Authorization/System consumers, and Mango source contributors using development commands.
- Engineering Effort: 15 to 30 minutes for generated consumers; up to 60 minutes for customized direct integrations.
- Execution Window: 30 to 90 minutes including dependency upgrade, clean build, startup, and Resource reconciliation verification.
- Service Downtime: no framework-mandated downtime; consumers use their normal application restart window.
- Rollback Effort: 15 to 30 minutes to restore Maven `1.0.48` and CLI `1.2.8`.
- Assumptions: Java 21, Node `22.23.1`, configured registries, and a database containing or accepting the public branding API Resource at version 2.

## Upgrade Notes

1. Upgrade Mango Maven to `1.0.49` and `@mango/cli` to `1.2.9`; retain PMO `1.4.4` and the existing frontend package tuple.
2. Build and restart normally, then verify Resource synchronization accepts `GET /system/admin-branding/public` at version 2 and reaches a stable eventual cycle.
3. Do not overwrite Maven `1.0.48`, lower the persisted resource version, or manually bypass Resource downgrade protection.

## Verification

- Run the Issue #934 28-test suite, changed-module Java architecture/PMD/Checkstyle/SpotBugs gates, CLI regression tests, release plan/notes checks, registry doctor, sealed candidate consumers, and dual-registry back-checks.

## Rollback

- Restore consumers to Mango Maven `1.0.48` and `@mango/cli@1.2.8`; never overwrite immutable coordinates or delete Resource Registry data.
