## Pull Requests

- [PR #942](https://github.com/HardyDou/mango/pull/942) Changed the RBAC tenant-root organization label to show organization type and tenant ID. Packages: `@mango/rbac@1.0.31` and its generated npm dependents through `@mango/cli@1.2.11`. Business Adaptation: upgrade the generated frontend tuple and verify tenant-root labels in the organization view.
- [PR #945](https://github.com/HardyDou/mango/pull/945) Fixed MESSAGE_TEMPLATE Resource Registry finalize for sparse runtime lifecycle declarations, with transactional cleanup and tenant-safe target resolution. Packages: Mango Maven/docs `1.0.51` and generated npm dependents through `@mango/cli@1.2.11`. Business Adaptation: upgrade Mango Maven and CLI together, then verify sparse disable/delete against runtime-modified and already-removed templates.

## Fixed

- Resolve MESSAGE_TEMPLATE disable/delete by the persisted target ID and real tenant instead of requiring deleted initialization fields.
- Preserve runtime-owned template fields, make missing targets idempotent, enforce target-table and tenant boundaries, and roll back associated cleanup atomically.

## Changed

- Display the root organization type and tenant ID in the RBAC organization tree.
- Advance the complete generated npm closure to the versions in the machine plan while keeping `@mango/pmo@1.4.4` unchanged.

## Versions

- Mango Maven non-application reactor and `io.mango:mango-docs-bundle`: `1.0.50` to `1.0.51` (192 machine-generated coordinates).
- Generated npm closure: `@mango/rbac@1.0.31`, `@mango/system@1.0.39`, `@mango/admin-pages@1.0.40`, `@mango/calendar@1.0.41`, `@mango/cms@1.0.30`, `@mango/job@1.0.34`, `@mango/link@1.0.27`, `@mango/notice@1.0.46`, `@mango/numgen@1.0.42`, `@mango/payment@1.0.33`, `@mango/template@1.0.42`, `@mango/workflow@1.0.50`, `@mango/admin-shell@1.0.71`, `@mango/workflow-business-example@1.0.49`, `@mango/admin@1.1.7`, and `@mango/cli@1.2.11`; `@mango/pmo@1.4.4` remains unchanged.

## Published Packages

1. Publish the machine-generated 192-coordinate non-application Maven/docs batch at `1.0.51`.
2. Publish the machine-generated 16-package npm topology from `@mango/rbac@1.0.31` through `@mango/cli@1.2.11` after Maven/docs verification.
3. Create the immutable Tag and GitHub Release only after dual-registry and pure-consumer verification.

## Business Impact

- Resource Registry finalize can safely disable or delete templates after users modify or remove the original business rows; it does not recreate rows or overwrite runtime changes.
- RBAC organization users can distinguish a tenant root from its tenant ID without changing permissions, tenant isolation, or API contracts.
- No production database, business deployment, schema migration, or historical Resource Registry payload rewrite is included.

## Upgrade Estimate

- Audience: generated Mango applications and direct Resource Registry, Notice, RBAC, and organization consumers.
- Engineering Effort: 45 to 90 minutes for generated consumers; 2 to 4 hours for customized Resource declarations or tenant/Notice regression suites.
- Execution Window: 60 to 150 minutes including dependency upgrade, clean build, Resource finalize verification, and organization UI smoke checks.
- Service Downtime: no framework-mandated downtime; consumers use their normal deployment and restart window.
- Rollback Effort: 30 to 60 minutes to restore Maven `1.0.50`, CLI `1.2.10`, and the previous npm tuple.
- Assumptions: Java 21, Node `22.23.1`, configured registries, representative tenant data, and existing Notice template references.

## Upgrade Notes

1. Upgrade the complete generated tuple to the versions above and use Mango Maven `1.0.51` with CLI `1.2.11`; retain PMO `1.4.4`.
2. Run Resource Registry plan, rolling apply, verify, and finalize against a copy of representative data; verify runtime-modified fields remain unchanged and missing targets are skipped without recreation.
3. Verify tenant-root organization labels and normal tenant-scoped organization access after the frontend upgrade.

## Verification

- Run the 18-case H2/Spring/MyBatis MESSAGE_TEMPLATE integration suite, affected Maven module verify and static checks, test-quality and mock audits, and protected PR checks.
- Run release plan and release-notes checks, registry doctor, sealed mixed consumers, dual-registry back-checks, and a pure consume-registry consumer for the exact Maven/npm tuple.

## Rollback

- Restore consumers to Maven `1.0.50`, CLI `1.2.10`, and the prior npm tuple, then redeploy through the normal immutable application release process.
- Never overwrite `1.0.51` or published npm coordinates, recreate deleted templates, or restore obsolete Resource Registry payloads as part of rollback.
