## Pull Requests

- [PR #847](https://github.com/HardyDou/mango/pull/847) Changed Mango release verification to use bounded parallel Maven publication, POM-first remote checks and one aggregate clean Maven consumer. Packages: `@mango/pmo@1.4.1`, `@mango/cli@1.1.1`. Business Adaptation: release operators use the faster default `basic` Maven verification; set `MANGO_RELEASE_MAVEN_VERIFY_MODE=full` only for an explicitly requested per-JAR audit.

## Fixed

- Keep Maven release evidence complete while removing redundant per-JAR remote downloads from the default verification path.

## Added

## Changed

- Bound Maven publication and consume-registry visibility concurrency at 16 by default, while preserving per-coordinate journal and recovery evidence.
- Run one aggregate clean Maven consumer for the sealed batch instead of starting one clean consumer per JAR.

## Versions

- `@mango/pmo`: `1.4.0` to `1.4.1`.
- `@mango/cli`: `1.1.0` to `1.1.1` because CLI consumes the exact PMO version.
- Mango Maven and `io.mango:mango-docs-bundle` remain at `1.0.39`; no Maven coordinate is republished in this npm-only batch.

## Published Packages

1. `@mango/pmo@1.4.1`.
2. `@mango/cli@1.1.1`.
3. No Maven, application fat JAR or unchanged npm coordinate is published.

## Business Impact

- Release operators get a shorter default Maven verification path with the same immutable publication journal, bounded concurrency and fail-closed recovery behavior.
- Runtime consumers have no API, database, permission, tenant or service configuration migration. Existing consumers continue using Maven `1.0.39`.

## Upgrade Estimate

- Audience: Mango maintainers and teams operating the platform release workflow; runtime consumers do not need an upgrade for this tooling-only change.
- Engineering Effort: 10 to 20 minutes to upgrade PMO/CLI in a release workspace; no business code changes.
- Execution Window: 15 to 30 minutes including clean CLI/PMO consumer verification and release registry back-checks.
- Service Downtime: none.
- Rollback Effort: 5 to 15 minutes to restore `@mango/pmo@1.4.0` and `@mango/cli@1.1.0`; immutable coordinates remain untouched.
- Assumptions: configured npm publish/consume registries, clean workspace and access to the existing Maven `1.0.39` consume prerequisite.

## Upgrade Notes

1. Upgrade the release operator tuple to `@mango/pmo@1.4.1` and `@mango/cli@1.1.1` together.
2. Keep Maven at `1.0.39` for this npm-only batch.
3. Use the default Maven `basic` verification for ordinary releases; set `MANGO_RELEASE_MAVEN_VERIFY_MODE=full` only when per-JAR remote audit evidence is required.

## Verification

- `pnpm -C mango-ui release:test` passed all 80 release tests, including bounded Maven concurrency, basic/full verification modes and aggregate consumer generation.
- Prepare must pack the exact PMO/CLI tarballs, validate the release notes and run the mixed consumer against the configured npm consume registry and the unchanged Maven `1.0.39` prerequisite.
- Publish must back-check both npm registry roles and run a pure consume-registry consumer before Tag or GitHub Release creation.

## Rollback

- Restore `@mango/pmo@1.4.0` and `@mango/cli@1.1.0` in the operator environment; do not delete, overwrite or republish immutable npm or Maven coordinates.
- If a publication request becomes ambiguous, use `mango release status` and `mango release repair` with the same candidate instead of starting a new publication.

## Audit History

- PRs #843 and #844 belong to the completed Maven `1.0.39` release and are outside this candidate.
