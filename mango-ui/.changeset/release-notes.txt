## Pull Requests

- [PR #952](https://github.com/HardyDou/mango/pull/952) Fixed notice business configuration pagination. Packages: `@mango/notice@1.0.48` and generated npm dependents through `@mango/cli@1.2.13`. Business Adaptation: upgrade the complete tuple and verify paginated notice records.
- [PR #954](https://github.com/HardyDou/mango/pull/954) Fixed administrator root-organization initialization and organization reference provisioning. Packages: Mango Maven/docs `1.0.53`, `@mango/rbac@1.0.33`, and generated npm dependents through `@mango/cli@1.2.13`. Business Adaptation: verify tenant-scoped root organization access after upgrade.

## Fixed

- Restore complete pagination controls and query parameters for notice business configuration records.
- Initialize and provision a usable administrator root organization for new tenants while preserving tenant isolation.

## Changed

- Advance the machine-generated Maven/docs platform batch from `1.0.52` to `1.0.53`.
- Advance the generated npm closure from `@mango/rbac@1.0.32` through `@mango/cli@1.2.13`; `@mango/pmo@1.4.4` remains unchanged.

## Versions

- Mango Maven non-application reactor and `io.mango:mango-docs-bundle`: `1.0.52` to `1.0.53` (192 machine-generated coordinates).
- Generated npm closure: `@mango/rbac@1.0.33`, `@mango/system@1.0.41`, `@mango/admin-pages@1.0.42`, `@mango/calendar@1.0.43`, `@mango/cms@1.0.32`, `@mango/job@1.0.36`, `@mango/link@1.0.29`, `@mango/notice@1.0.48`, `@mango/numgen@1.0.44`, `@mango/payment@1.0.35`, `@mango/template@1.0.44`, `@mango/workflow@1.0.52`, `@mango/admin-shell@1.0.73`, `@mango/workflow-business-example@1.0.51`, `@mango/admin@1.1.9`, and `@mango/cli@1.2.13`; `@mango/pmo@1.4.4` remains unchanged.

## Published Packages

1. Publish the machine-generated 192-coordinate non-application Maven/docs batch at `1.0.53`.
2. Publish the machine-generated 16-package npm topology ending at `@mango/cli@1.2.13` after Maven/docs verification.
3. Create the immutable tag and GitHub Release only after dual-registry and pure-consumer verification.

## Business Impact

- Notice administrators can browse all business configuration records without truncation.
- New tenants receive a valid administrator root organization and organization references remain tenant-scoped; existing tenant data is not rewritten.
- No production database operation, application deployment, schema migration, or historical data rewrite is included.

## Upgrade Estimate

- Audience: generated Mango applications and direct Notice, RBAC, Identity, Organization, and CLI consumers.
- Engineering Effort: 30 to 60 minutes for generated consumers; 1 to 3 hours for customized organization provisioning or notice pages.
- Execution Window: 45 to 120 minutes including dependency upgrade, clean build, tenant bootstrap verification, and paginated notice checks.
- Service Downtime: no framework-mandated downtime; consumers use their normal deployment and restart window.
- Rollback Effort: 20 to 45 minutes to restore Maven `1.0.52`, CLI `1.2.12`, and the previous npm tuple.
- Assumptions: Java 21, Node `22.23.1`, configured registries, a clean tenant bootstrap database, and representative notice records.

## Upgrade Notes

1. Upgrade the complete tuple to Maven `1.0.53`, CLI `1.2.13`, and the npm versions above; retain PMO `1.4.4`.
2. For new tenants, verify administrator root organization creation and tenant-scoped organization reads.
3. Open Notice business configuration and verify page navigation, total count, and records beyond the first page.

## Verification

- Run catalog/style/workspace checks, release-plan and release-notes checks, affected Notice/RBAC/Identity/Org tests, Maven verification, registry doctor, sealed mixed consumers, dual-registry back-checks, and a pure consume-registry consumer for the exact tuple.

## Rollback

- Restore consumers to Maven `1.0.52`, CLI `1.2.12`, and the previous npm tuple through the normal immutable application release process; never overwrite immutable coordinates or bypass tenant-root initialization checks.
