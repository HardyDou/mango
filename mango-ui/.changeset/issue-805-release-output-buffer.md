---
'@mango/cli': patch
---

Allow Mango release commands to retain high-volume Maven and npm build output without terminating the release candidate preparation when Node's default child-process buffer is exceeded.
