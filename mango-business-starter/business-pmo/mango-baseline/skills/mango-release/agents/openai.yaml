interface:
  display_name: "Mango Release"
  short_description: "Publish and verify a governed Mango release batch"
  default_prompt: "Use $mango-release to plan, publish, and verify this Mango release batch."
policy:
  allow_implicit_invocation: true
