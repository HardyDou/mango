interface:
  display_name: "Mango Existing Issue Handler"
  short_description: "Reproduce and route an existing Mango GitHub Issue"
  default_prompt: "Use $mango-issue-handle to inspect and handle this existing Mango Issue from current evidence."
policy:
  allow_implicit_invocation: true
