# Issue #918 修复审批记录

- 审批人：HardyDou（当前仓库所有者与会话用户）
- 审批日期：2026-09-01
- 原始任务：`处理issue：918`
- 事实确认：最新 `main@870f1bbdb` 仍存在角色菜单级联回显失真，以及企微同步把部门 ID 写入 `INTERNAL_ORG.partyId` 的问题。
- 方案确认：用户在收到完整修复方案后回复“好的开始处理吧。”，批准进入实现。
- 前端方案：角色菜单树按已授权叶子恢复级联显示，保存时提交全选节点与半选祖先节点并集，保持必要父目录且不扩大到兄弟节点。
- 主体契约：`INTERNAL / INTERNAL_USER / INTERNAL_ORG` 的 `partyId` 统一为当前数值租户 ID；部门归属只保存在成员主组织和成员组织关系中。
- 同步方案：企微创建、更新、重复同步和 unchanged 路径都校验并修复租户主体，不因资料跳过或 `updateMatchedUsers=false` 保留错误主体。
- 数据修复：Identity 与 Authorization 在各自表所有权内新增 Flyway migration；Authorization 先删除与正确租户主体重复的错误绑定，再归一化剩余绑定。
- 验证授权：允许在本任务 worktree、隔离数据库和本地服务上执行静态、单元、集成、真实 MySQL、API 与浏览器验证；禁止调用真实企业微信和修改生产数据。
- 未授权事项：Commit、Push、创建或合并 PR、发布、Tag、GitHub Release、业务部署、生产数据修改及关闭 Issue。

## 2026-09-01 默认单租户产品态增补审批

- 用户决定：明确要求“改成单租户模式”。
- 最小方案确认：内部保留 `tenant_id`、租户拦截器与租户 API；默认产品只初始化并展示“芒果集团”，唯一租户自动选择，租户管理菜单隐藏。
- 实施授权：用户在收到最小改动清单后回复“可以开始处理吧。”，批准修改代码、Resource、测试和当前 Issue 文档。
- 数据边界：只允许重建任务专属本地演示数据库；不得自动删除正式环境或其它工作区已有租户。
