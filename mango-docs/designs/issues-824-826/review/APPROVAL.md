# Issue 824 / 826 联合修复审批记录

- 审批人：HardyDou（当前仓库所有者与会话用户）
- 审批日期：2026-08-18
- 用户原始指令：将 Issue 824 与 Issue 826 合并处理，在一个 worktree 同步解决。
- 方案确认：用户在收到联合行为方案后回复“继续”，批准按统一第三方身份来源方案实施。
- 方案 A 最终确认：用户明确回复“好的按照方案A进行吧”，批准以企业微信完整昵称为主要身份信息，有头像时展示头像，无头像时不占位。
- 身份展示：第三方显示名不得回退 Mango 昵称；`****4826` 仅转换为“企业微信账号尾号 4826”作为辅助信息，不得作为账号名称；昵称缺失时显示“企业微信资料未同步”，提示点击右侧同步按钮；头像与解绑弹窗使用 32px 尺寸；解绑前展示同一昵称、头像与尾号信息。
- 头像处理：通讯录同步将企业微信头像导入 Mango 文件中心，Identity 只保存头像文件 ID，不持久化外部头像 URL；头像导入失败不阻断昵称同步，头像被企业微信移除时可清理旧引用。
- 企业微信发送：面向 Mango userId 的 WECOM 发送按当前租户、渠道 CorpID、WECOM Provider 和有效状态读取 Identity 外部身份，不读取或回退 Notice WECOM 接收账户。
- 企业微信同步：保留组织、成员和映射同步，只维护 Identity WECOM 外部身份；资料未变化时仍修复身份绑定。
- 个人资料获取方案：用户明确要求不启用整套通讯录同步，因为 Mango 与企业微信的组织架构可能不同；个人资料刷新只读取当前成员昵称头像，不同步部门、组织、岗位、角色，也不调用 `NoticeWecomSyncService`。
- 自助绑定：绑定成功后自动查询当前成员昵称头像；成功才保存并展示，查询、头像导入或快照更新失败均不影响绑定成功，不伪造资料。
- 手动单账号同步：用户确认在绑定账号列表的企业微信资料后显示同步图标按钮；点击仅同步当前登录人的当前绑定，成功覆盖旧昵称头像，失败提示无权限、成员不可见、可信 IP 或企业微信异常并结束，不清空已有快照。
- 数据处理：允许新增只清理由 Mango 昵称历史回填造成的 WECOM SELF 显示名迁移，不清理真实第三方显示名。
- 执行授权：允许在联合任务 worktree 内创建 FULL 文档、修改代码和测试资产、执行非破坏性静态验证。
- 原验证限制：方案批准时启用的 simple 技能要求跳过所有测试；当时仅允许编译测试资产。
- 后续验证授权：用户随后明确要求“启动服务测试”，因此允许在专用 worktree、隔离数据库和本地服务上执行非破坏性自动化、API 与浏览器验证；仍禁止真实企业微信调用和消息发送。
- 后续提交授权：用户在完成验收后明确要求“提交代码、PR、合并 PR”，授权当前联合任务分支执行 commit、push、创建 PR，并在仓库门禁满足后合并；该授权不包含自行审批。
- 仍未授权事项：制品发布、Tag、GitHub Release、业务部署、生产配置修改、真实企业微信调用和破坏性数据库操作；Issue 状态仅允许由 PR 中的 `Closes #824`、`Closes #826` 在合并时自动更新，浏览器验收必须拦截资料刷新接口。
