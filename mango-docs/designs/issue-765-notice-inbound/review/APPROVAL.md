# Issue 765 消息接收能力审批记录

- 审批人：HardyDou（当前仓库所有者与会话用户）
- 审批日期：2026-08-13
- 审批结论：批准 Issue 765 按 FULL 模式形成需求、设计和实施计划，并按已确认方向继续开发，不逐阶段重复询问。
- 用户原始授权：“登记issue 然后开始处理”“第一版本支持 邮箱接收、企业微信接收，消息一定可以广播给需要的其他服务”“邮箱两种方式都支持下；定时拉取、接口推送”“配置什么用什么”“留下 spi扩展方式；现以阿里云、腾讯云的为主”“你自己明确就行了”“附件发送到file服务”。
- 业务范围：邮箱 IMAP/POP3 按账号配置轮询、邮箱入站 Webhook SPI、企业微信 GET 验证与 POST 收件、Inbox 幂等与可靠广播、附件保存到 mango-file，以及管理人员使用的组织级接收消息列表和详情。
- 管理端边界：用户明确要求“给管理人员用的，不是个人”；接收消息页面按租户和 `notice:inbound:view` 权限隔离，不进入个人消息中心。
- 能力边界：阿里云、腾讯云只在官方提供真实入站收件协议时启用适配器；发送投递、退信、打开、点击等回执不作为收件消息。
- 安全边界：邮箱和企业微信凭据只从安全配置注入，不写入仓库、业务表、日志或事件；公网回调必须验签、限流并建立租户/账号上下文。
- 测试边界：126 邮箱凭据及服务器配置已由用户提供并仅通过隐藏终端输入；SMTP/POP3 真实收件通过，IMAP 新邮件同步受第三方窗口限制，不能宣称完整 IMAP 自发自收通过。
- 执行授权：允许在 Issue 765 任务 worktree 内创建生命周期资产、修改代码和测试并执行非破坏性验证；不包含 commit、push、PR、发布、生产配置变更或破坏性数据库操作。
