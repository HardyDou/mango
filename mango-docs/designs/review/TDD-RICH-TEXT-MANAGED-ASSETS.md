# TDD-RICH-TEXT-MANAGED-ASSETS 审批记录

- 审批人：Mango Tech Lead（当前会话用户）
- 审批日期：2026-07-22
- 审批结论：批准技术设计进入实施计划阶段。
- 审批依据：用户在当前 Codex 会话明确回复“确认”，确认其以 Mango Tech Lead 或授权责任人身份批准 `TDD-RICH-TEXT-MANAGED-ASSETS`，并允许推进到 `APPROVED/NEXT` 后生成 L3/FULL 实施计划。
- 审批范围：富文本托管图片 token 双态、剪贴板图片上传、服务端受控远程图片导入、附件工具栏扩展、现有文件存储链路复用、SSRF 防护、兼容策略及 TC-001～TC-007。
- 数据边界：不得持久化 Base64、Blob URL、预览 URL、下载 URL或第三方图片 URL；不得新增数据库表、字段、migration 或关联表。
- 安全限制：远程导入必须逐跳执行 URI、DNS 和地址分类校验，禁止凭据转发，并落实协议、端口、超时、重定向、字节数、MIME 与魔数限制；实际连接目标无法与已校验解析结果保持一致时不得启用远程导入。
- 生命周期适用性：本任务由用户直接确认能力边界并显式提供技术设计，未启用独立 BRD/SRS；FULL 生命周期截至 TDD 阶段只要求 `tdd`，本记录同时作为缩减前置阶段的外部适用性证据。
- 后续门禁：实施计划必须继承本 TDD 的完整 SHA-256 与 L3 风险，计划经用户确认前不得编码。
- 范围澄清：用户于 2026-07-22 明确指出“这个不是审批组件，我们要做的是富文本的基础能力”。据此删除审批组件和 workflow 业务接入假设，附件工具栏范围固定为公共 Editor 的 `toolbar-actions` slot，并由 MangoEditor 基础能力示例复用现有 `MUpload` 验证；不修改审批、workflow 或其它业务数据契约。
