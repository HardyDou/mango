# Workflow 历史债务治理方案批准记录

- 日期：2026-07-14
- 审批人：HardyDou（当前仓库所有者与会话用户）
- 结论：批准后续模块沿用 Payment/CMS 推荐方案，一次性治理到最终态并由 Agent 持续推进。
- 用户依据：用户明确要求“其它模块并未依赖支付模块”“一步到位，最佳方案”“用推荐方案开始处理”“全部按照 payment 政策”，并在 CMS 合并后要求继续既定目标。
- 数据政策：只支持新数据库；Flyway 仅 DDL；正式必需数据与 Demo 分模块、分目录登记；运行态数据不初始化。
- 兼容边界：保持 Workflow Java/HTTP/Feign 接口、任务逻辑、权限、租户、状态、领域事件、设计器和前端特性不变。
- 验证边界：先建立有价值的改前基线，改后运行同一测试入口；只执行 Workflow 定向验证，不重复全仓检查。
