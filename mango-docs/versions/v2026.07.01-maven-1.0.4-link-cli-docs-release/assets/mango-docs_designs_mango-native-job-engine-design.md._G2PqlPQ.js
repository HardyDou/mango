import{_ as i,o,c as l,a2 as a}from"./chunks/framework.QoSTM79V.js";const k=JSON.parse('{"title":"Mango 原生 Job Engine 最终特性目标与技术设计","description":"","frontmatter":{},"headers":[],"relativePath":"mango-docs/designs/mango-native-job-engine-design.md","filePath":"mango-docs/designs/mango-native-job-engine-design.md"}'),n={name:"mango-docs/designs/mango-native-job-engine-design.md"};function t(d,e,s,r,c,p){return o(),l("div",null,[...e[0]||(e[0]=[a(`<h1 id="mango-原生-job-engine-最终特性目标与技术设计" tabindex="-1">Mango 原生 Job Engine 最终特性目标与技术设计 <a class="header-anchor" href="#mango-原生-job-engine-最终特性目标与技术设计" aria-label="Permalink to &quot;Mango 原生 Job Engine 最终特性目标与技术设计&quot;">​</a></h1><h2 id="_1-目标" tabindex="-1">1. 目标 <a class="header-anchor" href="#_1-目标" aria-label="Permalink to &quot;1. 目标&quot;">​</a></h2><p>Mango Job 的最终目标是提供 Mango 原生任务调度能力，而不是把第三方调度平台包装成 Mango 页面。</p><p>最终决策：</p><ul><li>Mango 自己拥有任务定义、调度游标、执行实例、执行尝试、Worker、日志、事件、告警和审计事实。</li><li>PowerJob 只作为能力参考和迁移参考，不作为默认运行时、不作为事实源、不作为第一实现依赖。</li><li>后台统一入口为 <code>平台能力 -&gt; 任务管理</code>。</li><li>数据库使用独立 <code>mango_job</code>，不和 Mango 主库做跨库外键或跨库事务。</li><li>支持灵活部署：单体内嵌 JobCenter + Worker、单体多实例、独立 JobCenter + 远程 Worker。</li><li>业务代码只知道 Mango Job 契约，不感知 Worker 在本进程、同集群其它进程或远程微服务。</li></ul><p>本设计替代 <code>mango-docs/designs/mango-job-design.md</code> 中“底层优先集成 PowerJob 作为调度执行引擎”的路线。旧文档保留为历史设计，不再作为 Job 后续实现依据。</p><h2 id="_2-设计输入" tabindex="-1">2. 设计输入 <a class="header-anchor" href="#_2-设计输入" aria-label="Permalink to &quot;2. 设计输入&quot;">​</a></h2><ul><li>用户确认：按 Mango 原生 Job Engine 方向改造，PowerJob 只作为能力参考。</li><li>用户要求：不要为了验收写临时代码、虚假代码；日志、实例、Worker、频次执行必须真实可见。</li><li>用户要求：单体部署必须支持，单体多实例也必须安全；微服务部署时调度器能找到执行器。</li><li>用户要求：部署模式切换对业务任务定义无感知。</li><li>用户要求：结构化参数表单，不要求业务用户直接编辑 JSON。</li><li>用户要求：执行实例后面直接看日志，不保留独立执行日志菜单。</li><li>用户要求：告警直接接入 <code>mango-notice</code>。</li><li>用户要求：Mango 数据库按 <code>mango_{moduleName}</code> 统一前缀，Job 模块使用 <code>mango_job</code>。</li><li>PowerJob 资料复核日期：2026-06-06。</li></ul><p>PowerJob 官方资料复核：</p><ul><li>PowerJob README 定位为分布式调度与计算框架，提供 Web UI、任务状态监控和在线日志。</li><li>PowerJob 支持 CRON、固定频率、固定延迟、OpenAPI 四类定时策略。</li><li>PowerJob 支持单机、广播、Map、MapReduce 四类执行模式。</li><li>PowerJob 支持 DAG 工作流、Spring Bean/Java/Shell/Python 处理器、失败重试、高可用和横向扩展。</li><li>PowerJob Worker 与 Server 通信不是 UDP；其远程通信层主要支持 <code>AKKA</code> 和 <code>HTTP</code> 协议，Worker 侧通过 <code>protocol</code> 配置选择。老版本 Worker 默认偏 <code>AKKA</code>，4.3.0 以后 Server 侧内部通信转向 HTTP，HTTP 是新通信框架方向。</li><li>GitHub Release 页面可见 <code>PowerJob-V5.1.2</code>。</li><li>Maven Central 查询 <code>tech.powerjob:powerjob-worker</code> 当前返回最新版本 <code>5.1.1</code>。两者存在发布介质差异，说明 Mango 原生引擎设计不能绑定 PowerJob 运行版本。</li></ul><p>参考资料：</p><ul><li><code>https://github.com/PowerJob/PowerJob</code></li><li><code>https://github.com/PowerJob/PowerJob/releases</code></li><li><code>https://search.maven.org/search?q=tech.powerjob</code></li><li><code>https://www.yuque.com/powerjob/guidence/intro</code></li></ul><h2 id="_3-范围" tabindex="-1">3. 范围 <a class="header-anchor" href="#_3-范围" aria-label="Permalink to &quot;3. 范围&quot;">​</a></h2><p>本设计覆盖：</p><ul><li>Mango Job 最终特性目标。</li><li>PowerJob 能力参考矩阵。</li><li>原生 Job Engine 架构。</li><li>后端模块边界。</li><li>前端菜单和页面模型。</li><li>数据模型。</li><li>状态机。</li><li>调度、分发、租约、重试、超时、取消和补偿。</li><li>Worker 注册、心跳、能力上报和手动上下线。</li><li>日志采集、查询、归档和敏感数据处理。</li><li>多租户、权限、审计、告警和可观测性。</li><li>部署模式。</li><li>从当前 PowerJob Adapter 方案迁移到原生引擎的策略。</li><li>验收和测试计划。</li></ul><h2 id="_4-不做什么" tabindex="-1">4. 不做什么 <a class="header-anchor" href="#_4-不做什么" aria-label="Permalink to &quot;4. 不做什么&quot;">​</a></h2><ul><li>不把 PowerJob Server、PowerJob Worker 或 PowerJob Console 作为 Mango Job 默认运行时。</li><li>不让 Mango UI 直接读取 PowerJob 内部表作为事实源。</li><li>不在 API、VO、Command、前端类型和业务处理器契约中暴露 PowerJob 概念。</li><li>不承诺严格 exactly-once。Mango Job 提供至少一次执行，业务处理器必须按幂等键处理重复执行。</li><li>不做跨 <code>mango</code> 主库和 <code>mango_job</code> 的外键、跨库 join 或跨库强事务。</li><li>首轮不开放脚本任务、任意 HTTP 任务、DAG 工作流和 MapReduce。</li><li>首轮不做 XXL-JOB、Quartz、PowerJob 适配器。</li><li>不保留独立“执行日志”运营菜单。日志作为执行实例详情的一部分。</li></ul><h2 id="_5-powerjob-能力参考矩阵" tabindex="-1">5. PowerJob 能力参考矩阵 <a class="header-anchor" href="#_5-powerjob-能力参考矩阵" aria-label="Permalink to &quot;5. PowerJob 能力参考矩阵&quot;">​</a></h2><table tabindex="0"><thead><tr><th>PowerJob 能力</th><th style="text-align:right;">Mango 是否吸收</th><th>Mango 设计结论</th></tr></thead><tbody><tr><td>Web UI 管理任务、实例、日志</td><td style="text-align:right;">是</td><td>Mango 自研统一 UI，接入租户、权限、菜单和审计。</td></tr><tr><td>CRON、固定频率、固定延迟、OpenAPI</td><td style="text-align:right;">是</td><td>Mango 首轮支持 CRON、固定频率、一次性、手动/API 触发；固定延迟按执行完成后再触发进入后续。</td></tr><tr><td>单机执行</td><td style="text-align:right;">是</td><td>作为 <code>STANDALONE</code> 执行模式。</td></tr><tr><td>广播执行</td><td style="text-align:right;">后续</td><td>需要 task fan-out 和多 Worker 结果聚合，首轮不做。</td></tr><tr><td>Map/MapReduce</td><td style="text-align:right;">后续</td><td>需要子任务拆分、聚合状态和大任务治理，首轮不做。</td></tr><tr><td>DAG 工作流</td><td style="text-align:right;">后续</td><td>由 Mango 工作流或后续 Job Workflow 统一设计，首轮不做。</td></tr><tr><td>Worker 注册和心跳</td><td style="text-align:right;">是</td><td>Mango 原生 Worker 注册表、心跳、能力、容量和上下线。</td></tr><tr><td>失败重试</td><td style="text-align:right;">是</td><td>原生 attempt 重试，明确最大次数、退避、失败终态。</td></tr><tr><td>超时和故障转移</td><td style="text-align:right;">是</td><td>通过 task lease、fencing token、Worker 过期回收实现。</td></tr><tr><td>在线日志</td><td style="text-align:right;">是</td><td>原生日志 chunk + index，不读取第三方内部日志表。</td></tr><tr><td>高可用调度中心</td><td style="text-align:right;">是</td><td>多 JobCenter 节点通过数据库行锁/CAS 抢占调度游标和任务租约。</td></tr><tr><td>脚本处理器</td><td style="text-align:right;">暂不吸收</td><td>安全风险高，首轮禁止。</td></tr><tr><td>第三方告警通道</td><td style="text-align:right;">部分吸收</td><td>Job 对失败实例提交 <code>mango-notice</code> 通知请求，通知模板、接收人规则和第三方通道统一由 <code>mango-notice</code> 维护。</td></tr></tbody></table><p>结论：PowerJob 的能力证明了 Mango Job 应具备哪些企业级调度能力，但当前问题恰恰来自“外部引擎是事实源，Mango 再同步一份”。Mango 后续不再沿用该模型。</p><h2 id="_6-mango-job-最终特性目标" tabindex="-1">6. Mango Job 最终特性目标 <a class="header-anchor" href="#_6-mango-job-最终特性目标" aria-label="Permalink to &quot;6. Mango Job 最终特性目标&quot;">​</a></h2><h3 id="_6-1-任务定义" tabindex="-1">6.1 任务定义 <a class="header-anchor" href="#_6-1-任务定义" aria-label="Permalink to &quot;6.1 任务定义&quot;">​</a></h3><ul><li>支持任务编码、任务名称、所属应用、所属模块、所属租户、处理器、参数 schema、默认参数、频次、并发策略、超时策略、重试策略和告警策略。</li><li>支持 <code>CRON</code>、<code>FIXED_RATE</code>、<code>ONE_TIME</code>、<code>MANUAL</code>。</li><li>支持启用、暂停、禁用、删除。</li><li>结构化参数表单由 <code>param_schema</code> 驱动，保存仍使用 JSON 对象，不要求业务用户直接编辑 JSON 字符串。</li><li>任务定义不绑定部署模式，不绑定 Worker 地址，不绑定第三方引擎 ID。</li></ul><h3 id="_6-2-调度与执行" tabindex="-1">6.2 调度与执行 <a class="header-anchor" href="#_6-2-调度与执行" aria-label="Permalink to &quot;6.2 调度与执行&quot;">​</a></h3><ul><li>JobCenter 负责扫描调度游标、创建实例、创建执行尝试、分发任务、回收租约、处理超时、执行重试、取消和补偿。</li><li>Worker 负责注册、心跳、拉取任务、执行业务处理器、上报进度、上报日志、上报结果。</li><li>执行语义为至少一次执行，平台提供 <code>idempotency_key</code>，业务处理器必须基于该键保证业务幂等。</li><li>多 JobCenter 节点同时部署时，必须通过行锁或 CAS 保证同一调度窗口只创建一个实例。</li><li>Worker 写终态必须携带最新 <code>fencing_token</code>，旧 Worker、超时 Worker、失联后恢复的 Worker 不能覆盖新状态。</li></ul><h3 id="_6-3-worker-治理" tabindex="-1">6.3 Worker 治理 <a class="header-anchor" href="#_6-3-worker-治理" aria-label="Permalink to &quot;6.3 Worker 治理&quot;">​</a></h3><ul><li>Worker 可自动注册，也可在后台手动登记、停用、下线、排空。</li><li>Worker 上报 <code>app_code</code>、环境、实例 ID、地址、协议、标签、版本、容量、当前负载、支持的 handler。</li><li>Worker 状态包括在线、排空中、离线、过期、禁用。</li><li>单体内嵌 Worker 和远程 Worker 使用同一 Worker 模型；差异只在 transport。</li></ul><h3 id="_6-4-日志与实例" tabindex="-1">6.4 日志与实例 <a class="header-anchor" href="#_6-4-日志与实例" aria-label="Permalink to &quot;6.4 日志与实例&quot;">​</a></h3><ul><li>执行实例是一次业务运行。</li><li>执行尝试是一次实际派发到 Worker 的执行动作。</li><li>一次实例可以有多次尝试，因此实例与日志是 1:N。</li><li>日志展示放在执行实例详情和行内日志按钮中，不单独作为运营列表菜单。</li><li>日志必须包含 <code>System.out</code>、<code>System.err</code>、业务 logger、平台事件和处理器返回结果。</li><li>日志支持按实例、attempt、时间、级别、关键字分页查询。</li><li>日志存储支持 chunk、索引、归档、截断和敏感字段脱敏。</li></ul><h3 id="_6-5-ui-与权限" tabindex="-1">6.5 UI 与权限 <a class="header-anchor" href="#_6-5-ui-与权限" aria-label="Permalink to &quot;6.5 UI 与权限&quot;">​</a></h3><p>菜单结构：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>平台能力</span></span>
<span class="line"><span>└── 任务管理</span></span>
<span class="line"><span>    ├── 任务定义</span></span>
<span class="line"><span>    ├── 执行实例</span></span>
<span class="line"><span>    ├── Worker 节点</span></span>
<span class="line"><span>    ├── 运行状态</span></span>
<span class="line"><span>    └── 告警规则</span></span></code></pre></div><p>页面要求：</p><ul><li>任务定义：搜索区使用紧凑表单；任务名称下拉；频次用结构化控件；参数用 schema 表单；提供启用、暂停、触发、编辑、删除。</li><li>执行实例：列表优先展示任务名称、任务编码、状态、触发来源、计划触发时间、开始/结束时间、耗时、Worker、重试次数；行内提供日志、重试、取消。</li><li>Worker 节点：展示真实 Worker，不能显示 <code>N/A</code> 节点；支持手动添加、禁用、排空、下线；显示应用、地址、版本、容量、负载、最近心跳、正在执行数、失败数、支持 handler。</li><li>运行状态：展示 JobCenter 节点、调度器开关、当前节点角色、数据库 lease、扫描延迟、实例积压、异常游标、失败率和超时数。</li><li>告警规则：支持任务级规则和应用级默认规则；只配置通知场景、模板编码、启用策略、收件规则、单用户和多用户结构化字段；具体通道由 <code>mango-notice</code> 负责。</li></ul><p>权限要求：</p><ul><li>页面、按钮和数据查询接入 Mango 权限。</li><li>查询按租户隔离。</li><li>管理 Worker、取消实例、重试实例、修改告警属于高风险操作，需要独立权限码。</li></ul><h2 id="_7-总体架构" tabindex="-1">7. 总体架构 <a class="header-anchor" href="#_7-总体架构" aria-label="Permalink to &quot;7. 总体架构&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>业务模块</span></span>
<span class="line"><span>  └─ MangoJobHandler / MangoJobApi</span></span>
<span class="line"><span></span></span>
<span class="line"><span>mango-job-api</span></span>
<span class="line"><span>  ├─ Command / Query / VO / enums</span></span>
<span class="line"><span>  ├─ MangoJobHandler</span></span>
<span class="line"><span>  └─ MangoJobContext / MangoJobHandleResult</span></span>
<span class="line"><span></span></span>
<span class="line"><span>mango-job-core</span></span>
<span class="line"><span>  ├─ Definition Service</span></span>
<span class="line"><span>  ├─ Schedule Cursor Service</span></span>
<span class="line"><span>  ├─ JobCenter Service</span></span>
<span class="line"><span>  ├─ Dispatch Service</span></span>
<span class="line"><span>  ├─ Worker Registry Service</span></span>
<span class="line"><span>  ├─ Attempt State Machine</span></span>
<span class="line"><span>  ├─ Log Service</span></span>
<span class="line"><span>  ├─ Alarm Event Service</span></span>
<span class="line"><span>  └─ Operation Audit Service</span></span>
<span class="line"><span></span></span>
<span class="line"><span>mango-job-starter</span></span>
<span class="line"><span>  ├─ JobCenter auto configuration</span></span>
<span class="line"><span>  ├─ Embedded Worker auto configuration</span></span>
<span class="line"><span>  ├─ Transport auto configuration</span></span>
<span class="line"><span>  ├─ Flyway migration</span></span>
<span class="line"><span>  └─ Menu / permission seed</span></span>
<span class="line"><span></span></span>
<span class="line"><span>mango-job-worker-starter</span></span>
<span class="line"><span>  ├─ Worker register / heartbeat</span></span>
<span class="line"><span>  ├─ Task pull / result report</span></span>
<span class="line"><span>  ├─ Console/log bridge</span></span>
<span class="line"><span>  └─ Handler registry</span></span>
<span class="line"><span></span></span>
<span class="line"><span>mango-ui/packages/job</span></span>
<span class="line"><span>  ├─ Task Definition</span></span>
<span class="line"><span>  ├─ Instance Detail + Log</span></span>
<span class="line"><span>  ├─ Worker Nodes</span></span>
<span class="line"><span>  ├─ JobCenter Nodes</span></span>
<span class="line"><span>  └─ Alarm Rules</span></span></code></pre></div><p>依赖方向：</p><ul><li>业务模块只依赖 <code>mango-job-api</code> 或 <code>mango-job-worker-starter</code>。</li><li><code>mango-job-api</code> 不依赖 core、starter、数据库、Controller 和 UI。</li><li><code>mango-job-core</code> 持有领域服务和 MyBatis-Plus Mapper。</li><li><code>mango-job-starter</code> 负责自动配置，不承载领域逻辑。</li><li>前端页面只访问 Mango Job API，不访问 Worker 私有地址，不访问第三方引擎。</li></ul><h2 id="_8-核心模型" tabindex="-1">8. 核心模型 <a class="header-anchor" href="#_8-核心模型" aria-label="Permalink to &quot;8. 核心模型&quot;">​</a></h2><h3 id="_8-1-mango-job-definition" tabindex="-1">8.1 <code>mango_job_definition</code> <a class="header-anchor" href="#_8-1-mango-job-definition" aria-label="Permalink to &quot;8.1 \`mango_job_definition\`&quot;">​</a></h3><p>任务定义。</p><p>关键字段：</p><ul><li><code>id</code></li><li><code>tenant_id</code></li><li><code>app_code</code></li><li><code>owner_service</code></li><li><code>worker_group</code></li><li><code>module_code</code></li><li><code>job_code</code></li><li><code>job_name</code></li><li><code>handler_name</code></li><li><code>handler_version</code></li><li><code>schedule_type</code></li><li><code>schedule_expression</code></li><li><code>timezone</code></li><li><code>param_schema</code></li><li><code>param_value</code></li><li><code>concurrency_policy</code></li><li><code>misfire_policy</code></li><li><code>timeout_seconds</code></li><li><code>retry_policy</code></li><li><code>max_retry_count</code></li><li><code>status</code></li><li><code>version</code></li><li><code>created_by</code></li><li><code>created_at</code></li><li><code>updated_by</code></li><li><code>updated_at</code></li><li><code>deleted</code></li></ul><p>约束：</p><ul><li><code>uk_job_definition_code</code>: <code>tenant_id, app_code, job_code, deleted</code>。</li><li><code>owner_service</code> 默认等于 <code>app_code</code>，表达任务所属服务。</li><li><code>worker_group</code> 默认等于 <code>owner_service</code>，表达同一服务下的执行池隔离。</li><li>任务启用和调度时必须存在可用 handler capability。</li></ul><h3 id="_8-2-mango-job-schedule-cursor" tabindex="-1">8.2 <code>mango_job_schedule_cursor</code> <a class="header-anchor" href="#_8-2-mango-job-schedule-cursor" aria-label="Permalink to &quot;8.2 \`mango_job_schedule_cursor\`&quot;">​</a></h3><p>调度游标。</p><p>关键字段：</p><ul><li><code>job_id</code></li><li><code>tenant_id</code></li><li><code>schedule_version</code></li><li><code>last_fire_time</code></li><li><code>next_fire_time</code></li><li><code>misfire_policy</code></li><li><code>lock_owner</code></li><li><code>lock_until</code></li><li><code>last_scan_at</code></li></ul><p>约束：</p><ul><li><code>uk_schedule_cursor_job</code>: <code>job_id</code>。</li><li>JobCenter 抢占游标必须使用行级锁或 CAS 更新 <code>lock_owner/lock_until/schedule_version</code>。</li></ul><h3 id="_8-3-mango-job-instance" tabindex="-1">8.3 <code>mango_job_instance</code> <a class="header-anchor" href="#_8-3-mango-job-instance" aria-label="Permalink to &quot;8.3 \`mango_job_instance\`&quot;">​</a></h3><p>执行实例，表示一次业务运行。</p><p>关键字段：</p><ul><li><code>id</code></li><li><code>tenant_id</code></li><li><code>job_id</code></li><li><code>job_code</code></li><li><code>job_name_snapshot</code></li><li><code>trigger_type</code></li><li><code>trigger_user_id</code></li><li><code>trigger_batch_no</code></li><li><code>idempotency_key</code></li><li><code>scheduled_fire_time</code></li><li><code>actual_fire_time</code></li><li><code>start_time</code></li><li><code>end_time</code></li><li><code>status</code></li><li><code>attempt_count</code></li><li><code>next_retry_time</code></li><li><code>retry_reason</code></li><li><code>duration_millis</code></li><li><code>trace_id</code></li><li><code>error_summary</code></li><li><code>result_summary</code></li></ul><p>约束：</p><ul><li><code>uk_instance_idempotency</code>: <code>tenant_id, job_id, idempotency_key</code>。</li><li>定时触发的 <code>idempotency_key</code> 按 <code>job_id + schedule_version + scheduled_fire_time</code> 生成。</li><li>手动/API 触发必须由请求生成批次号，支持重复提交防抖。</li></ul><h3 id="_8-4-mango-job-attempt" tabindex="-1">8.4 <code>mango_job_attempt</code> <a class="header-anchor" href="#_8-4-mango-job-attempt" aria-label="Permalink to &quot;8.4 \`mango_job_attempt\`&quot;">​</a></h3><p>执行尝试，表示一次派发给 Worker 的实际执行。</p><p>关键字段：</p><ul><li><code>id</code></li><li><code>tenant_id</code></li><li><code>instance_id</code></li><li><code>job_id</code></li><li><code>attempt_no</code></li><li><code>worker_id</code></li><li><code>worker_address_snapshot</code></li><li><code>status</code></li><li><code>lease_owner</code></li><li><code>lease_until</code></li><li><code>fencing_token</code></li><li><code>dispatch_time</code></li><li><code>start_time</code></li><li><code>last_heartbeat_at</code></li><li><code>end_time</code></li><li><code>exit_code</code></li><li><code>error_summary</code></li><li><code>result_payload</code></li></ul><p>约束：</p><ul><li><code>uk_attempt_no</code>: <code>instance_id, attempt_no</code>。</li><li>Worker 上报结果必须匹配 <code>attempt_id + fencing_token</code>。</li><li>Attempt 租约过期后，JobCenter 可标记 <code>LOST</code> 并按策略创建新 attempt。</li></ul><h3 id="_8-5-mango-job-worker-snapshot" tabindex="-1">8.5 <code>mango_job_worker_snapshot</code> <a class="header-anchor" href="#_8-5-mango-job-worker-snapshot" aria-label="Permalink to &quot;8.5 \`mango_job_worker_snapshot\`&quot;">​</a></h3><p>Worker 快照表。</p><p>关键字段：</p><ul><li><code>id</code></li><li><code>tenant_id</code></li><li><code>app_code</code></li><li><code>service_code</code></li><li><code>worker_group</code></li><li><code>worker_address</code></li><li><code>runtime_address</code></li><li><code>transport_type</code></li><li><code>register_source</code></li><li><code>instance_id</code></li><li><code>engine_type</code></li><li><code>engine_worker_id</code></li><li><code>status</code></li><li><code>last_heartbeat_at</code></li></ul><p>约束：</p><ul><li>Worker 快照唯一键为 <code>tenant_id + service_code + worker_group + engine_type + worker_address</code>。</li><li>内嵌 Worker 地址由当前进程生成，当前实现格式为 <code>in-memory://{host}/embedded-{pid}@{host}</code>。</li><li>远程 Worker 地址使用 <code>http(s)://...</code>。</li><li><code>transport_type</code> 是调度选择 transport 的事实字段，地址前缀只作为历史兼容回退。</li><li>Worker 不能只靠配置推断在线，必须有真实心跳。</li></ul><h3 id="_8-6-mango-job-worker-capability" tabindex="-1">8.6 <code>mango_job_worker_capability</code> <a class="header-anchor" href="#_8-6-mango-job-worker-capability" aria-label="Permalink to &quot;8.6 \`mango_job_worker_capability\`&quot;">​</a></h3><p>Worker 能力表。</p><p>关键字段：</p><ul><li><code>worker_id</code></li><li><code>service_code</code></li><li><code>worker_group</code></li><li><code>app_code</code></li><li><code>job_code</code></li><li><code>handler_name</code></li><li><code>handler_version</code></li><li><code>param_schema_hash</code></li><li><code>enabled</code></li></ul><p>用途：</p><ul><li>JobCenter 分发任务前按 <code>tenantId + ownerService + workerGroup + appCode + handlerName + jobCode</code> 选择 Worker。</li><li>UI 展示“哪些 Worker 可以执行这个任务”。</li><li><code>job_code</code> 存储为空串表示该 handler 不限制具体任务编码；指定值时只允许执行对应 <code>jobCode</code>。</li></ul><p>约束：</p><ul><li>Worker 快照唯一键为 <code>tenant_id + service_code + worker_group + engine_type + worker_address</code>。</li><li>Worker 能力唯一键为 <code>worker_id + service_code + worker_group + app_code + handler_name + job_code</code>。</li><li>同一个远程地址可以按不同 <code>service_code + worker_group</code> 注册为不同 Worker。</li></ul><h3 id="_8-7-mango-job-log-index-和-mango-job-log-chunk" tabindex="-1">8.7 <code>mango_job_log_index</code> 和 <code>mango_job_log_chunk</code> <a class="header-anchor" href="#_8-7-mango-job-log-index-和-mango-job-log-chunk" aria-label="Permalink to &quot;8.7 \`mango_job_log_index\` 和 \`mango_job_log_chunk\`&quot;">​</a></h3><p>日志索引和日志分片。</p><p><code>mango_job_log_index</code>：</p><ul><li><code>id</code></li><li><code>tenant_id</code></li><li><code>instance_id</code></li><li><code>attempt_id</code></li><li><code>job_id</code></li><li><code>status</code></li><li><code>first_log_time</code></li><li><code>last_log_time</code></li><li><code>line_count</code></li><li><code>size_bytes</code></li><li><code>truncated</code></li><li><code>archive_file_id</code></li></ul><p><code>mango_job_log_chunk</code>：</p><ul><li><code>id</code></li><li><code>tenant_id</code></li><li><code>instance_id</code></li><li><code>attempt_id</code></li><li><code>sequence_no</code></li><li><code>log_time</code></li><li><code>level</code></li><li><code>logger_name</code></li><li><code>thread_name</code></li><li><code>content</code></li><li><code>content_hash</code></li><li><code>redacted</code></li></ul><p>约束：</p><ul><li>业务表不保存文件预览或下载 URL。归档只保存 <code>file_id</code>。</li><li>敏感参数必须在入库前脱敏。</li></ul><h3 id="_8-8-mango-job-event" tabindex="-1">8.8 <code>mango_job_event</code> <a class="header-anchor" href="#_8-8-mango-job-event" aria-label="Permalink to &quot;8.8 \`mango_job_event\`&quot;">​</a></h3><p>append-only 事件表。</p><p>事件类型：</p><ul><li><code>JOB_CREATED</code></li><li><code>JOB_ENABLED</code></li><li><code>SCHEDULE_FIRED</code></li><li><code>INSTANCE_CREATED</code></li><li><code>ATTEMPT_DISPATCHED</code></li><li><code>ATTEMPT_STARTED</code></li><li><code>LOG_APPENDED</code></li><li><code>ATTEMPT_SUCCEEDED</code></li><li><code>ATTEMPT_FAILED</code></li><li><code>ATTEMPT_TIMEOUT</code></li><li><code>INSTANCE_SUCCEEDED</code></li><li><code>INSTANCE_FAILED</code></li><li><code>WORKER_REGISTERED</code></li><li><code>WORKER_HEARTBEAT_LOST</code></li><li><code>ALARM_TRIGGERED</code></li></ul><p>用途：</p><ul><li>排障。</li><li>审计。</li><li>告警异步消费。</li><li>后续指标聚合。</li></ul><h3 id="_8-9-告警和审计" tabindex="-1">8.9 告警和审计 <a class="header-anchor" href="#_8-9-告警和审计" aria-label="Permalink to &quot;8.9 告警和审计&quot;">​</a></h3><ul><li><code>mango_job_alarm_rule</code>: 告警规则。</li><li><code>mango_job_alarm_record</code>: 告警触发记录。</li><li><code>mango_job_operation_log</code>: 用户操作审计。</li></ul><p>通知发送由 <code>mango-notice</code> 完成。Job 模块在失败实例终态后使用固定业务 Key <code>job.instance.failed</code> 提交 <code>SendNoticeCommand</code>，模板参数和接收方表达式来自启用的告警规则；系统消息、短信、邮件或企业微信通道由 <code>mango-notice</code> 的业务类型、渠道模板和接收人配置负责。</p><h2 id="_9-状态机" tabindex="-1">9. 状态机 <a class="header-anchor" href="#_9-状态机" aria-label="Permalink to &quot;9. 状态机&quot;">​</a></h2><h3 id="_9-1-任务定义状态" tabindex="-1">9.1 任务定义状态 <a class="header-anchor" href="#_9-1-任务定义状态" aria-label="Permalink to &quot;9.1 任务定义状态&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DRAFT -&gt; ENABLED -&gt; PAUSED -&gt; ENABLED</span></span>
<span class="line"><span>      -&gt; DISABLED -&gt; DELETED</span></span></code></pre></div><p>规则：</p><ul><li><code>DRAFT</code> 不参与调度。</li><li><code>ENABLED</code> 必须有有效调度表达式和 handler capability。</li><li><code>PAUSED</code> 保留定义和游标，不产生新实例。</li><li><code>DISABLED</code> 停止调度，允许保留历史实例。</li><li><code>DELETED</code> 软删。删除前必须停止调度，运行中实例按用户选择取消或保留。</li></ul><h3 id="_9-2-实例状态" tabindex="-1">9.2 实例状态 <a class="header-anchor" href="#_9-2-实例状态" aria-label="Permalink to &quot;9.2 实例状态&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CREATED -&gt; WAITING -&gt; DISPATCHED -&gt; RUNNING -&gt; SUCCESS</span></span>
<span class="line"><span>                                      -&gt; RETRY_WAITING -&gt; WAITING</span></span>
<span class="line"><span>                                      -&gt; FAILED</span></span>
<span class="line"><span>                                      -&gt; TIMEOUT</span></span>
<span class="line"><span>                                      -&gt; CANCELED</span></span></code></pre></div><p>规则：</p><ul><li>实例代表业务运行，一次实例可有多次 attempt。</li><li><code>FAILED/TIMEOUT</code> 是否进入 <code>RETRY_WAITING</code> 由 retry policy 决定。</li><li><code>RETRY_WAITING</code> 到期后回到 <code>WAITING</code>，创建下一次 attempt。</li><li>取消实例时，JobCenter 先停止新 attempt，再通知已租约 Worker 取消。</li></ul><h3 id="_9-3-attempt-状态" tabindex="-1">9.3 Attempt 状态 <a class="header-anchor" href="#_9-3-attempt-状态" aria-label="Permalink to &quot;9.3 Attempt 状态&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>READY -&gt; LEASED -&gt; RUNNING -&gt; SUCCEEDED</span></span>
<span class="line"><span>                         -&gt; FAILED</span></span>
<span class="line"><span>                         -&gt; TIMED_OUT</span></span>
<span class="line"><span>                         -&gt; LOST</span></span>
<span class="line"><span>                         -&gt; CANCELED</span></span></code></pre></div><p>规则：</p><ul><li><code>LEASED</code> 必须有 <code>lease_until</code> 和 <code>fencing_token</code>。</li><li>Worker 开始执行后进入 <code>RUNNING</code>。</li><li>只有最新 <code>fencing_token</code> 可以写终态。</li><li>租约过期且 Worker 心跳丢失时进入 <code>LOST</code>。</li></ul><h3 id="_9-4-worker-状态" tabindex="-1">9.4 Worker 状态 <a class="header-anchor" href="#_9-4-worker-状态" aria-label="Permalink to &quot;9.4 Worker 状态&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>REGISTERED -&gt; ONLINE -&gt; DRAINING -&gt; OFFLINE</span></span>
<span class="line"><span>                         -&gt; EXPIRED</span></span>
<span class="line"><span>             -&gt; DISABLED</span></span></code></pre></div><p>规则：</p><ul><li><code>ONLINE</code> 来自真实心跳。</li><li><code>DRAINING</code> 不接新任务，等待当前 attempt 结束。</li><li>心跳超时进入 <code>EXPIRED</code>，其租约由 JobCenter 回收。</li><li>手动禁用后不能自动接单。</li></ul><h2 id="_10-调度设计" tabindex="-1">10. 调度设计 <a class="header-anchor" href="#_10-调度设计" aria-label="Permalink to &quot;10. 调度设计&quot;">​</a></h2><h3 id="_10-1-调度扫描" tabindex="-1">10.1 调度扫描 <a class="header-anchor" href="#_10-1-调度扫描" aria-label="Permalink to &quot;10.1 调度扫描&quot;">​</a></h3><p>JobCenter 周期扫描 <code>mango_job_schedule_cursor</code>。</p><p>流程：</p><ol><li>查询 <code>next_fire_time &lt;= now</code> 且任务为 <code>ENABLED</code> 的游标。</li><li>使用数据库行级锁或 CAS 抢占游标；首轮优先数据库 lease/CAS，不引入独立 leader election 服务。</li><li>按 <code>misfire_policy</code> 计算本次触发窗口。</li><li>生成 <code>idempotency_key</code>。</li><li>插入 <code>mango_job_instance</code>，依赖唯一约束防重复。</li><li>更新 <code>last_fire_time</code> 和 <code>next_fire_time</code>。</li><li>发布 <code>SCHEDULE_FIRED</code> 和 <code>INSTANCE_CREATED</code> 事件。</li></ol><h3 id="_10-2-misfire-策略" tabindex="-1">10.2 Misfire 策略 <a class="header-anchor" href="#_10-2-misfire-策略" aria-label="Permalink to &quot;10.2 Misfire 策略&quot;">​</a></h3><p>首轮支持：</p><ul><li><code>SKIP</code>: 错过的调度窗口跳过，只计算下一次。</li><li><code>FIRE_ONCE</code>: 错过多次只补一次。</li><li><code>FIRE_ALL</code>: 逐个补偿错过窗口，但受最大补偿次数保护。</li></ul><h3 id="_10-3-并发策略" tabindex="-1">10.3 并发策略 <a class="header-anchor" href="#_10-3-并发策略" aria-label="Permalink to &quot;10.3 并发策略&quot;">​</a></h3><p>首轮支持：</p><ul><li><code>ALLOW</code>: 允许同一任务多个实例并发。</li><li><code>SKIP_IF_RUNNING</code>: 有运行中实例时跳过本次。</li><li><code>QUEUE</code>: 保留实例，等待前序结束后分发。</li></ul><p>后续候选：</p><ul><li><code>CANCEL_PREVIOUS</code>: 新实例取消旧实例。</li></ul><h2 id="_11-分发与租约设计" tabindex="-1">11. 分发与租约设计 <a class="header-anchor" href="#_11-分发与租约设计" aria-label="Permalink to &quot;11. 分发与租约设计&quot;">​</a></h2><p>JobCenter 分发流程：</p><ol><li>查询 <code>WAITING</code> 实例。</li><li>根据租户、任务归属、应用、处理器、任务编码、能力启用状态和 Worker 在线状态选择 Worker。</li><li>创建 <code>mango_job_attempt</code>。</li><li>写入 <code>lease_owner</code>、<code>lease_until</code>、<code>fencing_token</code>。</li><li>将实例置为 <code>DISPATCHED</code>。</li><li>通过 transport 投递任务或等待 Worker 拉取。</li><li>Worker 确认后 attempt 进入 <code>RUNNING</code>，实例进入 <code>RUNNING</code>。</li></ol><p>租约规则：</p><ul><li>Worker 执行期间必须定期续租。</li><li>JobCenter 扫描租约过期 attempt。</li><li>attempt 过期时，先标记 <code>LOST</code> 或 <code>TIMED_OUT</code>，再按 retry policy 创建新 attempt。</li><li>旧 Worker 即使恢复，也不能用旧 token 写成功。</li></ul><h2 id="_12-worker-设计" tabindex="-1">12. Worker 设计 <a class="header-anchor" href="#_12-worker-设计" aria-label="Permalink to &quot;12. Worker 设计&quot;">​</a></h2><p>Worker 有两种启动方式：</p><ul><li>内嵌 Worker：随 Mango 应用启动，适合单体部署。</li><li>远程 Worker：业务服务引入 <code>mango-job-worker-starter</code>，向 JobCenter 注册。</li></ul><p>Worker 能力：</p><ul><li>注册和续约。</li><li>上报 handler capability。</li><li>拉取任务或接受推送任务。</li><li>执行业务 <code>MangoJobHandler</code>。</li><li>捕获 <code>System.out</code>、<code>System.err</code> 和 logger 输出。</li><li>批量上报日志 chunk。</li><li>上报进度和结果。</li><li>响应取消、排空和下线。</li></ul><p>业务处理器契约：</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MangoJobHandler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> String </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">appCode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> String </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">serviceCode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> appCode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> String </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">workerGroup</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> serviceCode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Set&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">supportedJobCodes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Set.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    String </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">handlerName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    MangoJobHandleResult </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">handle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(MangoJobContext </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">context</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>归属规则：</p><ul><li>任务定义必须保存 <code>ownerService</code> 和 <code>workerGroup</code>；未配置时分别回退到 <code>appCode</code> 和 <code>ownerService</code>。</li><li>Worker 启动时必须注册 <code>serviceCode</code>、<code>workerGroup</code>、<code>appCode</code>、<code>handlerName</code> 和可选 <code>supportedJobCodes</code>。</li><li>JobCenter 派发前按 capability 过滤，禁止所有 Worker 共用一个全局队列后只按空闲随机派发。</li><li>Worker 收到命令后必须按同一归属再次查找本进程 handler；归属不匹配时拒绝执行，不能猜测执行。</li><li>内嵌 Worker 只能由当前 Spring 容器真实 <code>MangoJobHandler</code> 自动注册，不允许手动添加。</li></ul><p><code>MangoJobContext</code> 至少包含：</p><ul><li><code>tenantId</code></li><li><code>appCode</code></li><li><code>jobId</code></li><li><code>jobCode</code></li><li><code>instanceId</code></li><li><code>attemptId</code></li><li><code>triggerType</code></li><li><code>triggerBatchNo</code></li><li><code>idempotencyKey</code></li><li><code>traceId</code></li><li><code>parameter</code></li><li><code>scheduledFireTime</code></li><li><code>log</code></li></ul><p>业务获取参数：</p><ul><li>通过 <code>context.getParameter()</code> 获取完整参数对象。</li><li>推荐通过结构化参数 binding 获取类型化字段。</li><li>禁止业务处理器直接读取 Job 表或第三方引擎上下文。</li></ul><p>租户上下文：</p><ul><li>定时触发没有 Web 请求，JobCenter 必须从任务定义和实例中生成 <code>tenantId</code>、<code>appCode</code>、system actor 和 <code>traceId</code>。</li><li>Worker 执行前由 Mango Job runtime 恢复租户上下文和 trace 上下文。</li><li>业务处理器不显式传递 tenantId，不在 SQL 中手写租户过滤；应接入 Mango 统一租户能力。</li><li>用户手动触发时记录触发用户，Worker 执行仍以实例 tenantId 为准。</li></ul><h2 id="_13-transport-抽象" tabindex="-1">13. Transport 抽象 <a class="header-anchor" href="#_13-transport-抽象" aria-label="Permalink to &quot;13. Transport 抽象&quot;">​</a></h2><p>统一接口：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>MangoJobTransport</span></span>
<span class="line"><span>├── registerWorker</span></span>
<span class="line"><span>├── heartbeat</span></span>
<span class="line"><span>├── acquireTask</span></span>
<span class="line"><span>├── acknowledgeStart</span></span>
<span class="line"><span>├── appendLogs</span></span>
<span class="line"><span>├── reportProgress</span></span>
<span class="line"><span>├── reportResult</span></span>
<span class="line"><span>└── cancelAttempt</span></span></code></pre></div><p>首轮必须实现：</p><ul><li><code>IN_MEMORY</code>: 同进程内存调用，用于单体内嵌 JobCenter + Worker。单体部署禁止为了复用远程链路再绕一圈 HTTP、TCP 端口或本机回环地址。</li><li><code>HTTP_INTERNAL</code>: Mango 内部调用安全机制，用于独立 JobCenter 和远程 Worker。微服务部署时 Worker 可以通过独立端口和 JobCenter 通信。</li></ul><p>通信选择规则：</p><ul><li>单体内嵌：默认且必须使用 <code>IN_MEMORY</code>。JobCenter 直接调用本进程 Worker runtime，不开放 Worker 独立通信端口。</li><li>单体多实例：每个进程内的 JobCenter 到本进程 Worker 使用 <code>IN_MEMORY</code>；跨进程任务分发可以通过数据库租约选择目标 Worker，由目标进程本地 Worker 执行，必要时再启用 <code>HTTP_INTERNAL</code> 做跨进程控制。</li><li>独立 JobCenter + 远程 Worker：必须使用 <code>HTTP_INTERNAL</code>，Worker 通过独立端口注册、心跳、拉取任务、上报日志和结果。</li><li>部署模式切换只改变 transport 和启动配置，不改变任务定义、handler、参数 schema 和调度策略。</li><li><code>IN_MEMORY</code> 和 <code>HTTP_INTERNAL</code> 都必须走同一套实例、attempt、租约、fencing token、日志和状态机，不能因为内存调用绕过治理模型。</li></ul><p>后续候选：</p><ul><li>MQ transport。</li><li>gRPC transport。</li></ul><p>不采用：</p><ul><li>不采用 UDP。</li><li>首轮不采用 Akka，避免引入 ActorSystem、NAT/容器端口暴露和协议栈复杂度。</li></ul><h2 id="_14-部署模式" tabindex="-1">14. 部署模式 <a class="header-anchor" href="#_14-部署模式" aria-label="Permalink to &quot;14. 部署模式&quot;">​</a></h2><h3 id="_14-1-单体内嵌" tabindex="-1">14.1 单体内嵌 <a class="header-anchor" href="#_14-1-单体内嵌" aria-label="Permalink to &quot;14.1 单体内嵌&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Mango Admin Process</span></span>
<span class="line"><span>├── JobCenter</span></span>
<span class="line"><span>└── Embedded Worker</span></span></code></pre></div><p>适用：</p><ul><li>本地开发。</li><li>单体部署。</li><li>小规模业务。</li></ul><p>要求：</p><ul><li><code>IN_MEMORY</code> transport。</li><li>不启动 Worker 独立端口。</li><li>不通过 HTTP、本机回环地址或 TCP 远程协议绕行。</li><li>仍写 <code>mango_job</code>。</li><li>仍使用租约和状态机，不能因为同进程就绕过核心模型。</li></ul><h3 id="_14-2-单体多实例" tabindex="-1">14.2 单体多实例 <a class="header-anchor" href="#_14-2-单体多实例" aria-label="Permalink to &quot;14.2 单体多实例&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Mango Admin #1 ─┐</span></span>
<span class="line"><span>Mango Admin #2 ─┼── mango_job</span></span>
<span class="line"><span>Mango Admin #3 ─┘</span></span></code></pre></div><p>每个实例可同时启动 JobCenter 和 Worker。</p><p>要求：</p><ul><li>调度游标抢占防重复触发。</li><li>Worker 注册使用实例 ID 区分。</li><li>分发使用租约和 fencing token。</li><li>本进程 JobCenter 调本进程 Worker 使用 <code>IN_MEMORY</code>。</li><li>需要跨进程控制、取消或远程拉取时使用 <code>HTTP_INTERNAL</code>，并明确开放端口。</li><li>多节点同时调度不重复创建同一窗口实例。</li></ul><h3 id="_14-3-独立-jobcenter-远程-worker" tabindex="-1">14.3 独立 JobCenter + 远程 Worker <a class="header-anchor" href="#_14-3-独立-jobcenter-远程-worker" aria-label="Permalink to &quot;14.3 独立 JobCenter + 远程 Worker&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Mango JobCenter Cluster ─── mango_job</span></span>
<span class="line"><span>          │</span></span>
<span class="line"><span>          ├── Worker: mango-system</span></span>
<span class="line"><span>          ├── Worker: mango-job</span></span>
<span class="line"><span>          └── Worker: business-service</span></span></code></pre></div><p>适用：</p><ul><li>微服务部署。</li><li>专门的调度中心。</li><li>执行能力独立扩缩容。</li></ul><p>要求：</p><ul><li>Worker 使用 <code>HTTP_INTERNAL</code> 注册、心跳和任务交互。</li><li>Worker 使用独立端口对 JobCenter 暴露内部通信接口，端口来自部署配置或服务注册。</li><li>服务身份、租户上下文和权限边界明确。</li><li>Worker 不直接写实例终态，必须通过 JobCenter API 且携带 token。</li></ul><h2 id="_15-api-设计" tabindex="-1">15. API 设计 <a class="header-anchor" href="#_15-api-设计" aria-label="Permalink to &quot;15. API 设计&quot;">​</a></h2><p>API 分组：</p><ul><li>任务定义 API。</li><li>触发 API。</li><li>执行实例 API。</li><li>Worker API。</li><li>JobCenter API。</li><li>告警规则 API。</li><li>日志 API。</li></ul><p>关键接口：</p><table tabindex="0"><thead><tr><th>接口</th><th>用途</th></tr></thead><tbody><tr><td><code>POST /job/definitions</code></td><td>创建任务定义。</td></tr><tr><td><code>PUT /job/definitions/{id}</code></td><td>修改任务定义。</td></tr><tr><td><code>POST /job/definitions/{id}/enable</code></td><td>启用。</td></tr><tr><td><code>POST /job/definitions/{id}/pause</code></td><td>暂停。</td></tr><tr><td><code>POST /job/definitions/{id}/disable</code></td><td>禁用。</td></tr><tr><td><code>POST /job/definitions/{id}/trigger</code></td><td>手动触发。</td></tr><tr><td><code>GET /job/instances</code></td><td>查询执行实例。</td></tr><tr><td><code>GET /job/instances/{id}</code></td><td>实例详情。</td></tr><tr><td><code>GET /job/instances/{id}/attempts</code></td><td>实例尝试列表。</td></tr><tr><td><code>GET /job/instances/{id}/logs</code></td><td>实例日志。</td></tr><tr><td><code>POST /job/instances/{id}/cancel</code></td><td>取消实例。</td></tr><tr><td><code>POST /job/instances/{id}/retry</code></td><td>重试实例。</td></tr><tr><td><code>GET /job/workers</code></td><td>Worker 列表。</td></tr><tr><td><code>POST /job/workers/{id}/drain</code></td><td>Worker 排空。</td></tr><tr><td><code>POST /job/workers/{id}/disable</code></td><td>Worker 禁用。</td></tr><tr><td><code>GET /job/runtime-status</code></td><td>运行状态，包含 JobCenter、数据库 lease、积压、失败率和 Worker 总览。</td></tr></tbody></table><p>内部 Worker API：</p><table tabindex="0"><thead><tr><th>接口</th><th>用途</th></tr></thead><tbody><tr><td><code>POST /job/internal/workers/register</code></td><td>Worker 注册。</td></tr><tr><td><code>POST /job/internal/workers/heartbeat</code></td><td>心跳。</td></tr><tr><td><code>POST /job/internal/tasks/acquire</code></td><td>拉取任务。</td></tr><tr><td><code>POST /job/internal/attempts/{id}/start</code></td><td>开始执行。</td></tr><tr><td><code>POST /job/internal/attempts/{id}/logs</code></td><td>上报日志。</td></tr><tr><td><code>POST /job/internal/attempts/{id}/result</code></td><td>上报结果。</td></tr></tbody></table><h2 id="_16-多租户、安全和审计" tabindex="-1">16. 多租户、安全和审计 <a class="header-anchor" href="#_16-多租户、安全和审计" aria-label="Permalink to &quot;16. 多租户、安全和审计&quot;">​</a></h2><ul><li>所有用户 API 按 Mango 登录态和租户上下文过滤。</li><li>Worker 内部 API 使用 Mango 内部调用安全机制。</li><li>Worker 注册必须带服务身份、appCode、envCode 和签名。</li><li>Worker 上报结果必须带 <code>attempt_id + fencing_token</code>。</li><li>参数 schema 可标记敏感字段，日志和审计入库前脱敏。</li><li>操作审计记录创建、编辑、启停、触发、取消、重试、Worker 上下线、告警变更。</li><li>Job 模块不保存文件访问 URL；日志归档保存 <code>file_id</code>。</li></ul><h2 id="_17-告警与可观测性" tabindex="-1">17. 告警与可观测性 <a class="header-anchor" href="#_17-告警与可观测性" aria-label="Permalink to &quot;17. 告警与可观测性&quot;">​</a></h2><p>告警场景：</p><ul><li>任务失败。</li><li>任务超时。</li><li>连续失败。</li><li>Worker 离线。</li><li>调度延迟超过阈值。</li><li>实例积压超过阈值。</li><li>失败后恢复。</li></ul><p>告警降噪：</p><ul><li>支持连续失败阈值。</li><li>支持同一任务同一错误窗口聚合。</li><li>支持静默窗口。</li><li>支持恢复通知。</li><li>接收人来源可以是固定用户、角色、部门、任务负责人或表达式。</li></ul><p>指标：</p><ul><li>任务总数、启用数、暂停数。</li><li>实例成功率、失败率、超时率。</li><li>调度延迟。</li><li>队列积压。</li><li>Worker 在线数、容量、负载。</li><li>日志写入延迟。</li></ul><p>通知：</p><ul><li>Job 写告警事件和告警记录，失败实例会按启用的告警规则提交 <code>mango-notice</code>。</li><li><code>mango-notice</code> 根据业务 Key <code>job.instance.failed</code>、渠道模板和模板参数发送系统消息、短信、邮件、企业微信等；内置 SQL 只固化 <code>JOB</code> 业务域下的系统消息模板。</li></ul><h2 id="_18-迁移策略" tabindex="-1">18. 迁移策略 <a class="header-anchor" href="#_18-迁移策略" aria-label="Permalink to &quot;18. 迁移策略&quot;">​</a></h2><p>当前仓库曾存在 PowerJob Adapter 方案的代码、DDL、E2E 和截图证据。当前交付已废弃 PowerJob 运行时集成，迁移必须避免新旧模型混跑。</p><p>迁移步骤：</p><ol><li>标记旧设计和旧交付台账为历史路线。</li><li>新增原生引擎 DDL，避免继续扩展 PowerJob 映射字段。</li><li>删除 <code>PowerJobEngineAdapter</code>、PowerJob 自动配置、PowerJob SDK 依赖和对应测试包。</li><li>API 和 UI 改为原生实例、attempt、Worker、日志事实。</li><li>示例任务改为 <code>IN_MEMORY</code> 和 <code>HTTP_INTERNAL</code> 两种 transport 验证。</li><li>E2E 重新覆盖每 1 分钟 CRON、手动触发、日志输出、失败重试、Worker 下线恢复。</li><li>PowerJob 相关运行时代码不进入默认发布路径；历史调研资料只保留为能力参考。</li></ol><p>兼容策略：</p><ul><li>如果已有测试环境产生 PowerJob 映射数据，不做线上自动迁移。</li><li>原生引擎上线前清理测试数据或提供一次性 migration 工具，工具必须单独评审。</li><li>不把第三方 engine ID 继续放到新 API 和 UI。</li></ul><h2 id="_19-测试和验收" tabindex="-1">19. 测试和验收 <a class="header-anchor" href="#_19-测试和验收" aria-label="Permalink to &quot;19. 测试和验收&quot;">​</a></h2><h3 id="_19-1-单元测试" tabindex="-1">19.1 单元测试 <a class="header-anchor" href="#_19-1-单元测试" aria-label="Permalink to &quot;19.1 单元测试&quot;">​</a></h3><ul><li>Cron/fixed rate/one-time 下一次触发计算。</li><li>Misfire 策略。</li><li>并发策略。</li><li>状态机合法/非法迁移。</li><li>幂等键生成。</li><li>attempt 租约续期、过期、fencing token。</li><li>日志脱敏。</li><li>Worker capability 匹配。</li></ul><h3 id="_19-2-集成测试" tabindex="-1">19.2 集成测试 <a class="header-anchor" href="#_19-2-集成测试" aria-label="Permalink to &quot;19.2 集成测试&quot;">​</a></h3><ul><li>MyBatis-Plus 真实访问 <code>mango_job</code> 数据源。</li><li>多 JobCenter 并发扫描同一游标只创建一个实例。</li><li>Worker 注册、心跳、拉取、执行、日志上报、结果上报。</li><li>Worker 重复心跳注册按租户、应用、引擎和地址幂等更新，不能产生重复 Worker 快照。</li><li>Worker 失联后 attempt 回收和重试。</li><li>单体内嵌模式和 HTTP 远程模式都使用同一任务定义。</li><li>独立 JobCenter Spring Context 与独立 Worker Spring Context 通过 <code>HTTP_INTERNAL</code> 完成注册、派发、Java Handler 执行和日志回传。</li></ul><h3 id="_19-3-e2e-验收场景" tabindex="-1">19.3 E2E 验收场景 <a class="header-anchor" href="#_19-3-e2e-验收场景" aria-label="Permalink to &quot;19.3 E2E 验收场景&quot;">​</a></h3><p>必须保留截图和后台日志证据：</p><ol><li>创建每 1 分钟 CRON 任务，结构化表单配置频次和参数。</li><li>等待至少 3 个调度窗口，执行实例列表出现 3 次调度。</li><li>打开实例日志，看到 <code>System.out</code>、<code>System.err</code>、logger 和返回结果。</li><li>手动触发任务，实例行展示触发来源为手动。</li><li>停用 Worker 后任务不再分发到该 Worker。</li><li>Worker 心跳过期，运行中 attempt 被标记 LOST 并按策略重试。</li><li>单体多实例并发启动，不重复创建同一调度窗口实例。</li><li>权限不足用户不能触发、取消、禁用 Worker。</li><li>失败任务触发 <code>mango-notice</code> 告警记录。</li><li>租户 A 创建任务后，租户 B 看不到定义、实例、日志和租户任务上下文。</li><li>远程 Worker 注册、心跳、拉取任务、上报日志和结果都经过 Mango 内部调用安全机制。</li><li><code>mango_job</code> 独立库可迁移、可备份、可清理，主库不保存 Job 治理表。</li></ol><h2 id="_20-分阶段交付建议" tabindex="-1">20. 分阶段交付建议 <a class="header-anchor" href="#_20-分阶段交付建议" aria-label="Permalink to &quot;20. 分阶段交付建议&quot;">​</a></h2><h3 id="sprint-a-原生模型和旧方案下线设计" tabindex="-1">Sprint A：原生模型和旧方案下线设计 <a class="header-anchor" href="#sprint-a-原生模型和旧方案下线设计" aria-label="Permalink to &quot;Sprint A：原生模型和旧方案下线设计&quot;">​</a></h3><ul><li>新原生引擎 DDL。</li><li>API 契约和状态机。</li><li>旧 PowerJob Adapter 运行时代码下线方案。</li><li>单元测试覆盖状态机、游标、幂等键。</li></ul><h3 id="sprint-b-jobcenter-和内嵌-worker" tabindex="-1">Sprint B：JobCenter 和内嵌 Worker <a class="header-anchor" href="#sprint-b-jobcenter-和内嵌-worker" aria-label="Permalink to &quot;Sprint B：JobCenter 和内嵌 Worker&quot;">​</a></h3><ul><li>调度扫描。</li><li>实例和 attempt。</li><li><code>IN_MEMORY</code> transport。</li><li>不开放 Worker 独立端口。</li><li>日志捕获和查询。</li><li>单体内嵌 E2E。</li></ul><h3 id="sprint-c-多实例安全" tabindex="-1">Sprint C：多实例安全 <a class="header-anchor" href="#sprint-c-多实例安全" aria-label="Permalink to &quot;Sprint C：多实例安全&quot;">​</a></h3><ul><li>游标 CAS/行锁。</li><li>租约续期。</li><li>fencing token。</li><li>Worker 过期回收。</li><li>单体多实例集成测试。</li></ul><h3 id="sprint-d-远程-worker" tabindex="-1">Sprint D：远程 Worker <a class="header-anchor" href="#sprint-d-远程-worker" aria-label="Permalink to &quot;Sprint D：远程 Worker&quot;">​</a></h3><ul><li><code>HTTP_INTERNAL</code> transport。</li><li>Worker starter。</li><li>内部调用安全。</li><li>远程 Worker E2E。</li></ul><p>当前状态：<code>HTTP_INTERNAL</code> transport、远程 Worker starter 自动装配、内部执行 Controller、Worker 注册器和后端跨 Spring Context E2E 已完成。生产级内部调用鉴权策略仍按安全组件统一评审，不作为浏览器人工验收前置项。</p><h3 id="sprint-e-统一-ui-和告警" tabindex="-1">Sprint E：统一 UI 和告警 <a class="header-anchor" href="#sprint-e-统一-ui-和告警" aria-label="Permalink to &quot;Sprint E：统一 UI 和告警&quot;">​</a></h3><ul><li>任务定义、执行实例、Worker、运行状态、告警规则。</li><li>实例行内日志。</li><li><code>mango-notice</code> 接入。</li><li>权限和租户 E2E。</li></ul><h3 id="sprint-f-生产验收" tabindex="-1">Sprint F：生产验收 <a class="header-anchor" href="#sprint-f-生产验收" aria-label="Permalink to &quot;Sprint F：生产验收&quot;">​</a></h3><ul><li>压测。</li><li>长时间 Cron 稳定性。</li><li>日志保留和归档。</li><li>发布文档。</li><li>回滚方案。</li></ul><h2 id="_21-设计红线" tabindex="-1">21. 设计红线 <a class="header-anchor" href="#_21-设计红线" aria-label="Permalink to &quot;21. 设计红线&quot;">​</a></h2><ul><li>禁止把 PowerJob、XXL-JOB、Quartz 作为 Mango Job 第一实现引擎。</li><li>禁止调度事实分裂为 Mango 一份、第三方引擎一份。</li><li>禁止 UI 显示第三方引擎 ID、N/A Worker 或不可解释状态。</li><li>禁止没有 attempt、租约、fencing token 就宣称支持多实例和故障转移。</li><li>禁止没有日志 chunk/index 就宣称支持执行日志。</li><li>禁止没有真实 Worker 心跳就宣称 Worker 在线。</li><li>禁止部署模式改变时要求业务任务定义换类型。</li><li>禁止用 mock、临时代码、固定成功结果作为验收。</li></ul><h2 id="_22-开放问题" tabindex="-1">22. 开放问题 <a class="header-anchor" href="#_22-开放问题" aria-label="Permalink to &quot;22. 开放问题&quot;">​</a></h2><p>以下问题不阻塞设计，但进入 Sprint 前必须逐项确认：</p><ul><li>Cron 解析库选型：优先复用 Spring CronExpression 还是引入独立库。</li><li>日志 chunk 单行最大长度、单次上报最大大小、归档阈值。</li><li><code>IN_MEMORY</code> transport 中 <code>System.out/System.err</code> 捕获的线程隔离策略。</li><li>Worker 手动添加和自动注册冲突时的优先级。</li><li><code>QUEUE</code> 并发策略的最大排队数量和过期策略。</li><li>日志保留天数、单实例最大日志量、单 chunk 大小和归档阈值。</li></ul>`,226)])])}const b=i(n,[["render",t]]);export{k as __pageData,b as default};
