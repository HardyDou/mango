import{_ as o,o as d,c as i,a2 as a}from"./chunks/framework.Fbjs98zA.js";const h=JSON.parse('{"title":"按钮权限不显示排障","description":"","frontmatter":{},"headers":[],"relativePath":"mango-docs/guides/business-integration/permission-button-troubleshooting.md","filePath":"mango-docs/guides/business-integration/permission-button-troubleshooting.md"}'),c={name:"mango-docs/guides/business-integration/permission-button-troubleshooting.md"};function s(t,e,n,l,r,p){return d(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="按钮权限不显示排障" tabindex="-1">按钮权限不显示排障 <a class="header-anchor" href="#按钮权限不显示排障" aria-label="Permalink to &quot;按钮权限不显示排障&quot;">​</a></h1><h2 id="_1-适用场景" tabindex="-1">1. 适用场景 <a class="header-anchor" href="#_1-适用场景" aria-label="Permalink to &quot;1. 适用场景&quot;">​</a></h2><p>用户能打开页面，但新增、编辑、删除、导出、审批等按钮不显示或点击后返回无权限。</p><h2 id="_2-阅读顺序" tabindex="-1">2. 阅读顺序 <a class="header-anchor" href="#_2-阅读顺序" aria-label="Permalink to &quot;2. 阅读顺序&quot;">​</a></h2><table tabindex="0"><thead><tr><th>顺序</th><th>文档</th><th>关注点</th></tr></thead><tbody><tr><td>1</td><td><a href="./../../../mango/mango-platform/mango-access/README">Access 后端 README</a></td><td>接口权限、上下文、拦截边界</td></tr><tr><td>2</td><td><a href="./../../../mango/mango-platform/mango-authorization/README">Authorization 后端 README</a></td><td>权限资源、角色授权、菜单关系</td></tr><tr><td>3</td><td><a href="./../../../mango-ui/packages/rbac/README">@mango/rbac README</a></td><td>前端授权数据和管理页面</td></tr><tr><td>4</td><td><a href="./../../../mango-ui/packages/admin-shell/README">@mango/admin-shell README</a></td><td>登录后权限集合和按钮指令</td></tr></tbody></table><h2 id="_3-接入检查点" tabindex="-1">3. 接入检查点 <a class="header-anchor" href="#_3-接入检查点" aria-label="Permalink to &quot;3. 接入检查点&quot;">​</a></h2><table tabindex="0"><thead><tr><th>环节</th><th>检查点</th></tr></thead><tbody><tr><td>按钮资源</td><td>后端资源中存在按钮 permissionCode</td></tr><tr><td>菜单关系</td><td>菜单资源与按钮权限存在父子关系</td></tr><tr><td>角色授权</td><td>角色管理「分配权限」弹框能看到按钮节点，且角色已绑定对应按钮权限</td></tr><tr><td>用户上下文</td><td>当前用户拥有该角色，且角色在当前租户和组织上下文内生效</td></tr><tr><td>登录态权限</td><td>登录后权限集合包含目标 permissionCode</td></tr><tr><td>按钮展示规则</td><td>登录后 <code>buttonRules</code> 中目标按钮的 <code>displayRule</code> 执行结果为显示</td></tr><tr><td>前端判断</td><td>前端按钮使用的权限码与后端资源一致</td></tr><tr><td>接口校验</td><td>接口层权限校验与前端按钮权限码一致或有清晰映射</td></tr></tbody></table><h2 id="_4-最小闭环" tabindex="-1">4. 最小闭环 <a class="header-anchor" href="#_4-最小闭环" aria-label="Permalink to &quot;4. 最小闭环&quot;">​</a></h2><ol><li>给测试角色授权目标菜单和按钮。</li><li>用测试用户重新登录。</li><li>打开页面确认按钮出现。</li><li>点击按钮并确认接口返回业务成功或明确的业务校验错误。</li><li>取消按钮授权后重新登录，按钮不可见或接口返回无权限。</li></ol><h2 id="_5-常见失败" tabindex="-1">5. 常见失败 <a class="header-anchor" href="#_5-常见失败" aria-label="Permalink to &quot;5. 常见失败&quot;">​</a></h2><table tabindex="0"><thead><tr><th>现象</th><th>优先检查</th></tr></thead><tbody><tr><td>按钮完全不显示</td><td>前端 <code>v-auth</code> 或权限判断使用的 permissionCode</td></tr><tr><td>按钮显示但接口 403</td><td>接口权限注解、Access 上下文、后端授权集合</td></tr><tr><td>管理员可见普通用户不可见</td><td>角色授权、用户角色绑定、组织/岗位限制</td></tr><tr><td>菜单可见按钮不可见</td><td>菜单授权和按钮授权是否分开配置</td></tr><tr><td>有按钮权限但按钮仍隐藏</td><td>按钮展示规则、<code>v-auth</code> 对象写法和页面传入的 <code>row</code> / <code>pageState</code> / <code>query</code> / <code>selectedRows</code></td></tr><tr><td>角色授权弹框看不到按钮</td><td>后端可分配菜单树是否返回按钮节点、按钮资源是否挂在页面菜单下</td></tr><tr><td>重新授权后仍不生效</td><td>登录态权限缓存、前端刷新、token 重新获取</td></tr></tbody></table><h2 id="_6-验证命令" tabindex="-1">6. 验证命令 <a class="header-anchor" href="#_6-验证命令" aria-label="Permalink to &quot;6. 验证命令&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango/pom.xml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -pl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango-platform/mango-access,mango-platform/mango-authorization</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -am</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @mango/rbac</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @mango/admin-shell</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span></code></pre></div><p>模块验证入口：</p><ul><li><a href="./../../../mango/mango-platform/mango-access/README#10-验证方式">Access 验证方式</a></li><li><a href="./../../../mango/mango-platform/mango-authorization/README#10-验证方式">Authorization 验证方式</a></li><li><a href="./../../../mango-ui/packages/rbac/README#10-验证方式">RBAC Frontend 验证方式</a></li><li><a href="./../../../mango-ui/packages/admin-shell/README#10-验证方式">Admin Shell 验证方式</a></li></ul><h2 id="_7-关联规则" tabindex="-1">7. 关联规则 <a class="header-anchor" href="#_7-关联规则" aria-label="Permalink to &quot;7. 关联规则&quot;">​</a></h2><ul><li><a href="./../../../mango-pmo/rules/08-capability-docs">能力说明维护规范</a></li><li><a href="./../../../mango-pmo/rules/03-ai-coding-redlines">AI 编码红线</a></li></ul><h2 id="_8-变更影响记录" tabindex="-1">8. 变更影响记录 <a class="header-anchor" href="#_8-变更影响记录" aria-label="Permalink to &quot;8. 变更影响记录&quot;">​</a></h2><ul><li><p>v2026.06.30-maven-1.0.1-admin-branding-cli-release 发布固定后端 Maven <code>1.0.1</code> 和后台品牌配置前端批次；不改变既有按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。品牌配置保存权限使用 <code>system:admin-branding:edit</code>，授权后需重新登录刷新权限集合。</p></li><li><p>PR #327 扩展 <code>AUTH_SUBJECT_ROLE</code> 基线声明，支持通过 <code>subjectId</code>、<code>subjectCode</code>、<code>memberNo</code> 或 <code>username</code> 解析租户成员后绑定角色；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和前端判断方式。排查清库初始化后的按钮缺失时，如依赖成员角色绑定基线，需要额外确认声明中的稳定主体键能解析到未离租的 <code>tenant_member</code>。</p></li><li><p>PR #314 修复授权 API 资源运行时匹配和同步覆盖：同 method + path 的旧模块 active 资源会在新扫描结果注册时被禁用，精确路径优先于通配符或路径变量匹配。按钮 <code>permissionCode</code>、按钮展示规则、角色按钮授权关系、接口鉴权注解、租户边界和前端判断方式不变。排查按钮显示但基础接口 403 时，需要区分接口是否应为 <code>LOGIN</code>/<code>PUBLIC</code> 基础入口；若是基础入口，确认资源同步已执行、同路由旧 <code>PERMISSION</code> 记录已禁用，并刷新 API 资源运行时缓存；真正的业务动作接口仍按按钮权限码授权。</p></li><li><p>本次 PR 允许授权快照从页面菜单 <code>permissions</code> 读取权限码，页面级 <code>menuType=2</code> 权限和按钮级 <code>menuType=3</code> 权限都会进入登录态权限集合；不改变按钮 <code>permissionCode</code>、按钮展示规则、接口鉴权注解、租户边界和前端判断方式。排查按钮或接口无权限时，除按钮节点外，也要确认所属页面菜单是否已授权且页面 <code>permissions</code> 是否包含复用的接口权限码。</p></li><li><p>v2026.06.29-workflow-return-cli-db-release 发布 <code>workflow:task:return</code> 权限和前端聚合版本锁；不改变既有按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。新增工作流退回按钮需在资源同步后按常规按钮权限流程授权。</p></li><li><p>PR #295 只治理 Issue #183 后端测试规范、Mockito 审计和核心 service/resource handler 集成测试；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。</p></li><li><p>PR #183 只治理后端测试规范、Mockito 审计和授权资源处理器测试样板；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。</p></li><li><p>v2026.06.27-workflow-history-dialog-release 同步发布工作流 UI 修复批次和 <code>@mango/admin@1.0.33</code>、<code>@mango/cli@1.0.46</code> 版本锁；不改变按钮权限码、角色授权树、登录态权限聚合、按钮展示规则、菜单页面 key、租户绑定和本场景排障步骤。</p></li><li><p>v2026.06.27-admin-shell-menu-redirect-release 发布 <code>@mango/admin-shell@1.0.28</code>、<code>@mango/admin@1.0.32</code> 和 <code>@mango/cli@1.0.45</code>，仅让业务项目可通过 npm 包消费 Issue #274 的目录菜单 redirect 修复；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。</p></li><li><p>Issue #274 修复 <code>@mango/admin-shell</code> 目录型菜单 redirect 解析，仅影响点击目录菜单或直接访问目录 path 时的页面落点；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。若用户先被目录菜单带到其它可访问子页面，再发现按钮不可见，仍按当前实际页面的按钮权限码和登录态权限集合排查。</p></li><li><p>v2026.06.27-system-component-release 同步发布 <code>@mango/system@1.0.11</code> 及其前端依赖批次，仅对齐 npm 物料和 CLI/starter 版本锁；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。业务项目排查按钮缺失时，仍先确认前端包批次一致、权限资源已同步、用户登录态已刷新。</p></li><li><p>PR #267 新增通知公告管理和用户公告确认能力，公告管理页面会使用 <code>notice:announcement:*</code> 类权限控制新增、编辑、发布、下线等动作；不改变按钮 <code>permissionCode</code> 机制、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。若公告管理按钮不可见或接口报无权限，按角色是否授权对应 <code>notice:announcement:*</code> 权限、登录态是否刷新、页面 <code>v-auth</code>/接口鉴权是否一致的顺序排查。</p></li><li><p>PR #256 将后台工作台默认布局调整为页面内固定配置，仅影响没有个人工作台配置或恢复默认后的首页卡片排布；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。</p></li><li><p>PR #253 新增 Resource Registry 的 <code>AUTH_ROLE</code>、<code>AUTH_ROLE_DATA_SCOPE</code>、<code>AUTH_SUBJECT_ROLE</code> 基线声明，可让角色和成员角色绑定随资源同步注入；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。排查清库初始化后的按钮缺失时，可额外确认角色基线和主体角色绑定声明是否同步成功。</p></li><li><p>Issue #264 发布 <code>@mango/grid-widgets@1.0.3</code>、<code>@mango/system@1.0.10</code>、<code>@mango/admin-pages@1.0.11</code>、<code>@mango/admin-shell@1.0.25</code>、<code>@mango/admin@1.0.29</code>、<code>@mango/cli@1.0.42</code>，补齐此前未进入 npm 物料的工作台日历小组件和新版系统配置页面；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。业务项目排查首页日历缺失或系统配置页面旧版时，应先确认前端包和 CLI/starter 锁已升级到本批次。</p></li><li><p>PR #246 发布 <code>@mango/grid-widgets@1.0.2</code>、<code>@mango/admin-shell@1.0.23</code>、<code>@mango/admin@1.0.27</code>、<code>@mango/cli@1.0.39</code>，用于修复 grid widgets 样式发布产物并对齐业务项目版本锁；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。</p></li><li><p>PR #243 新增 CMS 管理页面、CMS 菜单按钮权限和 <code>@mango/cms</code> 前端页面包；不改变按钮 <code>permissionCode</code> 机制、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。排查 CMS 按钮时按后端 <code>mango-cms</code> 菜单资源与 <code>@mango/cms</code> 页面说明确认 <code>cms:*</code> 权限码是否授权。</p></li><li><p>PR #241 发布 <code>@mango/admin-shell@1.0.22</code>、<code>@mango/admin@1.0.25</code> 并新增工作流业务回传路径与审批任务详情页布局优化；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。回传路径仅改变工作流任务详情页返回落点，不新增按钮权限判断。</p></li><li><p>PR #235 发布 <code>@mango/admin-shell@1.0.21</code>、<code>@mango/admin@1.0.24</code> 并对齐 <code>@mango/cli@1.0.36</code> 的发布版本锁；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。</p></li><li><p>本次 PR 调整 <code>@mango/admin-shell</code> 在布局 1、2、4 下的 footer 贴边和内容区底部安全距离；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。</p></li><li><p>本次 PR 新增 <code>@mango/grid-widgets</code> 我的申请系统小组件和 workflow 申请统计接口；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。我的申请小组件只复用 <code>workflow:task:list</code> 权限读取统计和跳转已有申请列表，不新增按钮权限判断。</p></li><li><p>本次 PR 新增 <code>@mango/grid-widgets</code> 我的待办系统小组件和 workflow 待办统计接口；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。我的待办小组件只复用 <code>workflow:task:list</code> 权限读取统计和列表，不新增按钮权限判断。</p></li><li><p>本次 PR 为管理端 Element Plus 全局中文 locale 配置，确保分页等内置组件默认文案使用中文；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、启动方式和本场景排障步骤。</p></li><li><p>本次 PR 新增 <code>@mango/grid-widgets</code> 消息中心系统小组件；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。消息中心小组件的查看全部和全部已读能力只调用已有通知能力，不新增或绕过按钮权限判断。</p></li><li><p>本次 PR 新增 <code>@mango/grid-widgets</code> 用户信息系统小组件，并整理系统小组件目录；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。用户信息小组件的个人中心和修改密码按钮只跳转已有页面，不新增或绕过按钮权限判断。</p></li><li><p>PR #216 加固前端 <code>@mango/*</code> npm 包发布边界，非 CLI 包不再发布 <code>src</code> 等源码目录，并补充发布包 tarball 和业务消费 typecheck 基线；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。业务项目应继续使用公开 package 入口和样式入口，升级到后续发布的新包版本后重新运行前端 typecheck。</p></li><li><p>PR #215 新增 <code>@mango/grid-widgets</code> 小组件注册聚合能力与工作台快捷入口小组件；本次不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。快捷入口小组件只基于已可见菜单生成入口，不新增或绕过按钮权限判断。</p></li><li><p>本次 PR 仅纠正菜单管理页面字段文案、补齐 RBAC views README 页面 key，并补充授权聚合测试中 <code>buttonRules</code> 断言；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。</p></li><li><p>Issue #250 新增 <code>system:user:unlock</code> 和 <code>system:user:reset-password</code> 的用户管理动作，且用户列表的“解锁”按钮会常驻展示但在未锁定时禁用；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界和按钮展示规则执行逻辑。排查解锁按钮点了报无权限时，需要确认角色是否已获授权且登录态已重新获取最新权限集合。</p></li><li><p>PR #207 补齐 <code>mango-resource-api</code> 中的 <code>ResourceTypes.FRONTEND_APP_REGISTRY</code> 和 <code>ResourceTypes.FRONTEND_MODULE_RUNTIME_STRATEGY</code> Java 常量，并保持授权侧 <code>AuthorizationResourceTypes</code> 兼容别名；不改变按钮 <code>permissionCode</code>、登录态权限集合、 角色按钮授权关系、接口鉴权、租户边界、按钮展示规则、前端运行态同步资源类型字符串和本场景排障步骤。</p></li><li><p>PR #206 新增授权侧前端运行态 Resource Registry 同步和 <code>runtimeDescriptor</code> 返回的部署 profile、前端应用注册、模块运行策略信息；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则和本场景排障步骤。清库重建或 1.0 rebase 升级后，若按钮权限正常但前端运行态应用或模块策略缺失，需要额外确认 <code>FRONTEND_APP_REGISTRY</code> 和 <code>FRONTEND_MODULE_RUNTIME_STRATEGY</code> 声明是否已同步到授权前端运行态表。</p></li><li><p>Issue #186 runtime baseline follow-up 补齐 Resource Registry 运行态验收、Nacos 能力 app 配置、Feign 动态目标保持、系统菜单套餐继承和管理端 E2E 基线；不改变按钮 permissionCode、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界和页面交互。业务升级后排查按钮缺失时，除角色授权外，还要确认对应 <code>AUTH_MENU</code> 的 <code>packageCodes</code> 继承结果、<code>API_RESOURCE</code> 同步日志和远程 Resource Registry 上报来源服务。</p></li><li><p>PR #199 将平台菜单和接口权限资源统一收敛到 Resource Registry 注入链路，并加固 starter/module 依赖边界；不改变按钮 permissionCode、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界和页面交互。清库重建或 1.0 rebase 升级后，排查按钮缺失时优先确认 <code>API_RESOURCE</code>、<code>AUTH_MENU</code> 声明已同步，Resource Registry 同步日志成功，且目标模块 handler 已消费声明。</p></li><li><p>PR #195 加固前端 <code>@mango/*</code> 包的 <code>exports</code>、<code>types</code> 和生成声明文件，使业务项目通过发布后的 <code>dist</code> 产物独立消费；不改变按钮 permissionCode、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、页面、启动方式和本场景排障步骤。业务项目应继续使用公开 package 入口和 <code>./style.css</code>，不要依赖包内 <code>src</code> 路径。</p></li><li><p>PR #194 发布资源注册中心版本并升级 <code>@mango/admin@1.0.23</code>、<code>@mango/admin-shell@1.0.20</code>、<code>@mango/rbac@1.0.8</code>、<code>@mango/common@1.0.10</code>、<code>@mango/cli@1.0.34</code> 等前端包；不改变按钮 permissionCode、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、页面和本场景排障步骤。业务升级时应成组升级前端 <code>@mango/*</code> 包并刷新后端 Mango <code>1.0.0-SNAPSHOT</code> 依赖。</p></li><li><p>PR #193 新增 <code>mango-resource</code> 注册中心并将授权接口资源迁移为资源声明同步；不改变按钮 permissionCode、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、页面和按钮权限排障步骤。排查按钮资源缺失时，需要同时确认 <code>API_RESOURCE</code> 声明是否已由资源同步链路写入授权资源表。</p></li><li><p>PR #181 将授权数据权限提供者改为由 <code>AuthorizationAutoConfiguration</code> 显式注册，修复业务同时引入授权与持久化 starter 时 <code>DataScopeApplier</code> 未创建导致的启动失败；不改变按钮 permissionCode、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、授权 API、配置项、页面和本场景排障步骤。业务只需要刷新后端 Maven <code>1.0.0-SNAPSHOT</code> 依赖，不需要升级 npm 包。</p></li><li><p>PR #179 修复持久化 starter 与授权 starter 组合使用时 <code>DataScopeApplier</code> 自动配置顺序，并补齐授权 core 服务泛型契约；不改变按钮 permissionCode、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、授权 API、配置、页面、启动方式和本场景排障步骤。</p></li><li><p>PR #176 新增按钮展示规则配置，按钮可能在拥有权限后继续按 <code>displayRule</code> 判断显隐；不改变按钮权限码、角色按钮授权关系、接口鉴权和租户边界。排查“有权限但不可见”时，需要同时确认登录态 <code>buttonRules</code>、按钮 <code>menuCode</code> 与 <code>v-auth</code> 的 <code>code</code> 是否一致。</p></li><li><p>PR #171 新增角色数据权限配置入口，数据权限只影响接入 <code>DataScopeApplier</code> 的业务查询范围；不改变按钮 permissionCode、登录态权限集合、角色按钮授权关系、接口鉴权和租户边界。</p></li><li><p>PR #170 新增 <code>@mango/common</code> 的 <code>MangoDialog</code> 并在应用管理页面替换新增/编辑弹框外壳，只影响弹框布局和内容滚动体验；不改变按钮权限码、登录态权限集合、角色授权关系、接口鉴权、租户边界和按钮权限排障路径。</p></li><li><p>PR #169 角色授权弹框改为展示后端可分配菜单树中的按钮节点，业务排障时可直接在角色管理中确认按钮权限是否可被勾选；不改变按钮权限码、登录态权限集合、接口鉴权和租户边界。</p></li><li><p>PR #166 工作台自定义布局新增 <code>@mango/grid-layout</code> 和 <code>mango-grid-layout</code>，布局组件不接管按钮权限判断；不改变按钮权限的公开 API、配置、权限码、租户、页面和运行时行为。</p></li><li><p>PR #153 Maven revision 支持只调整构建和发布版本解析，不改变按钮权限的公开 API、配置、权限、租户、页面和运行时行为。</p></li><li><p>PR 本次持久化基线与 README 发布物料治理只补充业务开发查看 Mango 能力文档的入口，并让 npm 包携带 package README；不改变按钮 permissionCode、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、页面和本场景排障步骤。</p></li><li><p>本次用户信息小组件视觉优化 PR 仅调整 <code>@mango/grid-widgets</code> 中用户信息卡片展示和后台工作台默认布局高度；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。</p></li><li><p>PR 本次新增 <code>@mango/grid-widgets</code> 日历系统小组件，并在工作台默认布局中展示；不改变按钮 permissionCode、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面和本场景排障步骤。</p></li><li><p>本次 PR 隐藏后台布局配置抽屉中的深色模式、组件大小、缓存 Tagsview 和页面动画入口，仅收口未开放或未完整生效的个人偏好配置展示；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面和本场景排障步骤。</p></li><li><p>本次 PR 仅优化工作台系统小组件视觉、提示文案、组件库宽度、快捷入口排布和日历日期字号；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。</p></li><li><p>本次 PR 仅在进入登录页或退出登录时清理后台 TagsView 当前打开标签缓存；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑、页面路由和本场景排障步骤。</p></li><li><p>Issue #259 新增后台品牌配置查询和编辑权限，仅用于控制后台品牌配置页的查看与保存；不改变按钮 <code>permissionCode</code>、登录态权限集合、角色按钮授权关系、接口鉴权、租户边界、按钮展示规则执行逻辑和本场景排障步骤。若品牌配置保存时报 403，优先确认角色是否拥有 <code>system:admin-branding:edit</code>，并在授权变更后重新登录获取最新权限集合。</p></li></ul>`,19)])])}const g=o(c,[["render",s]]);export{h as __pageData,g as default};
