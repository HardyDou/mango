import{_ as a,o as s,c as o,a2 as i}from"./chunks/framework.WuHg5enu.js";const g=JSON.parse('{"title":"Mango Tools","description":"","frontmatter":{},"headers":[],"relativePath":"mango/mango-tools/README.md","filePath":"mango/mango-tools/README.md"}'),t={name:"mango/mango-tools/README.md"};function d(n,e,c,l,r,h){return s(),o("div",null,[...e[0]||(e[0]=[i(`<h1 id="mango-tools" tabindex="-1">Mango Tools <a class="header-anchor" href="#mango-tools" aria-label="Permalink to &quot;Mango Tools&quot;">​</a></h1><h2 id="_1-概览" tabindex="-1">1. 概览 <a class="header-anchor" href="#_1-概览" aria-label="Permalink to &quot;1. 概览&quot;">​</a></h2><p><code>mango-tools</code> 提供 Mango 后端开发期工具，包含无服务器 Java/Spring 架构门禁、质量检查、模块脚手架、CRUD 脚手架和权限资源生成。</p><p>主要使用者是 Mango 维护者、业务模块开发者、CI 门禁和 AI Agent。</p><h2 id="_2-功能清单" tabindex="-1">2. 功能清单 <a class="header-anchor" href="#_2-功能清单" aria-label="Permalink to &quot;2. 功能清单&quot;">​</a></h2><table tabindex="0"><thead><tr><th>能力</th><th>常用入口</th></tr></thead><tbody><tr><td>PR 需要执行 Java/Spring 架构门禁</td><td><code>mvn verify</code>，专项调试使用 <code>mvn mango:architecture</code></td></tr><tr><td>新增业务模块时生成标准目录和基础文件</td><td><code>mango module add</code>（<code>@mango/cli</code>）</td></tr><tr><td>快速生成 CRUD API、实体、Mapper、Service、Controller 等脚手架</td><td>Maven 依赖 / HTTP API / Java API</td></tr><tr><td>根据接口或资源生成权限数据草稿</td><td>Maven 依赖 / HTTP API / Java API</td></tr></tbody></table><h2 id="_3-能力边界" tabindex="-1">3. 能力边界 <a class="header-anchor" href="#_3-能力边界" aria-label="Permalink to &quot;3. 能力边界&quot;">​</a></h2><ul><li>不作为业务运行时依赖。</li><li>不替代真实业务建模、权限设计、README 和测试。</li><li>不保证生成代码可直接生产发布；生成后必须人工补齐业务逻辑和验收。</li></ul><h2 id="_4-模块入口" tabindex="-1">4. 模块入口 <a class="header-anchor" href="#_4-模块入口" aria-label="Permalink to &quot;4. 模块入口&quot;">​</a></h2><ul><li><code>mango-tools</code>：Maven 聚合模块。</li><li><code>mango-architecture-rules</code>：Enforcer 3.6.3、ArchUnit 1.4.2、PMD 7.26.0 规则 JAR。</li><li><code>mango-maven-plugin</code>：Maven 插件，goalPrefix 为 <code>mango</code>。</li><li><code>mango-architecture-verification</code>：Reactor 最后执行的自托管验证模块，避免要求预装本仓插件。</li><li>插件代码读取项目源码、migration、报告文件和 Git 变更，不写运行时数据库。</li></ul><h2 id="_5-接入方式" tabindex="-1">5. 接入方式 <a class="header-anchor" href="#_5-接入方式" aria-label="Permalink to &quot;5. 接入方式&quot;">​</a></h2><p>在 Mango Reactor 中统一调用：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango/pom.xml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> verify</span></span></code></pre></div><p>只调试架构检查且 Reactor 已完成编译时可执行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango/pom.xml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango:architecture</span></span></code></pre></div><p>业务模块必须使用发布版 CLI 的 canonical 模板：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mango</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> module</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> order</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --aggregate</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sales-order</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --aggregate-name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 销售订单</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --module-name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 订单模块</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project-dir</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">di</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">r</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>已有模块内可继续使用其余插件 goal：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango/pom.xml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango:gen-crud</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango/pom.xml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango:gen-permission</span></span></code></pre></div><p><code>mango:gen-module</code> 已 fail-closed 退役，调用时只返回迁移指引，不再生成可能绕过 <code>api/core/starter/starter-remote</code>、typed CRUD 和 PMO 门禁的旧结构。</p><p>具体参数以对应 Mojo 源码和 PMO 规则为准。</p><h2 id="_6-配置说明" tabindex="-1">6. 配置说明 <a class="header-anchor" href="#_6-配置说明" aria-label="Permalink to &quot;6. 配置说明&quot;">​</a></h2><p><code>mango:check</code> 常用参数：</p><table tabindex="0"><thead><tr><th>参数</th><th>示例</th><th>含义</th></tr></thead><tbody><tr><td><code>rule</code></td><td><code>-Drule=all</code></td><td>检查规则范围。</td></tr><tr><td><code>output</code></td><td><code>-Doutput=json</code></td><td>输出格式。</td></tr><tr><td><code>reportFile</code></td><td><code>-DreportFile=target/mango-check-report.json</code></td><td>JSON 报告路径。</td></tr><tr><td><code>mango.check.gate</code></td><td><code>all</code> 或 <code>no-new-violations</code></td><td><code>all</code> 阻断所有问题；<code>no-new-violations</code> 只阻断新增问题。</td></tr><tr><td><code>mango.check.changedFiles</code></td><td><code>path1,path2</code></td><td>显式指定变更文件。</td></tr><tr><td><code>mango.check.changedOnly</code></td><td><code>true</code></td><td>将 <code>no-new-violations</code> 的新增问题限定到可信变更文件，并作用于支持范围过滤的 Mango 规则。必须同时提供 <code>changedFiles</code> 或可解析的 <code>baseRef</code>；静态门禁的范围外发现写入 <code>baselineIssues</code>，规则扫描主动排除的发现写入 <code>excludedIssues</code>。</td></tr><tr><td><code>mango.check.baseRef</code></td><td><code>origin/main</code></td><td>未传 <code>changedFiles</code> 时用 Git diff 解析变更。</td></tr><tr><td><code>mango.check.baselineFile</code></td><td><code>target/baseline.json</code></td><td>存量问题基线报告。</td></tr><tr><td><code>mango.check.codeLevelExcludedModules</code></td><td><code>mango-platform/mango-file-preview</code></td><td>仅从 PMD、Checkstyle、SpotBugs 等代码级静态分析门禁中排除指定模块；Mango 自有规则仍会执行。</td></tr><tr><td><code>mango.check.staticFailurePolicy</code></td><td><code>block</code> 或 <code>report</code></td><td>静态分析委托失败时阻断或只报告。</td></tr><tr><td><code>mango.check.resourceStarterDependencyExceptions</code></td><td><code>artifactId=reason</code></td><td>Resource Registry runtime 依赖例外。必须人工明确确认并写明理由，多个例外用英文逗号分隔。</td></tr></tbody></table><p>依赖边界检查按 <code>api</code> / <code>support</code> / <code>core</code> / <code>starter-*</code> 模型执行：</p><ul><li><code>api</code> 是接口契约，禁止依赖 <code>support</code>、<code>core</code>、<code>starter</code> 或 <code>starter-*</code>。</li><li><code>support</code> 是可被其它 <code>core</code> 依赖的公共支撑能力，禁止依赖 <code>core</code>、<code>starter</code> 或 <code>starter-*</code>，也禁止包含持久化和自动配置内容。</li><li><code>core</code> 可以依赖其它模块 <code>api</code> 或 <code>support</code>，禁止依赖其它模块 <code>core</code>、<code>starter</code> 或 <code>starter-*</code>。</li><li><code>starter-remote</code> 在 <code>io.mango</code> 依赖中只允许本模块 <code>api</code>、本模块 <code>support</code> 和 <code>mango-infra-feign-starter</code>，禁止直接依赖 <code>spring-cloud-starter-openfeign</code>。</li></ul><p>反向内部 Controller（例如独立 Worker 接收平台派发）必须在所属聚合模块显式登记精确类名和 反向路径，并同步到 <code>mango-architecture-verification</code> 的完整 Reactor 验证配置。该登记只建模 已审计的反向适配器，不会关闭 Controller/API、参数绑定、返回包装或内部调用安全规则。</p><p>Resource Registry 依赖边界作为 #186 的专项守护继续保留：非 <code>mango-resource</code> 模块默认只能依赖 <code>mango-resource-api</code>，不能直接依赖 <code>mango-resource-core</code>、<code>mango-resource-support</code>、<code>mango-resource-starter</code>、<code>mango-resource-sync-starter</code> 或 <code>mango-resource-starter-remote</code>。确需例外时，必须在命令行显式传入 <code>artifactId=reason</code>；缺少 reason 的例外不会生效。</p><p>PR 架构检查推荐命令：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango/pom.xml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> verify</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -Dmango.architecture.base=origin/main</span></span></code></pre></div><p><code>mango:architecture</code> 的 <code>reportFile</code>、<code>rootDirectory</code>、<code>debtBaselineFile</code> 和 <code>globalEntityManifest</code> 均接受普通文件系统路径。Mango Maven <code>1.0.20</code> 起，这些参数通过 Maven/Plexus 支持的 <code>java.io.File</code> 接收并在插件内部转换为 <code>Path</code>，兼容 Maven 3；业务 POM 的现有 XML 配置和 <code>-Dmango.architecture.debtBaselineFile=&lt;path&gt;</code> 命令无需改写。</p><p>标准 partial PR 门禁把依赖准备和质量扫描分成两个阶段。干净 Runner 先对直接受影响模块执行带 <code>-am</code> 的跳过测试安装，把尚未发布的上游 SNAPSHOT 放入本地 Maven 仓库；随后质量阶段仍只选择直接受影响模块和外层架构验证模块，不带 <code>-am</code> 或 <code>-amd</code>。<code>mango:check</code> 委托 PMD、Checkstyle、SpotBugs 时只传入包含代码的 Reactor 模块，不把 <code>architecture-verification</code> 或 <code>mango-architecture-verification</code> 再次带入嵌套 Maven。PMD 和 Checkstyle 使用报告 goal 收集完整发现，再由 Mango 按 <code>all</code> 或 <code>no-new-violations</code> 统一判定；工具执行错误仍按 <code>mango.check.staticFailurePolicy</code> 处理，历史发现不会在新增问题分类前直接终止嵌套 Maven。外层 <code>mvn verify</code> 的架构检查不受影响。</p><p>Mango Maven <code>1.0.31</code> 起，静态分析的嵌套 Maven 会继承外层已解析的本地仓库、存在的 user/global settings、offline/update 模式、active profiles，以及 <code>revision</code>、<code>sha1</code>、<code>changelist</code> 三个 CI-friendly 版本属性。任意其它用户属性都不会透传，避免把密码或 token 写入子进程参数和门禁日志。使用 <code>-Dmaven.repo.local=&lt;isolated-repository&gt;</code>、<code>-s &lt;settings.xml&gt;</code> 或私有镜像的业务构建因此与外层依赖解析保持一致。</p><p><code>mango:check</code> 委托 Checkstyle 时始终显式选择规则文件，不使用 Maven Checkstyle 插件的 Sun 默认规则。规则选择优先级如下：</p><ol><li><code>-Dmango.check.checkstyleConfigLocation=&lt;path&gt;</code> 指定的业务规则。</li><li><code>-Dcheckstyle.config.location=&lt;path&gt;</code> 指定的标准 Checkstyle 规则。</li><li>项目根目录的 <code>config/quality/checkstyle.xml</code>，业务项目可以直接维护该文件。</li><li>Mango Maven 插件内置的 Mango Checkstyle 规范。</li></ol><p>Mango 内置规则及 CLI 业务模板允许使用三元表达式，也不对单行字符数设置硬限制；复杂度默认只检查圈复杂度，最大值为 15，不启用 NPath 和布尔表达式复杂度检查。业务项目如需更严格规则，可以通过上述自定义规则入口覆盖。</p><p>例如使用企业自定义规则：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango:check</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -Dmango.check.checkstyleConfigLocation=config/quality/company-checkstyle.xml</span></span></code></pre></div><p>配置路径不存在或规则文件无效时，Checkstyle 会按现有静态工具失败策略报错；不会静默切换到 Sun 规则。JSON 报告的 <code>gateMessages</code> 会标识本次使用的是自定义、项目还是 Mango 内置规则。</p><p>当 partial Reactor 中所有业务模块都没有 Java 源码时，<code>mango:architecture</code> 仍执行 Maven 依赖检查、模块归属和 schema v2 报告生成；bytecode、PMD 与 Java 命名空间检查以空输入记录为 0。非法依赖仍进入 blocking issues，不能通过零源码状态绕过门禁。</p><p>partial Reactor 未包含 API 或 Service 接口模块时，ArchUnit 只能导入外部类型存根。架构检查器按命名和依赖位置继续校验 Controller 的直接 API 契约及 Service 端口，不对缺少方法元数据的存根执行 Controller/API 方法一致性检查；完整 Reactor 导入接口字节码后仍执行全部接口类型和方法一致性校验。</p><p>架构报告固定写入 <code>mango/target/mango-architecture-report.json</code>。schema v2 报告包含完整 Reactor <code>modules</code> 目录，并为 dependency、ArchUnit、PMD 和 blocking 问题写入唯一 <code>moduleKey</code>；无法归属、坐标冲突或 Reactor 数量不完整时 fail-closed。默认 <code>changed</code> 模式先定位变更影响的问题，再只从 Git base SHA 的 <code>mango-pmo/baselines/architecture/debt-budget.json</code> 扣除已批准 stable identities，剩余身份才阻断；PR head 自己修改的预算不能豁免当前新增。报告仍包含全量存量：删除文件会被识别，父 POM 变化传播到全部子 Reactor，<code>module.properties</code> 变化传播到同领域类，外置全局 Entity 清单变化传播到 Entity 规则。<code>-Dmango.architecture.mode=full</code> 用于专项全量治理。Git base 无法解析、baseline schema/identity 非法、PMD 解析失败、包含 Java 源码却未导入到字节码或预期 Java 输入为零时均 fail-closed；整个 Reactor 本身没有 Java 源码时按前述 dependency-only 语义执行。</p><p>一次完整 Reactor 扫描后，可复用同一报告查询或递减模块债务；目录 selector 覆盖其全部 Maven 子模块，唯一 artifactId 定位单模块：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango-pmo/tools/check-architecture-debt-budget.mjs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --module</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango-platform/mango-system</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango-pmo/tools/check-architecture-debt-budget.mjs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --module</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango-system-core</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --write</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango-pmo/tools/check-architecture-debt-budget.mjs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --base-ref</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> merge-base HEAD origin/main)&quot;</span></span></code></pre></div><p>正式预算使用 schema v4，逐模块明细与全局聚合必须精确一致。模块写入只允许持平或下降；禁止使用部分 Reactor 报告写预算、跨模块转移违规或在模块模式接受增加。提交前必须执行最后一条无 <code>--module</code> 的全局终检。</p><p>业务模板的最终验证同时启用 POM-only <code>lockFullMode</code>、<code>lockFullReactor</code> 和 <code>requireFullScope</code>。即使 Enforcer 的 <code>requireProperty</code> 被 <code>skipRules</code> 跳过，架构插件仍拒绝 <code>mode=changed</code>；聚合静态检查要求每个实际包含 Java 编译源码的 Reactor 模块分别生成 PMD、Checkstyle 和 SpotBugs 报告，任一子模块缺报告都会阻断，纯 POM 聚合模块不要求报告。</p><p>使用仓库基线阻断新增问题：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango:check</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -Dmango.check.gate=no-new-violations</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -Dmango.check.baselineFile=../mango-pmo/baselines/mango-check/no-new-violations-baseline.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -Dmango.check.codeLevelExcludedModules=mango-platform/mango-file-preview</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -DreportFile=target/mango-check-report.json</span></span></code></pre></div><p><code>mango-pmo/baselines/mango-check/no-new-violations-baseline.json</code> 只服务仍由 <code>mango:check</code> 管理的存量规则。Java/Spring 架构红线不读取这个旧静态 baseline，而是读取 Git base 中独立的 schema-v4 architecture identity 预算；迁移窗口只读兼容 schema v3 顶层 identity。历史 identity 即使所在类被修改也不误阻断，同规则替换、跨模块移动或任何新 identity 仍直接阻断。</p><p>所有 <code>mango:check</code> JSON 报告都会为 <code>issues</code>、<code>newIssues</code>、<code>baselineIssues</code> 和 <code>excludedIssues</code> 中的问题写入稳定指纹。文件路径在报告生成时按 <code>baseDir</code> 转为项目相对路径；<code>no-new-violations</code> 加载基线时优先使用报告内指纹，因此同一业务仓库在不同绝对 worktree 路径下可以直接复用基线。旧版本生成且缺少指纹的报告仍按现有兼容逻辑读取；跨 worktree 使用前应由包含本修复的插件重新生成一次基线。</p><p><code>mango:architecture</code> 硬校验：Controller 实现 <code>XxxApi</code>、启用 <code>@Validated/@Valid</code>、只依赖 <code>IXxxService</code>、统一返回 <code>R&lt;T&gt;</code>；Service 使用 <code>Require + BizCode/ErrorCode</code> 校验业务前置条件且不返回或拼装 <code>R</code>；Entity、Mapper、Feign、Controller 和 Service 实现必须位于规定模块。</p><p>规则判定以业务边界为准：<code>MANGO-ARCH-BEAN-004</code> 只阻断手工构造的 Spring 托管 Service 实现，不把容器中注册的通用 <code>Map</code>、<code>Set</code> 或异常类型误认成业务 Service；组合 API 输入字段可用 <code>@Valid</code> 递归校验，<code>Require.rethrow</code> 被视为保持原异常语义的显式出口。三类判断均由正反例测试锁定。</p><p>检查当前变更文件内的模块菜单声明：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mvn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mango:check</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -Drule=module-menu</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -Dmango.check.changedOnly=true</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -Dmango.check.changedFiles=mango-platform/mango-demo/mango-demo-core/src/main/resources/db/migration/demo/V1__init_demo.sql</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -Doutput=json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -DreportFile=target/module-menu-check-report.json</span></span></code></pre></div><p><code>module-menu</code> 的 <code>changedOnly</code> 模式仍会扫描 <code>baseDir</code> 下的菜单声明问题，但只有变更文件命中的问题进入阻断结果；未命中的历史问题会写入 JSON 报告的 <code>excludedIssues</code>，用于区分当前范围失败和历史参考问题。</p><h2 id="_7-api-与扩展" tabindex="-1">7. API 与扩展 <a class="header-anchor" href="#_7-api-与扩展" aria-label="Permalink to &quot;7. API 与扩展&quot;">​</a></h2><p>当前 Maven goals：</p><table tabindex="0"><thead><tr><th>Goal</th><th>Mojo</th><th>用途</th></tr></thead><tbody><tr><td><code>mango:architecture</code></td><td><code>ArchitectureMojo</code></td><td>聚合 Enforcer、ArchUnit、PMD 7 架构结果并阻断新增违规。</td></tr><tr><td><code>mango:check</code></td><td><code>CheckMojo</code></td><td>执行 Mango 后端质量检查和 PR 门禁。</td></tr><tr><td><code>mango:gen-module</code></td><td><code>GenModuleMojo</code></td><td>已退役；fail-closed 并指向 <code>mango module add</code>。</td></tr><tr><td><code>mango:gen-crud</code></td><td><code>GenCrudMojo</code></td><td>生成 CRUD 脚手架。</td></tr><tr><td><code>mango:gen-permission</code></td><td><code>GenPermissionMojo</code></td><td>生成权限资源草稿。</td></tr><tr><td><code>mango:baseline-generate</code></td><td><code>BaselineGenerateMojo</code></td><td>在 API 制品构建中回放模块 V，生成并验证每模块唯一 cold baseline。</td></tr></tbody></table><p><code>CheckMojo</code> 当前仍较大，复杂度偏高，是既有技术债；后续拆分时应保持参数兼容和报告格式兼容。</p><h3 id="_7-1-构建期-cold-baseline" tabindex="-1">7.1 构建期 cold baseline <a class="header-anchor" href="#_7-1-构建期-cold-baseline" aria-label="Permalink to &quot;7.1 构建期 cold baseline&quot;">​</a></h3><p>业务 API POM 将 goal 绑定到 <code>generate-resources</code>。生成器扫描当前工程和运行时依赖 JAR 中的 <code>db/migration/&lt;module&gt;/V*.sql</code>，在临时 MySQL 中按 <code>moduleOrder</code> 回放，并把结果写入：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>target/generated-resources/db/baseline/&lt;module&gt;/B&lt;version&gt;__baseline.sql</span></span>
<span class="line"><span>target/generated-resources/META-INF/mango/baseline-manifest.json</span></span></code></pre></div><p>这些目录会被注册为 Maven resource。普通 JAR 从 <code>db</code> 目录下的 <code>baseline</code> 子目录读取。Spring Boot 可执行 JAR 中，B SQL 位于下面所示的 <code>BOOT-INF</code> classes 层级，manifest 必须保留在 JAR 根目录的 <code>META-INF/mango/baseline-manifest.json</code>，不能移动到 <code>BOOT-INF/classes/META-INF/...</code>：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BOOT-INF</span></span>
<span class="line"><span>└── classes</span></span>
<span class="line"><span>    └── db</span></span>
<span class="line"><span>        └── baseline</span></span>
<span class="line"><span>META-INF</span></span>
<span class="line"><span>└── mango</span></span>
<span class="line"><span>    └── baseline-manifest.json</span></span></code></pre></div><p>源码目录不会生成 B；Docker 镜像只复制已经完成校验的最终 JAR。</p><p>最小 POM 配置和 Jenkins 使用方式见<a href="https://github.com/HardyDou/mango/blob/main/mango-docs/guides/business-integration/build-time-cold-baseline.md" target="_blank" rel="noreferrer">业务 API 构建期 cold baseline</a>。常用参数：</p><table tabindex="0"><thead><tr><th>参数</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td><code>mango.baseline.jdbcUrl</code></td><td>无</td><td>一次性 MySQL 服务器 JDBC URL；必填，URL 中原数据库名不会被使用。</td></tr><tr><td><code>mango.baseline.username</code></td><td><code>root</code></td><td>一次性数据库账号。</td></tr><tr><td><code>mango.baseline.passwordEnv</code></td><td><code>MANGO_BASELINE_DB_PASSWORD</code></td><td>保存密码的环境变量名；密码不进入 POM、manifest 或日志。</td></tr><tr><td><code>mango.baseline.characterSet</code></td><td><code>utf8mb4</code></td><td>replay、determinism 和 verify schema 使用的目标字符集；默认与 Mango CLI 创建的业务数据库一致。</td></tr><tr><td><code>mango.baseline.collation</code></td><td><code>utf8mb4_unicode_ci</code></td><td>三类临时 schema 使用的目标排序规则；默认与 Mango CLI 创建的业务数据库一致。</td></tr><tr><td><code>mango.baseline.includedModules</code></td><td>全部发现模块</td><td>当前 API 制品需要打包的模块清单。</td></tr><tr><td><code>mango.baseline.moduleOrder</code></td><td>模块名稳定顺序</td><td>显式配置时必须恰好包含所有发现模块一次。</td></tr><tr><td><code>mango.baseline.moduleGroups</code></td><td>全部为 <code>default</code></td><td><code>module=group</code> 列表；同组模块使用同一对临时 schema，不同组隔离。</td></tr><tr><td><code>mango.baseline.outputDirectory</code></td><td><code>target/generated-resources</code></td><td>仅构建目录；不要指向 <code>src/main/resources</code>。</td></tr><tr><td><code>mango.baseline.keepSchemas</code></td><td><code>false</code></td><td>诊断时保留临时 schema；正常 CI 保持关闭。</td></tr></tbody></table><p>生成过程使用 replay/determinism/verify 三套 schema，三者都会显式使用 <code>mango.baseline.characterSet</code> 和 <code>mango.baseline.collation</code>，不继承构建机 MySQL 的 server 默认值。插件在创建临时 schema 前校验字符集与排序规则组合；未知组合或不安全的标识符会直接阻断构建。B SQL 和 manifest 分别固化实际表结构以及 <code>targetCharacterSet</code>、<code>targetCollation</code>，生成指纹也包含这两个目标值。业务项目如果覆盖默认值，目标空库必须使用相同字符集和排序规则，避免 B 之后新增的 V migration 重新继承另一套数据库语义。</p><p>前两套 schema 独立回放 V 时，确定性比较只忽略标准运行审计时间列 <code>created_at</code>、<code>updated_at</code>、<code>published_at</code>；B SQL 仍保留 replay 中这些列的真实值，verify schema 连续执行 B 两次后仍按全部列比较结构和 migration 静态行。其它列中的 <code>UUID()</code>、当前时间等非确定值继续阻断构建。</p><p>结构比较使用 MySQL 元数据的结构化语义快照，不按 <code>SHOW CREATE</code> 文本逐字比较。表、列、索引和约束的首个差异会定位到 <code>table:&lt;name&gt;</code>、<code>table:&lt;name&gt;.column:&lt;name&gt;</code>、<code>table:&lt;name&gt;.index:&lt;name&gt;</code> 或 <code>table:&lt;name&gt;.constraint:&lt;name&gt;</code>；隐式继承与显式声明只要落库后的 charset/collation 相同即视为等价。视图和触发器使用去除环境噪声后的 canonical DDL 比较。静态数据按列读取并用二进制十六进制值比较，因此字符集转换和不可见字节不会被字符串展示掩盖。存储过程、函数和事件当前 fail closed；重复版本、跨模块对象所有权、制品碰撞、缺失或被修改的 B、不可重入、结构或数据不等价都会使构建失败。安装新生成目录前保留上一次结果，安装异常时回滚。</p><h2 id="_8-数据与初始化" tabindex="-1">8. 数据与初始化 <a class="header-anchor" href="#_8-数据与初始化" aria-label="Permalink to &quot;8. 数据与初始化&quot;">​</a></h2><p>质量和脚手架 goal 不连接生产数据库。<code>baseline-generate</code> 只连接构建参数指定的一次性 MySQL，创建并清理带随机后缀的 replay/determinism/verify schema；不要向它提供生产或共享业务数据库账号。</p><h2 id="_9-管理入口" tabindex="-1">9. 管理入口 <a class="header-anchor" href="#_9-管理入口" aria-label="Permalink to &quot;9. 管理入口&quot;">​</a></h2><p>工具可生成或检查菜单、权限、API 资源相关文件；真正的菜单入库、角色授权和租户隔离仍由 <code>mango-authorization</code>、Flyway migration 和业务初始化流程负责。</p><h2 id="_10-快速开始" tabindex="-1">10. 快速开始 <a class="header-anchor" href="#_10-快速开始" aria-label="Permalink to &quot;10. 快速开始&quot;">​</a></h2><ol><li>使用 <code>mango module add</code> 生成模块；仅在已有模块内使用 <code>gen-crud</code> 生成聚合初稿。</li><li>补齐业务模型、Controller 权限、migration、README、能力地图和测试。</li><li>执行 <code>mvn verify</code>。</li><li>检查生成文件是否符合 PMO 模板和模块边界。</li></ol><p>发布完整非应用后端批次使用：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/publish-maven-batch.sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --all-non-app</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --release-version</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">versio</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --verify-base-url</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">maven-consume-repositor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>脚本在一次 Reactor deploy 中发布普通平台模块，并把 <code>mango-architecture-verification</code> 的扁平化 POM 单独部署；这样不会在排除 <code>mango-app/**</code> 的发布 Reactor 中错误执行该模块的全 Reactor verify 阶段。发布后仍统一回查全部目标坐标。</p><h2 id="_11-问题排查" tabindex="-1">11. 问题排查 <a class="header-anchor" href="#_11-问题排查" aria-label="Permalink to &quot;11. 问题排查&quot;">​</a></h2><ul><li><code>mango:architecture</code> 在 goal 执行前报告 <code>Cannot create instance of interface java.nio.file.Path</code>：升级到 Mango Maven <code>1.0.20</code> 或更高版本；这是旧插件 descriptor 的参数绑定缺陷，不能通过关闭架构门禁绕过。</li><li><code>mango:check</code> 报存量问题：PR 模式使用 <code>no-new-violations</code> 和 baseline，但不能把新增问题放进 baseline。</li><li><code>no-new-violations</code> 在分类前被 PMD/Checkstyle 历史问题直接终止：升级到 Mango Maven <code>1.0.19</code> 或更高版本；不得通过 <code>skip</code>、<code>report</code> 策略或修改业务规则绕过。</li><li>partial PR 在干净 Runner 报上游 SNAPSHOT 找不到：确认 workflow 已先执行受影响模块的依赖安装阶段，并且 scope classifier 输出了非空 <code>maven_dependency_projects</code>；不要给后续质量命令追加 <code>-am</code>。</li><li><code>mango:check</code> 的嵌套 Maven 在外层隔离仓库已安装依赖后仍转向默认仓库：升级到 Mango Maven <code>1.0.31</code> 或更高版本，并保留外层 <code>-Dmaven.repo.local</code>/<code>-s</code> 参数；不要把尚未验证的候选版本提前发布到远端来绕过本地门禁。</li><li><code>mango:check</code> 的嵌套 Maven 报 <code>mango.architecture.skip</code> 等治理属性缺失：确认使用的 Mango Maven 插件已经包含治理聚合模块过滤；架构验证应由外层 <code>mvn verify</code> 执行，不应进入 PMD、Checkstyle、SpotBugs 的嵌套 Reactor。</li><li>生成代码编译不过：脚手架只提供结构，业务字段、依赖和 mapper 仍要补齐。</li><li>生成权限后页面仍无按钮：还需要菜单资源入库、角色授权和前端按钮权限接入。</li><li>Maven 批次只缺 <code>mango-architecture-verification</code> POM：使用当前 <code>publish-maven-batch.sh</code> 重新规划新版本发布；不可变版本已经尝试后只能先核对仓库事实，禁止整批重发。</li></ul><h2 id="_12-相关文档" tabindex="-1">12. 相关文档 <a class="header-anchor" href="#_12-相关文档" aria-label="Permalink to &quot;12. 相关文档&quot;">​</a></h2><ul><li><a href="./../../mango-pmo/rules/backend/05-module">后端模块规范</a></li><li><a href="./../../mango-pmo/rules/backend/11-module-menu">模块菜单规范</a></li><li><a href="./../../mango-pmo/rules/08-capability-docs">能力说明维护规范</a></li><li><a href="./../../mango-pmo/rules/05-ai-delivery-quality">AI 交付质量门禁</a></li></ul><h2 id="_13-issue-690-构建消费说明" tabindex="-1">13. Issue #690 构建消费说明 <a class="header-anchor" href="#_13-issue-690-构建消费说明" aria-label="Permalink to &quot;13. Issue #690 构建消费说明&quot;">​</a></h2><p>业务仓若使用过 Maven <code>1.0.30</code> 或其它 <code>1.0.3x</code>，应等待包含 #690 修复的新 release tuple，并同步 Maven、CLI、PMO 和前端矩阵。BSQL 生成目录必须进入最终 JAR 的 <code>BOOT-INF</code> 下 <code>classes</code>、<code>db</code>、<code>baseline</code> 嵌套层级，manifest 保留在 JAR 根 <code>META-INF/mango/baseline-manifest.json</code>；结构化比较、确定性回放、重复版本、对象所有权和不可重入校验任一失败都应 fail closed。升级后至少执行 <code>mvn verify</code>、<code>mvn install</code>、独立 consumer offline verify 和 Boot JAR <code>java -jar</code> health 验证，不能通过跳过 baseline 或缩小 Reactor 掩盖问题。</p><h2 id="_14-补充资料" tabindex="-1">14. 补充资料 <a class="header-anchor" href="#_14-补充资料" aria-label="Permalink to &quot;14. 补充资料&quot;">​</a></h2><ul><li><a href="./../../mango-docs/capabilities/README">Mango 能力地图</a></li></ul>`,87)])])}const k=a(t,[["render",d]]);export{g as __pageData,k as default};
