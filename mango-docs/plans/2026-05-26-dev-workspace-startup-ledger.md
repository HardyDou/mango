# 开发环境稳定启动规范与脚本交付台账

| ID | 来源 | 要求 | 设计决策 | 交付物 | 验收方式 | 状态 | 证据文件 |
|---|---|---|---|---|---|---|---|
| DEVENV-001 | 用户要求 | 在 PMO 或合适位置追加开发环境规范 | 新增 PMO 长期规范，并接入规则索引 | `mango-pmo/rules/02-dev-environment.md`、`mango-pmo/rules/index.json` | PMO preflight 能返回开发环境规范 | DONE | `node mango-pmo/tools/pmo-preflight.mjs --role dev --phase develop --task "启动开发环境" --paths "scripts/dev-workspace.sh"` |
| DEVENV-002 | 用户要求 | 每个工作区初始化独立且稳定的后端端口、前端端口、数据库名称 | 生成 `.mango/dev-workspace.env`；存在时不覆盖；使用本机注册表避免跨工作区重复 | `scripts/dev-workspace.sh` | 执行 `scripts/dev-workspace.sh init` 并确认配置可打印 | DONE | `scripts/dev-workspace.sh init`、`scripts/dev-workspace.sh print` |
| DEVENV-003 | 用户要求 | 提供 shell 脚本启动前后端，减少命令错误 | `start` 先启动后端并等待健康检查，再以前端 env 覆盖代理目标和端口 | `scripts/dev-workspace.sh` | shell 语法检查通过；命令结构覆盖 `start/backend/frontend/print` | DONE | `bash -n scripts/dev-workspace.sh` |
| DEVENV-004 | PMO 要求 | 建立设计说明和交付台账并完成验证 | 使用本计划和台账记录范围、验证、风险 | `mango-docs/plans/2026-05-26-dev-workspace-startup-plan.md`、`mango-docs/plans/2026-05-26-dev-workspace-startup-ledger.md` | 交付台账检查通过 | DONE | `node mango-pmo/tools/delivery-contract-check.mjs --design mango-docs/plans/2026-05-26-dev-workspace-startup-plan.md --ledger mango-docs/plans/2026-05-26-dev-workspace-startup-ledger.md --mode verify` |
| DEVENV-005 | 用户要求 | 新开 worktree 时必须执行初始化脚本 | 使用 `post-checkout` hook 自动执行 `scripts/dev-workspace.sh init`，并提供 `install-hooks` 配置 `core.hooksPath` | `.githooks/post-checkout`、`scripts/dev-workspace.sh` | 配置 hooksPath 后创建临时 worktree，确认目标 worktree 生成 `.mango/dev-workspace.env` | DONE | `scripts/dev-workspace.sh install-hooks`、`git worktree add --detach <tmp> <temp-commit>` |
| DEVENV-006 | 用户要求 | 删除 worktree 时关闭服务，并明确是否删除数据库 | 新增 `worktree-remove`：先停止目标 env 端口服务，再移除 worktree；默认保留数据库，显式 `--drop-db` 才删除 `mango_dev_*` | `scripts/dev-workspace.sh`、`mango-pmo/rules/02-dev-environment.md` | 对临时 worktree 执行 `scripts/dev-workspace.sh worktree-remove <tmp>`，确认 worktree 被移除且数据库默认保留策略输出 | DONE | `scripts/dev-workspace.sh worktree-remove <tmp>` |
| DEVENV-007 | 用户要求 | 开发环境默认关闭 Office 插件，后续需要再打开 | 默认生成 `MANGO_OFFICE_PLUGIN_ENABLED=false`，后端启动参数传入 `--office.plugin.enabled=false` | `scripts/dev-workspace.sh`、`mango-pmo/rules/02-dev-environment.md` | 重建真实 worktree 后启动，确认后端不再因 office.home 缺失失败 | DONE | `scripts/dev-workspace.sh start` |
| DEVENV-008 | 真实 worktree 验证 | 新 worktree 前端缺少 `node_modules` 时启动失败 | 前端启动前检查 `node_modules/.bin/vite`，缺失且允许自动安装时执行 `pnpm install --frozen-lockfile` | `scripts/dev-workspace.sh`、`mango-docs/plans/2026-05-26-dev-workspace-startup-plan.md` | 真实 worktree 启动触发依赖准备并进入前端启动 | DONE | `scripts/dev-workspace.sh start` |
| DEVENV-009 | 用户要求 | 新 worktree 必须使用新数据库，不能误复用旧库 | 首次生成 env 时检查 MySQL 中候选库是否已存在，存在则换下一个候选；已有 env 仍保持稳定 | `scripts/dev-workspace.sh`、`mango-pmo/rules/02-dev-environment.md` | 预创建旧候选库后重新创建同路径 worktree，确认脚本选择新的库名 | DONE | `git worktree add --detach .mango/worktrees/full-flow-test <temp-commit>`、`scripts/dev-workspace.sh print` |
