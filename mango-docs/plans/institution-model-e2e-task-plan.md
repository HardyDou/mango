# 机构模型改造与 E2E 任务计划

## 目标

将用户可见的“租户”收敛为“机构/企业空间”，同时保留底层 `tenant_id` 作为技术隔离字段。

本阶段不做大规模表名、字段名物理重命名，避免把业务概念调整和底层迁移风险混在一起。对前端用户、接口文档、菜单、页面、错误提示、E2E 用例，统一使用“机构”。对后端隔离、上下文、SQL 插件、JWT claim，继续使用 `tenantId`。

## 术语边界

| 层级 | 名称 | 说明 |
|---|---|---|
| 产品/用户可见 | 机构 | 元丰行、担保公司、银行、客户企业等拥有独立后台空间的业务主体 |
| 用户可见后台空间 | 机构空间/企业空间 | 机构自己的组织、成员、角色、流程、菜单权限边界 |
| 技术隔离字段 | tenantId / tenant_id | 后端用于行级隔离、权限隔离、审计隔离的技术字段 |
| 登录身份 | 账号 | 全局可登录身份 |
| 机构内身份 | 成员 | 某账号在某机构内的成员身份，对应 `tenant_member` |
| 机构内部结构 | 组织/岗位 | 机构内部部门、岗位，不等同于机构 |

## 改造原则

- 用户可见文本统一叫“机构”，不再显示“租户”。
- 接口路径本阶段保留 `/system/tenant`，避免前后端联调路径反复震荡；Swagger 描述改成“机构管理”。
- 数据库表、实体类、上下文字段本阶段保留 `tenant` 命名，只作为技术实现。
- 必要、将来一定要用的能力不留尾巴：机构成员、机构授权、机构初始化、机构禁用/删除边界必须逐步补齐。
- 每完成一个任务必须运行对应 E2E；失败时先修复再进入下一个任务。

## 任务拆分

| 任务 | 状态 | 范围 | 验证 |
|---|---|---|---|
| T1 机构命名收敛 | 已完成 | 前端页面、登录页、菜单、Swagger、E2E 文案从“租户”改为“机构” | 已通过：机构管理 E2E、平台元数据隔离 E2E、系统管理页面 E2E、页面可见文本检查 |
| T1.5 非按钮菜单图标补齐 | 已完成 | `menu_type != 3` 且顶部/左侧可见的菜单必须配置图标 | 已通过：菜单接口图标检查、顶部/左侧菜单图标渲染 E2E |
| T2 登录机构选择优化 | 已完成 | 保留公开机构选项，同时补账号可进入机构能力；登录失败提示明确为“账号未加入该机构” | 已通过：登录页 E2E、错误分支 API E2E、当前用户信息 E2E |
| T3 机构初始化与状态边界 | 已完成 | 创建机构初始化根组织、岗位、管理员成员、角色授权；禁用机构后不可登录；删除需依赖检查 | 已通过：机构 provisioning E2E、机构管理 E2E |
| T4 成员管理最终收敛 | 已完成 | 用户管理语义收敛为机构成员；角色绑定主体固定为 `TENANT_MEMBER` | 已通过：成员管理 E2E、角色授权 E2E |
| T5 机构类型与能力模型 | 已完成 | 增加机构类型、能力/套餐开通字段；用于保函业务参与方建模 | 已通过：机构管理 E2E 覆盖 API 持久化、列表返回、页面字典中文标签展示 |
| T6 默认机构 fallback 风险治理 | 已完成 | Web 请求缺少机构上下文时禁止隐式写入平台机构；任务/平台元数据显式声明范围 | 已通过：持久化租户拦截器单元测试、登录机构选择 E2E、机构管理 E2E |
| T7 跨机构业务协同设计落地 | 待处理 | 建业务参与方、授权共享、平台代维审计模型 | 独立业务模块 E2E |

## 当前执行顺序

1. T1 机构命名收敛。已完成。
2. T1.5 非按钮菜单图标补齐。已完成。
3. T3 机构初始化与状态边界。已完成。
4. T4 成员管理最终收敛。已完成。
5. T2 登录机构选择优化。已完成。
6. T5 机构类型与能力模型。已完成。
7. T6 默认机构 fallback 风险治理。已完成。
8. T7 按业务模块接入前的风险排序继续执行。

## 菜单图标规范

- `menu_type = 3` 的按钮权限不需要图标。
- `menu_type = 1/2` 且 `visible = 1` 的目录、菜单必须配置图标。
- 顶部菜单和左侧菜单都必须显示稳定图标；新增迁移时一并修复历史缺失图标。
- 图标名称优先使用 Element Plus Icons 已存在的图标，避免自定义 SVG。
