# 标准交付记录

任务：Mango CLI 本地启动兼容修复

## 1. 元数据

- 任务 ID：CLI-DEV-START-20260902
- 交付模式：STANDARD
- 需求影响：L2 - 共享 CLI 的本地后端启动会被旧 workspace 参数和超大历史日志阻断。
- 方案风险：L2 - 修改共享 CLI 参数归一化和生命周期日志诊断，但不改变应用、数据库或发布协议。
- 最终风险：L2
- 工作区决策：REUSE - 复用现有 `fix/cli-dev-start-legacy-runtime-log` 任务 worktree

## 2. 目标与范围

- 目标：让旧 workspace 配置可继续启动，并确保生命周期诊断不会完整读取超大日志。
- 成功条件：匹配的旧 `runtime` 前缀只移除一次；非法模式仍失败；生命周期 marker 与日志尾部可从多 GB 稀疏文件读取。
- 处理范围：`@mango/cli` Spring Boot 参数解析、生命周期日志读取、CLI 测试与能力说明。
- 不处理范围：不修改数据库结构、应用 API、前端页面、生产部署或发布版本。

## 3. 可观察系统要求

| ID    | 参与者或入口                   | 输入或前置条件                                               | 预期行为                                                     | 失败语义                              | 验收标准                               |
| ----- | ------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------- | -------------------------------------- |
| SR-01 | `mango dev plan/start backend` | 旧额外参数为 `runtime --flag=value` 且 `processMode=runtime` | 生成一次 `runtime` 并保留 flag                               | 独立、冲突或重复模式继续报错          | 兼容正例和拒绝反例均有自动化覆盖       |
| SR-02 | 生命周期 verify/apply          | 日志已有多 GB 历史内容                                       | 从命令执行前的字节 offset 读取有界尾部并识别 mismatch marker | marker 不在有界尾部时按原失败路径停止 | 稀疏文件测试证明 offset 与 marker 识别 |
| SR-03 | `mango dev logs` 与失败尾部    | app 日志很大                                                 | 最多读取末尾 1 MiB 并输出最后 N 行                           | 文件不存在返回空内容                  | 单元测试覆盖缺失文件和有界尾部         |

## 4. 技术决定

| ID    | 对应要求     | 接口/数据/权限/兼容性决定                                                                          | 影响路径                           | 回滚方式                         |
| ----- | ------------ | -------------------------------------------------------------------------------------------------- | ---------------------------------- | -------------------------------- |
| TD-01 | SR-01        | 仅剥离一次、位于单个多 token 参数开头且与 `processMode` 相同的模式；归一化后再次扫描所有模式 token | `mango-cli/src/index.mjs`          | 回退参数归一化提交               |
| TD-02 | SR-02, SR-03 | 使用文件字节大小作为 offset，最多读取 1 MiB 后缀；不缓存完整文件                                   | `mango-cli/src/dev-log-reader.mjs` | 回退有界读取模块并恢复旧读取逻辑 |

## 5. 实施清单

| ID    | 对应决定     | 顺序 | 改动路径                                             | 完成条件                             |
| ----- | ------------ | ---- | ---------------------------------------------------- | ------------------------------------ |
| IM-01 | TD-02        | 1    | `mango-ui/packages/mango-cli/src/dev-log-reader.mjs` | 缺失文件、稀疏大文件、尾部行测试通过 |
| IM-02 | TD-01, TD-02 | 2    | `mango-ui/packages/mango-cli/src/index.mjs`          | CLI 参数与日志调用全部使用新边界     |
| IM-03 | TD-01        | 3    | `mango-ui/packages/mango-cli/scripts/check-cli.mjs`  | 旧参数兼容与现有拒绝用例同时通过     |
| IM-04 | TD-01, TD-02 | 4    | CLI README 与能力地图                                | 使用方式、兼容边界和日志上限可查     |

## 6. 验收映射与结果

| 要求 ID             | 验证方式              | 命令或步骤                                                                                                                | 结果 | 证据                                                                                                 |
| ------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------- | ---- | ---------------------------------------------------------------------------------------------------- |
| SR-01               | M10 单元/CLI 合同测试 | Node 22.23.1：`pnpm --filter @mango/cli test`                                                                             | PASS | CLI 合同通过；Node 测试 80 passed、2 个 Windows 平台用例按平台 skip                                  |
| SR-01               | M11 真实 CLI 计划     | 任务 worktree 配置 `runtime --mango.resource.registry.demo-enabled=true` 后执行 `pnpm -C mango-ui mango dev plan backend` | PASS | 计划仅含一个 `runtime`，保留 demo flag；端口 `18009`、数据库 `mango_dev_mango_cli_dev_start_fix_009` |
| SR-02, SR-03        | M10 单元测试          | `node --test packages/mango-cli/tests/dev-log-reader.test.mjs`                                                            | PASS | 3/3 通过，包含 4 GiB 稀疏前缀 marker 和 64-byte 有界 tail                                            |
| SR-01, SR-02, SR-03 | M09 静态与 PMO 检查   | `git diff --check`、Prettier、admin 两项样式检查、README 两项审计、PMO bundle build/check                                 | PASS | 所有命令退出码 0，未产生版本化生成漂移                                                               |

## 7. 例外与剩余风险

- 生命周期 marker 必须出现在命令新增日志的最后 1 MiB；超过该范围的单次失败输出仍按普通启动失败处理并保留完整日志路径。
- 本地 Node 测试按现有契约跳过 2 个仅 Windows 可执行的进程控制用例；跨平台参数和日志读取逻辑仍由平台无关单元与 CLI 合同测试覆盖。
