# Mango 外部发布物消费阻断修复台账

| ID | 来源 | 要求 | 设计决策 | 交付物 | 验收方式 | 状态 | 证据文件 |
|---|---|---|---|---|---|---|---|
| EXT-001 | 用户问题 1 | `@mango/admin-pages` 根入口不能引用 monorepo 内部 `apps/*` 路径 | 移除默认注册中的 `apps/mango-admin` 与 `apps/mango-admin-shell` 动态导入，改为包导出和可注入 shell page loader；业务示例页面通过 `@mango/workflow-business-example` 包根稳定导出注册 | `mango-ui/packages/admin-pages/src/defaults.ts`、`mango-ui/packages/admin-pages/src/core.ts`、`mango-ui/apps/mango-admin/src/config/componentsMap.ts`、`mango-ui/packages/workflow-business-example/src/index.ts` | 扫描 `admin-pages` 不再包含 `apps/mango-admin` 相对导入；前端构建通过；运行态访问 `/workflow/business-form` 不进入 404 | DONE | `rg -n "@mango/.*/src|apps/mango-admin|\\.\\./\\.\\./\\.\\./apps" mango-ui/packages/admin-pages mango-ui/packages/workflow-business-example mango-ui/apps/mango-admin-shell/src -g '!**/dist/**'`、`pnpm exec vite build`、浏览器冒烟 `workflow business example page PASS` |
| EXT-002 | 用户问题 2 | `@mango/admin-pages` 应正式导出稳定子路径 | 在 package exports 中提供 `core`、`defaults`、`dev-component-pages` 子路径，并将 shell 改为使用稳定子路径 | `mango-ui/packages/admin-pages/package.json`、`mango-ui/apps/mango-admin-shell/src/runtime/runtimeHost.ts` | 外部源码路径扫描不再需要 `@mango/admin-pages/src/core`；shell 构建通过 | DONE | `pnpm -F mango-admin-shell build` |
| EXT-003 | 用户问题 4 | 已发布 npm 包不要保留 `workspace:*` | 将 `@mango/*` workspace 依赖替换为版本约束，并整理 Vue、Element Plus、Pinia、Vue Router peer dependencies | `mango-ui/packages/*/package.json`、`mango-ui/pnpm-lock.yaml` | 扫描 `mango-ui/packages/*/package.json` 无 `workspace:*` | DONE | `rg -n "workspace:\\*" mango-ui/packages -g 'package.json'`、`pnpm install --lockfile-only --offline` |
| EXT-004 | 用户问题 6 | `mango-file-preview-starter` 不应通过 starter 配置覆盖宿主 `server.port` | 将 file-preview engine 配置中的 `server.port` 迁移为 `mango.file-preview.engine.port`，独立 `ServerMain` 启动时再映射到 Spring Boot `server.port` | `mango-file-preview-engine/src/main/config/application.properties`、`test.properties`、`ServerMain.java`、`PdfViewerCompatibilityTests`、`ServerMainTests` | 后端测试通过；扫描 file-preview 非 target 资源不再声明 `server.port` | DONE | `mvn -pl mango-platform/mango-file-preview/mango-file-preview-engine test -Dtest=PdfViewerCompatibilityTests,ServerMainTests`、`rg -n "^\\s*server\\.port\\s*=" mango/mango-platform/mango-file-preview -g '!**/target/**' -g '!**/LibreOfficePortable/**' -g '!**/static/**'` |
| EXT-005 | 用户问题 3 | Mango 前端需要真正可复用的管理后台壳包 | 记录为后续 Sprint，不在本次阻断修复中实现 | 本计划 | 后续项在计划中明确 | EXCEPTION | 用户要求优先解决发布物阻断；该项是新增壳包产品化能力，需要单独设计和交付 |
| EXT-006 | 用户问题 5 | 需要 Mango Initializr | 记录为后续 Sprint，不在本次阻断修复中实现 | 本计划 | 后续项在计划中明确 | EXCEPTION | 用户要求优先解决发布物阻断；该项是新增服务，需要单独设计和交付 |
| EXT-007 | 用户问题 7 | 需要可选初始化数据能力 | 记录为后续 Sprint，不在本次阻断修复中实现 | 本计划 | 后续项在计划中明确 | EXCEPTION | 用户要求优先解决发布物阻断；该项涉及种子数据和权限安全，需要单独设计和交付 |
| EXT-008 | 用户问题 8 | 需要业务模块菜单/权限/资源同步扩展规范 | 记录为后续 Sprint，不在本次阻断修复中实现 | 本计划 | 后续项在计划中明确 | EXCEPTION | 用户要求优先解决发布物阻断；该项涉及规范和脚手架生成器，需要单独设计和交付 |
