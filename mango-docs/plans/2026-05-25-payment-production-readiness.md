# 统一支付平台投产就绪计划

## 1. 目标

为统一支付平台建立投产前硬门禁，确保交付内容不是演示版、压缩版或不可用主链路，而是具备真实运行、可审计、可回滚、可观测、可验证的生产级能力。

## 2. 投产原则

1. 未经用户确认，禁止缩小设计范围。
2. 未完成交付台账，禁止声明完成。
3. 未通过 E2E，禁止提交 PR。
4. 未完成权限、租户隔离、幂等、状态机、审计、安全检查，禁止投产。
5. 通道配置、证书、密钥不得进入仓库，不得后台明文展示。
6. 芒果支付内置虚拟通道是正式通道类型，不与外部支付机构通信，不得使用未交付占位实现或演示替代。

## 3. 投产清单

| ID | 类别 | 检查项 | 通过标准 | 证据 |
|---|---|---|---|---|
| PRD-001 | 台账 | 交付台账完整 | 每个菜单、页面、API、数据、权限、E2E 均有原子验收项 | `2026-05-25-payment-delivery-ledger.md` |
| PRD-002 | 台账 | 台账 verify 通过 | `DONE` 或有用户确认的 `EXCEPTION`，未完成项为 0 | `delivery-contract-check` 输出 |
| ARCH-001 | 架构 | 模块边界 | 支付模块不依赖其他模块内部实现，只依赖公开 API | 依赖检查记录 |
| ARCH-002 | 架构 | 通道适配插件化 | 新增通道不修改订单核心状态机 | 代码审查记录 |
| DATA-001 | 数据 | 数据迁移 | 支付核心表、唯一约束、索引、审计字段迁移可重复执行 | 迁移执行记录 |
| DATA-002 | 数据 | 唯一成功支付 | 同一业务订单并发回调只允许一笔有效成功支付 | 并发测试记录 |
| DATA-003 | 数据 | 退款金额控制 | 多次退款累计不超过可退金额 | 并发测试记录 |
| DATA-004 | 数据 | 金额单位 | 金额统一以分为单位，禁止元分散落换算 | 单元测试记录 |
| TENANT-001 | 多租户 | 租户数据隔离 | 不同租户不可交叉查询配置、订单、流水、报表 | 集成测试、E2E |
| TENANT-002 | 多租户 | 平台管理员例外审计 | 跨租户操作必须记录审计 | 审计测试记录 |
| MENU-001 | 菜单 | 支付中心菜单入库 | 菜单由后端数据登记，按应用接入、支付通道、交易订单、对账结算分组，前端不长期维护静态支付菜单 | 数据库检查、E2E |
| MENU-002 | 菜单 | 无租户收银台菜单 | 后台不存在独立“租户收银台”菜单 | E2E 断言 |
| MENU-003 | 菜单 | 支付结果页非菜单 | 支付结果页只作为收银台流程页面 | E2E 断言 |
| PAGE-001 | 后台 | 应用管理 | 列表、详情、编辑、启停、通信安全配置、通知地址白名单、返回域名白名单、审计可用；管理后台不提供重置密钥按钮 | E2E |
| PAGE-002 | 后台 | 收银台配置 | 列表、详情、编辑、启停、所属应用、默认收银台、基础展示配置、允许企业主体、允许支付方式、默认方式、展示顺序、结果跳转、退款规则、行操作“收银台”按配置渲染页面可用 | E2E |
| PAGE-003 | 后台 | 企业主体管理 | 列表、详情、编辑、银行账户、脱敏、签约配置归属、审计可用 | E2E |
| PAGE-004 | 后台 | 通道签约配置 | 按支付通道签约字段模板渲染表单，企业主体、支付通道、环境、商户号、AppId、证书、密钥引用、签约能力开通、启停、审计可用；多行、密钥、文件、枚举、JSON 等控件正确保存 | E2E |
| PAGE-005 | 后台 | 支付通道管理 | 通道产品定义、通道类型、通道能力、适配器类型、基础网关、签约字段模板、启停、审计可用，不保存主体专属商户资料 | E2E |
| PAGE-005A | 后台 | 支付方式管理 | 三级标准支付方式、PC/Web 交互形态、支付物料类型、展示属性、限额、可见范围和路由策略可用；不得直接绑定通道或通道签约配置 | E2E |
| PAGE-005B | 后台 | 管理后台示例应用 | 示例应用可跑通应用接入、支付通道、收银台配置、芒果支付内置虚拟通道付款、通知、订单、流水、审计和对账 | E2E |
| PAGE-006 | 后台 | 订单与流水 | 业务订单、支付订单、退款订单、交易流水查询和详情可用 | E2E |
| PAGE-007 | 后台 | 异常与通知 | 异常订单、通知记录、通知重推、审计可用 | E2E |
| PAGE-008 | 后台 | 对账结算 | 对账管理、差异处理、结算汇总查询和受控操作可用 | E2E |
| CASHIER-001 | 收银台 | Web/H5 组件复用 | `PaymentCashier` 可被后台行操作、业务 Web/H5 支付页、芒果支付内置虚拟通道流程复用 | 组件测试、E2E |
| CASHIER-002 | 收银台 | Web/H5 支付形态 | 支持二维码、网银/银联跳转、线下转账、H5 跳转；App 和小程序页面不纳入本期 | 组件测试、E2E |
| CASHIER-003 | 收银台 | 金额防篡改 | 收银台只展示后端订单金额，不接收前端金额作为支付金额 | 安全测试 |
| CASHIER-004 | 收银台 | 聚合支付方式展示 | 多个签约能力支持同一个标准支付方式时，收银台只展示一个聚合支付方式，实际通道由后端路由决定 | E2E |
| API-001 | 开放接口 | 签名认证 | AppId、tenantId、timestamp、nonce、signature 校验生效 | API 安全测试 |
| API-002 | 开放接口 | 创建订单幂等 | tenantId + AppId + bizOrderNo 重复请求返回原订单 | API 测试 |
| API-003 | 开放接口 | 发起支付 | 支持切换支付方式，多笔支付订单只允许一笔成功 | API 测试、并发测试 |
| API-003A | 开放接口 | 支付方式路由 | 发起支付按租户、应用、企业主体、金额、终端、环境和标准支付方式命中签约能力，并记录路由规则和签约能力 | API 测试、路由测试 |
| API-004 | 开放接口 | 退款接口 | 幂等、可退金额、状态机校验生效 | API 测试 |
| API-005 | 开放接口 | 通知 ACK | 通知失败按策略重试，成功 ACK 后停止 | 集成测试 |
| CHANNEL-001 | 通道 | 芒果支付内置虚拟通道 | 支持全部标准支付方式，并具备支付下单、支付回调、主动查单、关单、退款、退款查询、账单生成、对账、返回码映射和异常场景控制能力 | 芒果支付 E2E、退款 E2E、查单/关单测试、账单核对测试、异常场景测试 |
| CHANNEL-002 | 通道 | 通联支付通道 | 通道产品定义、通道签约配置、支付、回调验签、查单、退款、账单解析按官方文档实现 | 联调记录 |
| CHANNEL-003 | 通道 | 华夏银行通道 | 通道产品定义、通道签约配置、支付、回调验签、查单、退款、账单解析按官方或内部文档实现 | 联调记录 |
| CHANNEL-004 | 通道 | 其他已知通道 | 清单经用户确认后逐项入台账，不得自行删减 | 用户确认记录 |
| CHANNEL-005 | 通道 SDK | 微信/支付宝 SDK 依赖 | SDK 仅用于通道适配层，核心域不直接依赖 SDK 模型 | 依赖检查、代码审查 |
| ROUTE-001 | 路由 | 跨通道路由策略 | 支持优先级、手工指定、失败降级、权重、成本和健康状态字段；未启用权重、成本、健康策略时按优先级执行 | 单元测试、E2E |
| ROUTE-002 | 路由 | 芒果支付内置虚拟通道隔离 | 内部验证场景只命中芒果支付内置虚拟通道签约能力，不得误走真实外部通道签约能力 | E2E |
| ROUTE-003 | 路由 | 禁用和超限过滤 | 标准支付方式、通道、签约配置、签约能力禁用或金额超限时不得命中 | 单元测试、E2E |
| SECURITY-001 | 安全 | 敏感字段 | appSecret、证书、私钥、银行账号、证件号加密存储和脱敏展示 | 安全测试 |
| SECURITY-001A | 安全 | 通道签约字段安全 | 签约字段模板可标记敏感、加密、脱敏；通道签约配置只保存密文、脱敏摘要或文件 ID，不保存文件访问地址 | 安全测试 |
| SECURITY-002 | 安全 | 日志脱敏 | 日志不得打印完整密钥、证书、银行卡号、身份证号 | 日志扫描 |
| SECURITY-003 | 安全 | 回调验签 | 通道回调必须验签、校验金额、商户号、订单号 | 集成测试 |
| OPS-001 | 运维 | 可观测性 | 指标、日志、链路、告警覆盖支付、退款、回调、通知、对账 | 运维检查 |
| OPS-002 | 运维 | 任务触发 | 订单过期、查单、通知重试、对账、结算由人工或平台统一调度触发 | 集成测试 |
| AUDIT-001 | 审计 | 人工操作审计 | 资金相关人工操作、配置变更、审批记录完整 | 审计测试 |
| TEST-001 | 测试 | 单元测试 | 状态机、金额、签名、通道路由、返回码映射通过 | 测试输出 |
| TEST-002 | 测试 | 集成测试 | API、数据库约束、并发、通知、对账、结算通过 | 测试输出 |
| TEST-003 | 测试 | E2E | 支付中心菜单、配置、收银台、芒果支付内置虚拟通道付款、结果页、后台查询通过 | Playwright 输出 |
| TEST-004 | 测试 | 禁止未交付占位标记扫描 | 不存在未完成占位、临时替代和伪实现等交付风险标记 | 扫描输出 |

## 4. 必跑验证命令

```bash
node mango-pmo/tools/delivery-contract-check.mjs \
  --design mango-docs/designs/统一支付系统设计说明书.md \
  --ledger mango-docs/plans/2026-05-25-payment-delivery-ledger.md \
  --mode plan
```

交付前必须执行 verify 模式：

```bash
node mango-pmo/tools/delivery-contract-check.mjs \
  --design mango-docs/designs/统一支付系统设计说明书.md \
  --ledger mango-docs/plans/2026-05-25-payment-delivery-ledger.md \
  --mode verify \
  --scan <支付相关改动路径>
```

研发完成后还必须执行实际工程命令：

```bash
pnpm lint
pnpm build
pnpm test
pnpm playwright test
```

后端测试命令以仓库支付模块实际构建工具为准，执行结果必须记录到 PR 描述和交付报告。

## 5. 阻塞登记规则

遇到以下情况必须停止对应交付项并登记，不得自行降级：

1. 华夏银行通道缺少官方或内部对接文档。
2. 外部通道缺少联调环境、商户参数、证书或回调验签规则。
3. 用户未确认其他已知通道清单。
4. 线下收款已作为独立通道完成菜单、数据模型、确认到账、银行流水导入匹配、退款处理和 E2E；该结论不替代外部真实通道和通道账单多获取源适配。
5. 租户定义影响权限或数据隔离模型。
6. 业务系统开票触发要求影响通知协议。

登记格式：

| 日期 | 阻塞项 | 影响台账 ID | 需要用户确认的问题 | 当前不能做的事 |
|---|---|---|---|---|
| 2026-06-07 | 通联支付真实联调资料缺失 | `PAY-CHANNEL-003` | 请提供通联商户号、应用 ID、联调网关、机构公钥、商户私钥/证书文件、回调验签规则、账单样例和通联侧联调账号 | 不能声明通联支付、退款、查单、回调验签、账单解析或对账完成 |
| 2026-06-07 | 华夏银行通道对接资料缺失 | `PAY-CHANNEL-005` | 请提供华夏银行官方或内部对接文档、联调环境、机构号、商户参数、证书、公钥私钥、回调验签规则和账单格式 | 不能实现或声明华夏银行支付、退款、查单、回调验签、账单解析完成，不能用其他通道替代 |
| 2026-06-07 | 其他已知通道清单和优先级未确认 | `PAY-CHANNEL-006`、`PAY-CONFIRM-002` | 请确认微信、支付宝、连连、银联商务天满等候选通道哪些纳入本期、哪些后续处理，以及接入优先级 | 不能自行删除候选通道、降级为芒果支付，或声明其他通道清单完成 |
| 2026-06-07 | 微信支付真实联调资料缺失 | `PAY-CHANNEL-007` | 请提供微信商户号、AppId、API v3 Key、商户证书序列号、私钥文件、平台证书、公网回调地址和联调账单 | 不能声明微信直连支付、退款、查单、回调验签或账单解析完成 |
| 2026-06-07 | 支付宝真实联调资料缺失 | `PAY-CHANNEL-008` | 请提供支付宝 AppId、应用私钥、支付宝公钥、网关环境、回调地址和联调账单 | 不能声明支付宝直连支付、退款、查单、回调验签或账单解析完成 |
| 2026-06-10 | 通道账单多获取源适配未完成 | `PAY-RECON-004` | 对账单获取方式需支持手动上传、FTP/FTPS 拉取、HTTP 时间区间分页或游标拉取；当前手动导入和线下银行流水 Excel 已完成，但 FTP/FTPS/HTTP 通道账单获取源尚未实现和联调 | 不能声明通道账单多获取源完成，不能用手动导入或线下银行流水导入替代外部通道自动拉取 |
| 2026-06-07 | 支付成功后开票触发要求未确认 | `PAY-CONFIRM-003` | 请确认支付成功后是否需要触发开票、由谁触发、是否需要扩展支付通知协议字段 | 不能把开票业务耦合进支付模块，不能声明开票触发协议完成 |
| 2026-06-07 | 租户业务定义未确认 | `PAY-CONFIRM-004` | 请确认租户代表内部事业部、外部客户，还是两者都支持；是否影响默认角色、菜单授权和数据隔离 | 不能把权限、菜单授权和数据隔离模型调整为单一假设 |
| 2026-06-07 | 交付台账仍有未完成项 | `PAY-TEST-004` | 请先关闭外部通道和用户确认项，或明确哪些项作为本期用户确认 `EXCEPTION` | 不能通过最终 `delivery-contract-check --mode verify`，不能声明支付模块整体投产完成 |

当前阻塞详情见：`mango-docs/plans/evidence/payment-production-blockers-acceptance.md`。

## 6. PR 合并门禁

1. 交付台账必须更新。
2. 投产清单必须更新证据。
3. 所有测试命令必须有执行记录。
4. 未完成项必须为 0，或全部为用户确认的 `EXCEPTION`。
5. PR 描述必须列出台账总项、完成项、例外项、未完成项。
6. 禁止在 PR 中出现未完成占位、临时替代和伪实现等未交付标记。
