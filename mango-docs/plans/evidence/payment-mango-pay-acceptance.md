# 芒果支付内置虚拟通道验收证据

## 1. 验收范围

- 后端服务：`PaymentChannelOrderQueryService`
- 后端服务：`PaymentChannelOrderCloseService`
- 后端服务：`PaymentChannelRefundQueryService`
- 后端服务：`PaymentReconciliationService.generateMangoPayVirtualBill`
- 后端服务：`PaymentMangoPayResultMappingService`
- 后端服务：`PaymentMangoPayScenarioControlService`
- 后端服务：`IPaymentChannelAdapter`、`PaymentChannelAdapterRegistry`、`PaymentMangoPayChannelAdapter`
- 后端服务：`PaymentNotificationService`
- 开放接口服务：`PaymentOpenApiService.refund`
- 受控入口：异常订单处理动作 `ACTIVE_QUERY`
- 受控入口：异常订单处理动作 `CLOSE_PAYMENT_ORDER`
- 数据：`payment_order`、`payment_refund_order`、`payment_business_order`、`payment_transaction_flow`、`payment_channel_contract`、`payment_channel_query_record`、`payment_refund_query_record`、`payment_reconciliation`、`payment_channel_bill_detail`、`payment_difference`、`payment_exception_order`、`payment_mango_pay_scenario_control`、`payment_notification_record`、`payment_operation_audit`
- 本轮覆盖芒果支付主动查单推进支付中订单、主动查单记录持久化、关单核心能力、异常订单页面触发真实主动查单和关闭支付订单、发起退款按通道场景入状态、主动查退款推进退款中订单、退款查询记录持久化、芒果支付按日期生成真实支付/退款账单、对账管理页面生成芒果支付账单真实 E2E、退款账单核对、芒果支付通道返回码映射，以及支付/查单/退款/退款查询/账单差异/回调延迟的下一笔异常场景控制和到期通知自动调度；支付中心全量浏览器 E2E 已单独形成 `payment-full-e2e-acceptance.md` 证据；不覆盖外部通道联调，长时间运行调度验收归属 `PAY-SCHED-001`。

## 2. 执行环境

- 本地工作区：`/Users/hardy/Work/mango/.claude/worktrees/payment-platform`
- 数据库或租户：`mango_dev_e397cd`，租户 `1`
- 后端测试：Maven Surefire
- 说明：本轮按当前主数据和新增迁移统一使用 MANGO_PAY 芒果支付。

## 3. 功能验收记录

| 台账 ID | 页面/接口 | 功能点 | 测试数据 | 关键断言 | UI/交互检查 | console/network 结果 | 截图/trace/日志 | 结论 |
|---|---|---|---|---|---|---|---|---|
| PAY-CHANNEL-001 / PAY-CHANNEL-002 | `IPaymentChannelAdapter`、`PaymentChannelAdapterRegistry`、`PaymentMangoPayChannelAdapter`、`PaymentChannelBillItemRow` | 芒果支付付款/退款发起、查单/查退款和账单生成接入统一通道适配 SPI | `channelCode=MANGO_PAY`，支付订单 `PO202606060001`，退款订单 `RO202606060001`，签约配置 `mangoPayScenario=SUCCESS`、`mangoPayRefundScenario=PROCESSING`，账单日期 `2026-06-06` | 新增通道适配接口，定义支付发起、退款发起、支付查单、退款查单和通用通道账单生成命令/结果；账单结果使用通用 `PaymentChannelBillItemRow`，SPI 不暴露芒果支付专属账单行类型；收银台支付、OpenAPI 发起退款、主动查单、主动查退款和对账账单生成服务不再直接消费芒果支付场景配置，而是通过 `PaymentChannelAdapterRegistry` 按通道编码分发；MANGO_PAY 适配器消费下一笔场景控制后回退真实签约配置或真实订单账单行；未注册外部通道按 `PAYMENT_CHANNEL_INVALID` 拒绝，避免伪实现；通联、华夏、微信、支付宝、连连等真实适配继续归属 `PAY-CHANNEL-003/005/006/007/008`，不在本项内伪完成 | 后端 SPI 和服务单测，不涉及页面布局 | `PaymentChannelAdapterRegistryTest`、`PaymentMangoPayChannelAdapterTest`、支付/退款发起服务测试、查单/查退款服务测试、对账生成服务测试通过 | `PaymentChannelAdapterRegistryTest`、`PaymentMangoPayChannelAdapterTest`、`PaymentCashierServiceImplTest`、`PaymentOpenApiServiceTest`、`PaymentChannelOrderQueryServiceTest`、`PaymentChannelRefundQueryServiceTest`、`PaymentReconciliationServiceTest` | DONE |
| PAY-CHANNEL-002 | `PaymentCashierServiceImpl.pay`、`PaymentChannelOrderQueryService.queryMangoPayPayment` | 芒果支付发起支付和主动查单可通过统一适配器获得通道状态 | `payOrderNo=PO202606060001`，签约配置 `mangoPayScenario=SUCCESS`，下一笔 `PAYMENT=FAIL` 控制 | 收银台支付通过 `PaymentChannelAdapterRegistry` 调用 MANGO_PAY 适配器获得初始状态；主动查单通过同一注册表调用 MANGO_PAY 查单；使用 `PaymentOrderMapper.selectByTenantAndPayOrderNo` 读取真实支付订单；使用 `updatePayingQueryResult` 按 `tenant_id/id/status='PAYING'` CAS 更新；生成 `PAY_SUCCESS` 交易流水；调用 `markCashierPaySuccess` 推进业务订单；写入 `payment_channel_query_record` 查单请求、响应、处理结果 | 后端服务单测，不涉及页面布局 | 单测通过，无 mock 作为交付，只用 mock 隔离 mapper 断言服务行为 | `PaymentCashierServiceImplTest`、`PaymentChannelOrderQueryServiceTest` | DONE |
| PAY-CHANNEL-002 | `PaymentChannelOrderQueryService.queryMangoPayPayment` | 芒果支付主动查单处理中、失败和终态幂等分支 | `mangoPayScenario=PROCESSING`、`mangoPayScenario=FAIL`、终态 `SUCCESS` | `PROCESSING/PAYING` 订单状态不变但写查单记录；`FAIL/FAILED` 推进支付订单到 `FAILED` 且不生成成功流水；本地终态不再查询通道配置、不覆盖本地终态，并写入 `NO_QUERY_TERMINAL` 记录 | 后端服务单测，不涉及页面布局 | 后端服务单测通过，未涉及浏览器网络请求 | `PaymentChannelOrderQueryServiceTest` | DONE |
| PAY-CHANNEL-002 | `payment_channel_query_record` | 主动查单次数、最后结果、请求响应和处理结果持久化 | `queryType=ACTIVE_QUERY`，`processResult=UPDATED/NO_CHANGE_PROCESSING/NO_QUERY_TERMINAL` | 新增 Flyway `V43__payment_channel_query_record.sql`、`PaymentChannelQueryRecordEntity extends AuditableEntity`、`PaymentChannelQueryRecordMapper extends BaseMapper`；每次主动查单写请求摘要、响应摘要、查单前状态、通道状态、处理后状态、处理结果和审计字段；返回 `queryCount` 与 `lastQueryResult`，查单间隔可由同一订单相邻 `query_time` 计算 | 后端数据能力，不涉及页面布局 | Maven 编译和服务单测通过 | `PaymentChannelQueryRecordMapper.xml`、`PaymentChannelOrderQueryServiceTest` | DONE |
| PAY-DOMAIN-002 | `PaymentOrderMapper.xml` | 支付订单状态由主动查单推进 | `payment_order.status='PAYING'` | 新增 `selectByTenantAndPayOrderNo` 和 `updatePayingQueryResult`；更新条件包含租户和 `status='PAYING'`，避免并发重复推进 | 后端 mapper 契约，不涉及页面布局 | Maven 编译和服务单测通过 | `PaymentOrderMapper.xml`、`PaymentChannelOrderQueryServiceTest` | DONE |
| PAY-OPS-001 | `PaymentReadonlyResourceService.handleExceptionOrder`、异常订单页面 `/payment/exception-orders` | 异常订单 `ACTIVE_QUERY` 受控动作触发真实查单 | 后端单测 `relatedOrderNo=PO2026061000000001`；E2E 真实支付订单 `PO\\d{16}`、异常单 `EX-AQ-E2E-*` | 仅真实收银台支付订单号格式 `PO\\d{16}` 触发查单；页面选择“主动查单”后支付订单由 `PAYING` 推进到 `SUCCESS`，业务订单由 `PAYING` 推进到 `SUCCESS`，生成 `PAY_SUCCESS` 交易流水，写入 `payment_channel_query_record`，异常单处理结果追加“查单结果”，仍记录 `HANDLE_EXCEPTION_ORDER` 审计；非真实支付订单号只保留原处理记录 | 异常订单页面已验证待处理筛选、处理弹窗、处理动作下拉、保存处理、已处理列表回显和处理按钮隐藏；截图 `test-results/payment-exception-orders-real-actions.png` | 后端服务单测通过；Playwright Chromium 定向 E2E 真实浏览器操作通过，console/pageerror/requestfailed 均无异常 | `PaymentReadonlyResourceServiceTest`、`payment-center.spec.ts` | DONE |
| PAY-CHANNEL-002 | `PaymentChannelOrderCloseService.closeMangoPayPayment` | 芒果支付关单可关闭未支付或支付中订单 | `payOrderNo=PO202606060001`，支付订单 `PAYING`，业务订单 `PAYING` | 使用 `PaymentOrderMapper.selectByTenantAndPayOrderNo` 读取真实支付订单；校验支付订单只能 `CREATED/PAYING` 关闭且 `success_flag != 1`；校验业务订单只能 `TO_PAY/PAYING` 关闭且 `paid_amount=0`；通过 `closeOpenPaymentOrder` 和 `closeOpenBusinessOrder` 进行租户 + 状态 CAS 更新；重复关单返回幂等结果；记录 `CLOSE_PAYMENT_ORDER` 操作审计 | 后端服务单测，不涉及页面布局 | 后端服务单测通过，未涉及浏览器网络请求 | `PaymentChannelOrderCloseServiceTest` | DONE |
| PAY-OPS-001 | `PaymentReadonlyResourceService.handleExceptionOrder`、异常订单页面 `/payment/exception-orders` | 异常订单 `CLOSE_PAYMENT_ORDER` 受控动作触发真实关单 | 后端单测 `relatedOrderNo=PO2026061000000002`；E2E 真实支付订单 `PO\\d{16}`、异常单 `EX-CL-E2E-*` | 仅真实收银台支付订单号格式 `PO\\d{16}` 触发关单；页面选择“关闭支付订单”后支付订单由 `PAYING` 推进到 `CLOSED`，业务订单由 `PAYING` 推进到 `CLOSED` 且 `paidAmount=0`，处理结果追加“关单结果”；仍记录 `HANDLE_EXCEPTION_ORDER` 审计；关单服务自身记录 `CLOSE_PAYMENT_ORDER` 审计 | 异常订单页面已验证待处理筛选、处理弹窗、处理动作下拉、保存处理、已处理列表回显和处理按钮隐藏；截图 `test-results/payment-exception-orders-real-actions.png` | 后端服务单测通过；Playwright Chromium 定向 E2E 真实浏览器操作通过，console/pageerror/requestfailed 均无异常 | `PaymentReadonlyResourceServiceTest`、`PaymentChannelOrderCloseServiceTest`、`payment-center.spec.ts` | DONE |
| PAY-CHANNEL-002 / PAY-API-007 / PAY-DOMAIN-003 | `PaymentOpenApiService.refund` | 芒果支付发起退款按签约场景进入真实初始状态 | `mangoPayRefundScenario=SUCCESS/PROCESSING/FAILED`，`bizRefundNo=RF_OPENAPI_001` | 开放接口认证后按 `tenant_id + app_id + biz_refund_no` 幂等；使用 `PaymentOrderMapper.lockSuccessfulOpenPaymentOrder` 锁定成功支付订单；`sumOccupyingRefundAmount` 将 `CREATED/REFUNDING/PROCESSING/SUCCESS` 退款金额计入占用防止超退；`SUCCESS` 才更新业务订单退款进度、写 `REFUND_SUCCESS` 流水并触发退款成功通知；`REFUNDING/FAILED` 不写成功流水、不推进已退款金额、不发成功通知 | 后端服务单测，不涉及页面布局 | `PaymentOpenApiServiceTest` 覆盖成功、处理中、失败、幂等冲突和超退 | `PaymentOpenApiServiceTest` | DONE |
| PAY-CHANNEL-002 / PAY-DOMAIN-003 | `PaymentOpenApiService.refund`、`PaymentChannelRefundQueryService.queryMangoPayRefund`、退款订单页面 `/payment/refund-orders` | 芒果支付退款发起和主动查退款可通过统一适配器获得通道状态 | 后端单测 `refundOrderNo=RO202606060001`；E2E 真实收银台支付单 `PO\\d{16}`、退款单 `RO-E2E-*`/`RO-NT-F-*`，签约配置 `mangoPayRefundScenario=SUCCESS/PROCESSING/FAILED`、`mangoPayRefundScenario=FAIL` | OpenAPI 发起退款通过 `PaymentChannelAdapterRegistry` 调用 MANGO_PAY 适配器获得初始状态；主动查退款通过同一注册表调用 MANGO_PAY 查退款；使用 `PaymentRefundOrderMapper.selectByTenantAndRefundOrderNo` 读取真实退款订单；仅 `REFUNDING/PROCESSING` 可由查退款推进；`SUCCESS` 分支按 `tenant_id/id/status in ('REFUNDING','PROCESSING')` CAS 更新退款订单、更新业务订单退款进度、生成 `REFUND_SUCCESS` 流水并触发退款成功通知；`FAILED` 推进退款订单状态为失败，不生成成功流水，并触发 `REFUND_FAILED` 业务通知；终态只记录查询依据不覆盖本地终态；E2E 验证页面点击“主动查退款”后退款订单 `REFUNDING -> SUCCESS`、业务订单变为 `PARTIAL_REFUNDED` 且 `refundedAmount=38800`、生成 `REFUND_SUCCESS` 流水，成功状态行隐藏“主动查退款”按钮；另有定向 E2E 验证 `REFUNDING -> FAILED` 后真实 HTTP 通知和通知记录回显 | 退款订单页面已验证状态筛选、详情抽屉、退款中行操作按钮、成功后按钮隐藏、空结果反馈；截图 `test-results/payment-refund-orders.png`、`test-results/payment-terminal-notifications.png` | `PaymentOpenApiServiceTest` 覆盖发起退款成功、处理中、失败；`PaymentChannelRefundQueryServiceTest` 覆盖成功、处理中、失败和终态幂等；Playwright Chromium 定向 E2E 真实浏览器操作通过，console/pageerror/requestfailed 均无异常 | `PaymentOpenApiServiceTest`、`PaymentChannelRefundQueryServiceTest`、`payment-center.spec.ts` | DONE |
| PAY-CHANNEL-002 / PAY-DOMAIN-003 | `PaymentChannelRefundQueryService.queryMangoPayRefund`、`PaymentChannelCallbackService.handle` | 芒果支付退款查询和标准化退款回调竞争保护 | 同一退款单 `RO202606060001`，两次 `REFUNDING -> SUCCESS` 竞争，第一次状态 CAS 成功，第二次状态 CAS 返回 0 | 主动查退款和标准化退款回调均依赖 `PaymentRefundOrderMapper.updateRefundingQueryResult` 的租户、订单 ID 和退款中状态条件推进；竞争失败分支返回 `PAYMENT_REFUND_ORDER_STATE_INVALID`，不会第二次更新业务退款进度，不会重复写 `REFUND_SUCCESS` 交易流水，不会重复触发退款通知；该证据只覆盖芒果支付内置虚拟通道和标准化入口，不代表外部机构公网回调验签已完成 | 后端服务单测，不涉及页面布局 | `PaymentChannelRefundQueryServiceTest`、`PaymentChannelCallbackServiceTest` 定向测试 16 个通过 | `mvn -f mango/pom.xml -pl mango-platform/mango-payment/mango-payment-core -am -Dtest=PaymentChannelRefundQueryServiceTest,PaymentChannelCallbackServiceTest -Dsurefire.failIfNoSpecifiedTests=false test -DskipTests=false` | DONE |
| PAY-CHANNEL-002 | `payment_refund_query_record` | 主动查退款次数、最后结果、请求响应和处理结果持久化 | `queryType=REFUND_QUERY`，`processResult=UPDATED/NO_CHANGE_PROCESSING/NO_QUERY_TERMINAL`；E2E 退款单 `RO-E2E-*` | 新增 Flyway `V44__payment_refund_query_record.sql`、`PaymentRefundQueryRecordEntity extends AuditableEntity`、`PaymentRefundQueryRecordMapper extends BaseMapper`；每次主动查退款写请求摘要、响应摘要、查询前状态、通道状态、处理后状态、处理结果和审计字段；返回 `queryCount` 与 `lastQueryResult`；E2E 通过真实 DB 断言最新记录 `beforeStatus=REFUNDING`、`channelStatus=SUCCESS`、`resultStatus=SUCCESS`、`processResult=UPDATED` | 退款订单页面操作链路触发真实 POST `/payment/refund-orders/query-channel` | Maven 编译、服务单测和 Playwright 定向 E2E 通过 | `PaymentRefundQueryRecordMapper.xml`、`PaymentChannelRefundQueryServiceTest`、`payment-center.spec.ts` | DONE |
| PAY-CHANNEL-002 / PAY-RECON-001 | `PaymentReconciliationService.generateMangoPayVirtualBill`、`PaymentMangoPayChannelAdapter.generateBill`、`POST /payment/reconciliations/mango-pay/virtual/generate` | 芒果支付可按日期经通道适配器生成通道账单批次 | `channelCode=MANGO_PAY`，`billDate=2026-06-06`，真实成功支付行 `CASHIER-PO001`，真实成功退款行 `CASHIER-REFUND-RO001` | 新增 `GenerateMangoPayVirtualBillCommand`；对账服务通过 `PaymentChannelAdapterRegistry.requireAdapter(channelCode).generateBill(...)` 调用 MANGO_PAY 适配器；MANGO_PAY 适配器通过 `PaymentReconciliationMapper.selectMangoPayBillItems` 从 `payment_order`、`payment_refund_order`、`payment_channel` 查询当天 `SUCCESS` 且通道为 `MANGO_PAY` 的真实支付/退款订单，并消费 `BILL` 场景控制；生成 `payment_reconciliation` 批次、`payment_channel_bill_detail` 明细；同一通道、日期、生成摘要重复时按 `PAYMENT_RECONCILIATION_FILE_DUPLICATED` 拒绝；记录 `GENERATE_MANGO_PAY_CHANNEL_BILL` 审计 | 后端服务和接口能力，不涉及页面布局 | `PaymentMangoPayChannelAdapterTest` 覆盖真实账单行返回和账单差异控制；`PaymentReconciliationServiceTest` 覆盖真实支付/退款行生成、重复摘要拒绝；starter controller 编译验证 | `PaymentMangoPayChannelAdapterTest`、`PaymentReconciliationServiceTest` | DONE |
| PAY-CHANNEL-002 / PAY-RECON-001 | 对账管理页面 `/payment/reconciliations`、`POST /payment/reconciliations/mango-pay/virtual/generate` | 页面可生成芒果支付账单并回显真实明细 | E2E 真实收银台支付订单 `PO\\d{16}`、通道交易号 `MANGO_PAY-PO\\d{16}`、`billDate=2026-06-06` | 页面点击“生成芒果支付账单”打开 Element Plus 弹窗；提交 `channelCode=MANGO_PAY` 和账单日期后调用真实接口；接口从真实成功支付行生成 `payment_reconciliation` 批次和 `payment_channel_bill_detail` 明细，返回 `MATCHED`；列表按批次号查到该批次，详情抽屉展示通道交易号和匹配支付订单；记录 `GENERATE_MANGO_PAY_CHANNEL_BILL` 审计 | 对账管理页面已验证搜索区、状态下拉、生成弹窗字段、提交成功消息、列表普通字段纯文本展示、状态标签、详情抽屉、无删除按钮；截图 `test-results/payment-reconciliations-special-bill.png` | Playwright Chromium 定向 E2E 真实浏览器操作通过，console/pageerror/reconciliation requestfailed 均无异常 | `payment-center.spec.ts` | DONE |
| PAY-CHANNEL-002 / PAY-RECON-002 | `PaymentReconciliationService.importReconciliation` | 退款账单纳入对账差异识别 | `tradeType=REFUND`，`channelTradeNo=CASHIER-REFUND-RO001`，通道金额 `4000`，本地退款金额 `3900` | `REFUND` 账单通过 `PaymentRefundOrderMapper.selectEntityByTenantAndChannelRefundNo` 匹配真实退款订单；校验本地退款状态必须为 `SUCCESS`，金额必须一致；本地无单生成 `CHANNEL_REFUND_LOCAL_MISSING`，状态不一致生成 `REFUND_STATUS_MISMATCH`，金额不一致生成 `REFUND_AMOUNT_MISMATCH` 差异单 | 后端服务单测，不涉及页面布局 | `PaymentReconciliationServiceTest` 覆盖退款金额不一致差异；支付账单原有金额核对保持 | `PaymentReconciliationServiceTest` | DONE |
| PAY-CHANNEL-002 / PAY-RECON-002 | `PaymentReconciliationService.importReconciliation`、生产 MyBatis mapper XML | 本地成功但芒果支付账单缺失差异识别 | 本地成功支付 `PO-MISSING`、本地成功退款 `RO-MISSING`，账单交易号集合未包含对应通道单号 | 对账导入时在处理账单明细后查询本地当天成功支付和成功退款；账单缺失的本地成功支付生成 `LOCAL_SUCCESS_CHANNEL_MISSING`，账单缺失的本地成功退款生成 `LOCAL_REFUND_CHANNEL_MISSING`；mapper 集成测试加载生产 XML，在 H2 MySQL 模式验证只返回同通道、同账单日、成功状态且未在账单集合中的记录；对账 SQL 日期上界由 Java 传入 `nextBillDate`，不依赖 `date_add` 方言函数 | 后端服务测试和 mapper 集成测试，不涉及页面布局 | Maven Surefire 通过，覆盖服务差异生成和生产 mapper XML 查询 | `PaymentReconciliationServiceTest`、`PaymentReconciliationMapperIntegrationTest` | DONE |
| PAY-CHANNEL-002 / PAY-CHANNEL-001 / PAY-TEST-001 | `PaymentMangoPayResultMappingService` | 芒果支付通道返回码映射为统一支付/退款状态 | `SUCCESS/OK`、`FAILED/FAIL`、`PARAM_ERROR/PARAMETER_ERROR`、`SIGN_ERROR/SIGNATURE_ERROR`、`CHANNEL_UNAVAILABLE`、`TIMEOUT`、未知码 | 收银台支付、主动查单、OpenAPI 发起退款、主动查退款均调用同一芒果支付映射服务；成功映射 `SUCCESS`，失败/参数错误/签名错误/通道不可用映射失败状态，超时和未知码映射处理中等待查单或查退款；查单/查退款记录写入 `returnCode`、`resultType` 和统一通道状态；使用 `mangoPayScenario` 字段 | 后端服务单测，不涉及页面布局 | `PaymentMangoPayResultMappingServiceTest` 覆盖支付/退款返回码映射，相关收银台/查单/退款测试通过 | `PaymentMangoPayResultMappingServiceTest` | DONE |
| PAY-CHANNEL-002 / PAY-TEST-001 | `POST /payment/mango-pay/virtual/scenario-controls`、`PaymentMangoPayScenarioControlService` | 芒果支付可控制下一笔支付、查单、退款和退款查询返回场景 | `channelCode=MANGO_PAY`，`scenarioType=PAYMENT/PAYMENT_QUERY/REFUND/REFUND_QUERY`，`scenarioCode=SUCCESS/FAIL/PROCESSING/TIMEOUT`，`effectiveCount=1` | 新增 `payment_mango_pay_scenario_control`；实体继承 `AuditableEntity`，mapper 继承 `BaseMapper`；创建控制记录时校验只允许 `MANGO_PAY`，可选 `contractId` 绑定签约配置；消费时优先取同签约控制，再取 MANGO_PAY 全局控制；消费成功后递增 `consumed_count`，达到 `effective_count` 后置为 `CONSUMED`；创建操作记录 `CREATE_MANGO_PAY_CHANNEL_SCENARIO` 审计；下单、主动查单、发起退款、主动查退款均优先消费下一笔控制，未配置时回退签约配置场景 | 后端服务和接口能力，不涉及页面布局 | `PaymentMangoPayScenarioControlServiceTest` 覆盖创建、拒绝非 MANGO_PAY、消费一次；`PaymentCashierServiceImplTest` 覆盖下一笔 FAIL 覆盖签约配置成功 | `PaymentMangoPayScenarioControlServiceTest`、`PaymentCashierServiceImplTest` | DONE |
| PAY-CHANNEL-002 / PAY-RECON-002 / PAY-TEST-001 | `PaymentMangoPayChannelAdapter.generateBill`、`PaymentReconciliationService.generateMangoPayVirtualBill` | 芒果支付可控制下一笔账单金额差异 | `scenarioType=BILL`，`billDifferenceType=AMOUNT_PLUS`，`differenceAmount=100`，`contractId=331001` | 生成账单仍从真实成功支付/退款订单读取行；MANGO_PAY 适配器消费 `BILL` 控制后仅调整下一笔账单首行金额，不创建假订单；对账服务复用原对账逻辑生成 `AMOUNT_MISMATCH` 或 `REFUND_AMOUNT_MISMATCH` 差异单；金额差异单位为分，使用 `long` | 后端服务单测，不涉及页面布局 | `PaymentMangoPayChannelAdapterTest` 覆盖账单金额 +100 分行调整；`PaymentReconciliationServiceTest` 覆盖账单金额 +100 分后产生 `AMOUNT_MISMATCH` 差异 | `PaymentMangoPayChannelAdapterTest`、`PaymentReconciliationServiceTest` | DONE |
| PAY-CHANNEL-002 / PAY-TEST-001 / PAY-API-010 | `PaymentNotificationService`、`PaymentNotificationDispatchScheduler`、`POST /payment/notification-records/deliver-due`、通知记录页面 | 芒果支付可控制下一笔成功通知回调延迟，并由支付模块调度器自动投递到期通知 | `scenarioType=CALLBACK_DELAY`，`callbackDelayMinutes=10`，`contractId=331001`；E2E 数据 `NT-AUTO-E2E-*`、`PO-AUTO-E2E-*`，目标地址为本地真实 HTTP ACK 接收服务 | 成功支付或退款通知仅在 `channelCode=MANGO_PAY` 时消费回调延迟场景；创建 `payment_notification_record` 真实记录，状态 `PENDING`，写入 `scheduled_notify_time`、`next_retry_time` 和 `payload_json`，创建当下不发起 HTTP POST；到期投递入口只查询当前租户、`next_retry_time <= now` 且有 `payload_json` 的记录，先 CAS 标记 `RETRYING` 再按真实 HTTP ACK 更新为 `SUCCESS/FAILED`；`mango-payment-starter` 自动扫描存在到期通知的租户，设置支付系统租户上下文后调用到期投递服务；调度开关、间隔、租户批量和投递批量由 `mango.payment.notification.dispatch.*` 配置控制；E2E 不调用手工 `deliver-due` 接口，等待调度器真实投递后断言 API 详情和页面列表均为通知成功 | 通知记录页面 `/payment/notification-records` 已验证搜索、状态筛选、表格回显、成功通知不显示重推按钮；截图 `test-results/payment-notification-auto-dispatch.png` | `PaymentNotificationServiceTest` 覆盖延迟创建不 HTTP 投递、到期后真实 HTTP POST 并按 ACK 更新成功；`PaymentMangoPayScenarioControlServiceTest` 覆盖回调延迟参数校验；`PaymentNotificationDispatchSchedulerTest` 覆盖按租户上下文调度、批量参数和关闭取消调度；Playwright Chromium 定向 E2E 覆盖自动调度、业务 ACK、API 状态和页面回显 | `PaymentNotificationServiceTest`、`PaymentMangoPayScenarioControlServiceTest`、`PaymentNotificationDispatchSchedulerTest`、`payment-center.spec.ts` | DONE |
| PAY-CHANNEL-002 / PAY-API-010 / PAY-DOMAIN-002 | `PaymentChannelOrderQueryService`、`PaymentChannelOrderCloseService`、`PaymentNotificationService`、异常订单页面、通知记录页面 | 支付失败和支付关闭终态可通过真实状态流触发业务通知 | 真实收银台支付订单 `PO\\d{16}`，异常单 `EX-NT-F-*`/`EX-NT-C-*`，订单级 `notifyUrl` 指向 Playwright 本地真实 HTTP ACK 接收服务 | 异常订单“主动查单”消费芒果支付 `FAILED` 场景，将支付订单推进为 `FAILED` 后发送 `PAYMENT_FAILED`；异常订单“关闭支付订单”将支付订单和业务订单推进为 `CLOSED` 后发送 `PAYMENT_CLOSED`；两类通知均创建真实 `payment_notification_record`，真实 HTTP POST 到业务方，收到 `SUCCESS` ACK 后记录更新为 `SUCCESS`；通知记录页面按通知单号和成功状态可回显 | 异常订单处理弹窗、通知记录列表搜索、状态下拉、状态标签和表格回显通过 Chromium 验证；截图 `test-results/payment-terminal-notifications.png` | `PaymentNotificationServiceTest` 覆盖支付失败/关闭事件；`PaymentChannelOrderQueryServiceTest` 覆盖失败查单通知；`PaymentChannelOrderCloseServiceTest` 覆盖关单通知；Playwright Chromium 定向 E2E 真实浏览器操作通过，console/pageerror/payment requestfailed 均无异常 | `PaymentNotificationServiceTest`、`PaymentChannelOrderQueryServiceTest`、`PaymentChannelOrderCloseServiceTest`、`payment-center.spec.ts` | DONE |

## 4. 验证命令

```bash
mvn -f mango/pom.xml -pl mango-platform/mango-payment/mango-payment-core -am -Dtest=PaymentNotificationServiceTest,PaymentMangoPayScenarioControlServiceTest,PaymentMangoPayResultMappingServiceTest,PaymentReconciliationServiceTest,PaymentSettlementSummaryServiceTest,PaymentChannelRefundQueryServiceTest,PaymentOpenApiServiceTest,PaymentChannelOrderCloseServiceTest,PaymentChannelOrderQueryServiceTest,PaymentReadonlyResourceServiceTest,PaymentCashierServiceImplTest,PaymentOrderStateServiceTest -Dsurefire.failIfNoSpecifiedTests=false test -DskipTests=false
mvn -f mango/pom.xml -pl mango-platform/mango-payment/mango-payment-core -am -Dtest=PaymentChannelAdapterRegistryTest,PaymentMangoPayChannelAdapterTest,PaymentChannelOrderQueryServiceTest,PaymentChannelRefundQueryServiceTest -Dsurefire.failIfNoSpecifiedTests=false test -DskipTests=false
mvn -f mango/pom.xml -pl mango-platform/mango-payment/mango-payment-core -am -Dtest=PaymentMangoPayChannelAdapterTest,PaymentChannelAdapterRegistryTest,PaymentCashierServiceImplTest,PaymentOpenApiServiceTest,PaymentChannelOrderQueryServiceTest,PaymentChannelRefundQueryServiceTest -Dsurefire.failIfNoSpecifiedTests=false test -DskipTests=false
mvn -f mango/pom.xml -pl mango-platform/mango-payment/mango-payment-core -am -Dtest=PaymentMangoPayChannelAdapterTest,PaymentReconciliationServiceTest,PaymentChannelOrderQueryServiceTest,PaymentChannelRefundQueryServiceTest,PaymentCashierServiceImplTest,PaymentOpenApiServiceTest -Dsurefire.failIfNoSpecifiedTests=false test -DskipTests=false
mvn -f mango/pom.xml -pl mango-platform/mango-payment/mango-payment-core -am -Dtest=PaymentChannelRefundQueryServiceTest,PaymentChannelCallbackServiceTest -Dsurefire.failIfNoSpecifiedTests=false test -DskipTests=false
mvn -f mango/pom.xml -pl mango-platform/mango-payment/mango-payment-core -am -Dtest=PaymentReconciliationServiceTest,PaymentReconciliationMapperIntegrationTest,PaymentMangoPayChannelAdapterTest -Dsurefire.failIfNoSpecifiedTests=false test -DskipTests=false
mvn -f mango/pom.xml -pl mango-platform/mango-payment/mango-payment-starter -am -Dtest=PaymentNotificationDispatchSchedulerTest -Dsurefire.failIfNoSpecifiedTests=false test -DskipTests=false
PLAYWRIGHT_USE_EXTERNAL_WEBSERVER=true PLAYWRIGHT_BASE_URL=http://127.0.0.1:7808 PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:18118 pnpm --dir mango-ui/apps/mango-admin exec playwright test e2e/specs/payment-center.spec.ts --project=chromium --grep "到期通知可由支付模块调度器自动投递并在页面回显成功"
PLAYWRIGHT_USE_EXTERNAL_WEBSERVER=true PLAYWRIGHT_BASE_URL=http://127.0.0.1:7808 PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:18118 pnpm --dir mango-ui/apps/mango-admin exec playwright test e2e/specs/payment-center.spec.ts --project=chromium --grep "异常订单页面可对真实支付订单执行主动查单和关闭支付订单"
PLAYWRIGHT_USE_EXTERNAL_WEBSERVER=true PLAYWRIGHT_BASE_URL=http://127.0.0.1:7808 PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:18118 pnpm --dir mango-ui/apps/mango-admin exec playwright test e2e/specs/payment-center.spec.ts --project=chromium --grep "退款订单列表、状态筛选和详情真实可用"
python3 scripts/payment_normalize_mango_pay.py
python3 scripts/payment_normalize_mango_pay.py --apply
PLAYWRIGHT_USE_EXTERNAL_WEBSERVER=true PLAYWRIGHT_BASE_URL=http://127.0.0.1:7808 PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:18118 pnpm --dir mango-ui/apps/mango-admin exec playwright test e2e/specs/payment-center.spec.ts --project=chromium --grep "对账管理可从页面生成芒果支付账单并回显真实明细"
PLAYWRIGHT_USE_EXTERNAL_WEBSERVER=true PLAYWRIGHT_BASE_URL=http://127.0.0.1:7808 PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:18118 pnpm --dir mango-ui/apps/mango-admin exec playwright test e2e/specs/payment-center.spec.ts --project=chromium --grep "支付失败关闭和退款失败通知可由真实状态流触发并在页面回显"
PLAYWRIGHT_USE_EXTERNAL_WEBSERVER=true PLAYWRIGHT_BASE_URL=http://127.0.0.1:7808 PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:18118 pnpm --dir mango-ui/apps/mango-admin exec playwright test e2e/specs/payment-center.spec.ts --project=chromium --workers=1
```

## 5. 未验证项和风险

| 项目 | 原因 | 影响 | 后续处理 | 用户确认 |
|---|---|---|---|---|
| 长时间运行调度 | 本轮已补自动调度定向 E2E，覆盖真实到期通知、业务 ACK、API 状态和通知记录页面回显；完整支付中心 E2E 已通过，但尚未做长时间运行稳定性、多租户批量和运维告警验证 | 不阻塞 `PAY-CHANNEL-002` 的芒果支付内置虚拟通道完整能力关闭；继续阻塞 `PAY-SCHED-001` | 后续补长时间运行、失败重试、多租户批量调度和告警验证 | 不适用 |
| 终态通知长周期稳定性 | 本轮已补支付失败、支付关闭、退款失败三类终态通知的真实状态流和定向 E2E；完整支付中心 E2E 已通过，但尚未做多租户批量、长时间失败重试和外部通道回调通知稳定性验收 | 不阻塞 `PAY-CHANNEL-002`；多租户批量和调度稳定性继续归属 `PAY-SCHED-001`，外部通道回调通知归属对应外部通道台账 | 后续按调度和外部通道台账补验证 | 不适用 |
| 通联、华夏、微信、支付宝等外部通道 | 本轮只处理芒果支付，不接外部机构 | 不能声明 `PAY-CHANNEL-003/005/007/008` 完成 | 后续拿到官方或内部联调资料后逐通道实现 | 不适用 |
