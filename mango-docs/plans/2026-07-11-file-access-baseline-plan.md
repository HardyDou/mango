# 文件访问权限基线实施计划

1. 将基础文件接口统一为 `LOGIN`，保留列表、归档、删除和管理权限。
2. 保持分页、详情、上传结果和预览查询直接返回 `previewUrl`、`downloadUrl`。
3. 默认启用 DIRECT 跨域安全签名，将预览和下载有效期统一为 86400 秒。
4. 移除前端上传、预览、下载按钮的旧角色权限限制。
5. 验证无角色权限登录用户、跨域签名链接、24 小时 TTL 和危险操作权限。
