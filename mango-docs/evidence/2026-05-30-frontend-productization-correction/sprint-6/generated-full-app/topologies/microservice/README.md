# Microservice Topology

The full preset includes a microservice runtime config skeleton for frontend composition. Backend service splitting depends on your deployment design and must keep required system/auth/authorization capabilities available.

Frontend runtime config can be switched to the microservice skeleton:

```bash
cp frontend/public/runtime-config.microservice.json frontend/public/runtime-config.json
```
