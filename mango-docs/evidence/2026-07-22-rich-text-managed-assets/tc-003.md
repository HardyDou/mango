# TC-003 远程图片导入安全边界

- 结果：EXCEPTION。
- 已执行：`RemoteImageAddressPolicyTest`、`RemoteFileImportServiceTest`、`FileImportControllerTest`、`FileControllerAccessModeTest` 和 `ApacheRemoteImageFetcherTest` 定向 Maven 测试。
- 结果：10/10 通过。地址策略覆盖 localhost、IP literal、混合 DNS；本地 HTTP fixture 覆盖合法 PNG、redirect 每跳重新校验、不转发 Authorization/Cookie/Referer、硬字节上限及 SVG/MIME magic 拒绝。
- 例外：HTTPS 与超时 fixture 尚未执行；完整模块 `verify` 执行到 49 条测试时为 48 passed、1 environment error，阻断原因为数据库账号仍是 `${MANGO_DB_USERNAME}`，MySQL Access denied。
