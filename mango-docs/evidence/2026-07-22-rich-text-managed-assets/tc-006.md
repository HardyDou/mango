# TC-006 兼容性回归

- 结果：PASS（common 包）。
- 命令：`pnpm --dir mango-ui --filter @mango/common test -- --run`；`pnpm --dir mango-ui --filter @mango/common build`。
- 结果：272 条测试通过，Vite common build 成功；默认、url、id、token 的现有断言保持通过。
- 备注：workspace typecheck 当前因未构建的 workspace 包与既有类型错误失败，不归因于本次 common 代码。
