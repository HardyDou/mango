# TC-005 Editor demo 附件工具栏

- 结果：EXCEPTION。
- 已实现：`EditorView.vue` 使用 `toolbar-actions` slot 组合 `MUpload`，固定 `purpose=attachment`、`access-level=PRIVATE`、`value-type=id`，并提供 `data-testid="mango-editor-attachment-upload"`。
- 阻断：`pnpm --dir mango-ui --filter @mango/admin-shell build` 被既有 workspace 依赖解析错误阻断：Rollup 无法解析 `@mango/job/style.css`；浏览器打开 `http://127.0.0.1:30039/#/components/editor` 时登录页获取登录机构返回“服务器内部错误”，未能进入 demo。
- 未完成：真实浏览器上传、附件列表、窄屏换行、console/network 截图。
