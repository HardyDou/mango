# TC-001 工具栏托管图片

- 结果：PASS（自动化）。
- 命令：`pnpm --dir mango-ui --filter @mango/common test -- --run`。
- 证据：common 测试 22 个文件、272 条测试通过；token 测试确认编辑态使用 previewUrl，对外序列化为 `mango-file:<id>`。
- 红线检查：未发现 Base64、Blob URL 或 previewUrl 出现在序列化 HTML。
