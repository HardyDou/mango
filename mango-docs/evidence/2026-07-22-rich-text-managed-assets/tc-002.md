# TC-002 复制粘贴来源分类

- 结果：PASS（前端自动化、接口测试和本地 HTTP fixture）。
- 证据：Editor 粘贴处理与 managedImages 序列化测试通过；既有 token 通过 `data-file-id`/token 识别，不重复上传；`FileImportControllerTest` 与 `RemoteFileImportServiceTest` 验证远程导入接口固定使用图片、PRIVATE 和现有保存链路。
- 限制：公网环境和真实登录态未执行，网络安全边界见 TC-003。
